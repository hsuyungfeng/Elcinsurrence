---
phase: 04-record-aggregator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/elc_audit_engine/record_aggregator/__init__.py
  - src/elc_audit_engine/record_aggregator/models.py
  - src/elc_audit_engine/record_aggregator/providers.py
  - src/elc_audit_engine/record_aggregator/aggregator.py
  - tests/fixtures/records/patient_A/records.json
  - tests/test_record_aggregator.py
autonomous: true
requirements: [REQ-record-aggregator]

must_haves:
  truths:
    - "純資料彙整層：無 LLM、無規則庫查詢（04-CONTEXT.md domain）"
    - "Provider 介面抽象化雲端/本地，時間窗邏輯集中在 aggregator（D-01/D-03）"
    - "四類紀錄型別共用 Record 基底（D-02），本地 Provider 讀 records.json 契約（D-04）"
    - "病歷缺席（目錄不存在）→ degraded 降級（C5）；JSON 損毀 → 拋 RecordProviderError（P0-2 教訓，D-05）"
    - "VisitRecord.soap_text 存原始文字，Phase 4 不依賴 Phase 3（D-06）"
  artifacts:
    - path: "src/elc_audit_engine/record_aggregator/models.py"
      provides: "Record 基底＋VisitRecord/LabRecord/ExamRecord/ImagingRecord＋PatientTimeline＋AggregationResult（frozen dataclass＋中文 docstring）"
    - path: "src/elc_audit_engine/record_aggregator/providers.py"
      provides: "RecordProvider ABC＋LocalFileProvider＋RecordProviderError/PatientRecordsNotFound"
    - path: "src/elc_audit_engine/record_aggregator/aggregator.py"
      provides: "build_timeline()：時間窗過濾＋排序＋降級處理"
    - path: "tests/fixtures/records/patient_A/records.json"
      provides: "四類紀錄 fixture（含窗內/窗外混合）"
    - path: "tests/test_record_aggregator.py"
      provides: "Provider 讀檔/降級/時間窗/排序/切換測試"
  key_links:
    - from: "src/elc_audit_engine/record_aggregator/aggregator.py"
      to: "src/elc_audit_engine/record_aggregator/providers.py"
      via: "from .providers import RecordProvider, PatientRecordsNotFound"
      pattern: "RecordProvider"
    - from: "tests/test_record_aggregator.py"
      to: "src/elc_audit_engine/record_aggregator/aggregator.py"
      via: "from elc_audit_engine.record_aggregator import build_timeline"
      pattern: "AggregationResult"
---

<objective>
Wave 1（Phase 4 唯一一輪）：交付 Provider 介面＋本地檔案 Provider＋半年病史時間軸，
滿足 REQ-record-aggregator 三項驗收：介面支援雲端/本地切換、本地 Provider 可運作、
可產出半年時間軸（就診/檢驗/檢查/影像清單）。

依 04-CONTEXT.md：純資料彙整層；Provider 只回傳全部紀錄、窗邏輯在 aggregator；
病歷缺席降級、JSON 損毀拋例外（P0-2 教訓）；SOAP 原文存 VisitRecord 不依賴 Phase 3。

Output: 4 個模組（models/providers/aggregator + __init__ 對外 API）、1 個 fixture、
1 個測試檔，全部測試綠。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-record-aggregator/04-CONTEXT.md
@src/elc_audit_engine/parsers/models.py
@config/settings.py
</context>

<interfaces>
<!-- 既有慣例（Phase 1/2/3 建立），Phase 4 必須延續 -->

型別慣例（frozen dataclass + 中文 docstring，比照 RuleResult/SubmissionCase）:
```python
@dataclass(frozen=True)
class RuleResult: ...
```

日期解析（Phase 2 建立，records.json 相容 8 碼格式時重用）:
```python
from elc_audit_engine.rule_repository.loaders.dates import parse_flexible_date
```

設定（Phase 1 建立）:
```python
DATA_DIR  # env-overridable，本地 Provider 預設 root = DATA_DIR/samples/records
```

對接鍵（Phase 3 建立）:
```python
SubmissionCase.record_no  # d3 病歷號 = Phase 5 串接 timeline 的 patient_id（D-07）
VisitRecord.soap_text     # 原始文字，Phase 5 呼叫 parse_soap_text（D-06）
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 資料型別（models.py）</name>
  <files>src/elc_audit_engine/record_aggregator/models.py</files>
  <read_first>
  - src/elc_audit_engine/parsers/models.py（frozen dataclass 慣例）
  </read_first>
  <automated>
    - Record 基底含 patient_id/date/source_note；四子類別欄位齊全（D-02）
    - PatientTimeline 含四類清單＋window_start/window_end＋source_provider＋excluded_counts
    - AggregationResult 含 timeline/degraded/reason
  </automated>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provider（providers.py）＋fixture</name>
  <files>src/elc_audit_engine/record_aggregator/providers.py, tests/fixtures/records/patient_A/records.json, tests/test_record_aggregator.py</files>
  <read_first>
  - 04-CONTEXT.md D-01/D-04/D-05
  </read_first>
  <automated>
    - RecordProvider 為 ABC（fetch_records + name），LocalFileProvider 實作之
    - 讀取 tests/fixtures/records/patient_A/records.json：四類紀錄皆解析、欄位正確
    - 病患目錄不存在 → PatientRecordsNotFound；JSON 損毀 → RecordProviderError
    - 日期 8 碼格式（YYYYMMDD）相容 ISO
  </automated>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 彙整器（aggregator.py）</name>
  <files>src/elc_audit_engine/record_aggregator/aggregator.py, tests/test_record_aggregator.py</files>
  <read_first>
  - 04-CONTEXT.md D-03/D-05/D-08
  </read_first>
  <automated>
    - build_timeline(provider, patient_id, months=6, end_date=None)：窗內（含邊界）紀錄依日期排序
    - 窗外紀錄計入 excluded_counts（不拋棄，供透明度）
    - PatientRecordsNotFound → AggregationResult(degraded=True, reason=...)
    - 空清單（病患存在但無紀錄）→ degraded=False
    - 時間窗位移正確（跨年、月底夾日 3/31−6m→9/30）
    - 自訂 end_date 生效；假 Provider（實作 RecordProvider）可替換 LocalFileProvider（切換驗證）
  </automated>
</task>

</tasks>
