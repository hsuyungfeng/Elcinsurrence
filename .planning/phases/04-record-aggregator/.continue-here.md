---
context: phase
phase: 04-record-aggregator
task: 04-01
total_tasks: 1
status: complete
last_updated: 2026-08-03T16:55:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [x] **病歷可缺席，但 infra 故障不得吞成「無病歷」** — C5 的降級只適用於「病歷缺席」（未接雲端／無檔案，PatientRecordsNotFound → degraded=True）；JSON 損毀等 infra 故障必須拋 `RecordProviderError`（P0-2 教訓，避免 Phase 5 把故障誤讀成「此病患無病歷」）。已由 `test_infra_error_propagates_not_degraded` 釘住。
- [x] **絕不 import/呼叫 `pageindex`** — 本階段無相關使用；commit 前 `grep -rn "pageindex" src/ tests/` 維持為空（docx_tree/__init__.py docstring 提及「不使用」除外）。

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 把 infra 故障降級成「無病歷」 | JSON 損毀被吞掉，Phase 5 誤判 | blocking | RecordProviderError 一律向外拋；只捕獲 PatientRecordsNotFound 降級 |
| Phase 4 依賴 Phase 3 | SOAP 分段拉到彙整層 | advisory | VisitRecord.soap_text 存原文；Phase 5 才呼叫 parse_soap_text（D-06） |
| 病歷進版控 | records.json 若含真實個資 | blocking | fixture 一律去識別化；真實病歷檔放 data/phi/（已 gitignore） |

<current_state>
Phase 4（病歷彙整器）**COMPLETE** — 04-01 單輪交付，14 新測試全綠，全套件 110 passed / 5 skipped。
</current_state>

<completed_work>

- **04-CONTEXT.md** — D-01~D-08 設計決策（Provider 介面／四類紀錄型別／半年時間軸／本地契約／降級語意／SOAP 對齊／join key／時間窗計算）
- **04-01-PLAN.md** — 單輪 Wave 1 規劃（3 tasks）
- **執行**：`record_aggregator/models.py`（Record 基底＋四類＋PatientTimeline＋AggregationResult）、`providers.py`（RecordProvider ABC＋LocalFileProvider＋RecordProviderError/PatientRecordsNotFound）、`aggregator.py`（build_timeline）、`__init__.py` 對外 API、`tests/fixtures/records/patient_A/records.json`、`tests/test_record_aggregator.py`（14 測試）
- **04-01-SUMMARY.md** — 交付摘要＋真實資料驗證
</completed_work>

<remaining_work>

- **Phase 4 已全部完成**
- **下一步：`/gsd-plan-phase 5`（三方比對器）** — 依賴 Phase 2/3/4（皆已完成）；消費：醫令清單（p4）→ get_rule()、診斷碼（d19/d20-d26）→ 醫療必要性、SOAP 分段（parse_soap_text）＋半年時間軸（build_timeline）→ 病歷佐證
- 非阻擋待辦：核減明細實體檔（Phase 3 reader 參數鎖死）、抽樣樣本檔 CSV、真實病歷樣本（進 data/phi/，不進版控）
</remaining_work>

<decisions_made>

- **D-01**：RecordProvider ABC，fetch_records 只回傳全部紀錄，時間窗邏輯集中 aggregator
- **D-02**：四類紀錄型別（Visit/Lab/Exam/Imaging）共用 Record 基底
- **D-03**：build_timeline(provider, patient_id, months=6, end_date=None) → PatientTimeline
- **D-04**：LocalFileProvider 讀 `<root>/<patient_id>/records.json`（ISO/8 碼日期相容）
- **D-05**：病歷缺席 → degraded=True（C5）；JSON 損毀 → RecordProviderError（P0-2）
- **D-06**：VisitRecord.soap_text 存原文，Phase 4 不依賴 Phase 3
- **D-07**：patient_id = Phase 3 d3 病歷號
- **D-08**：時間窗手動位移＋calendar.monthrange 夾日（不引入 dateutil）
</decisions_made>

<blockers>
- 無阻擋項。
</blockers>
