---
phase: 03-parsers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/elc_audit_engine/parsers/__init__.py
  - src/elc_audit_engine/parsers/models.py
  - src/elc_audit_engine/parsers/submission_xml.py
  - src/elc_audit_engine/parsers/deduction.py
  - src/elc_audit_engine/parsers/soap.py
  - src/elc_audit_engine/parsers/soap_keywords.py
  - tests/fixtures/submission_sample.xml
  - tests/fixtures/deduction_sample.csv
  - tests/test_submission_xml_parser.py
  - tests/test_deduction_parser.py
  - tests/test_soap_parser.py
autonomous: true
requirements: [REQ-parsers]

must_haves:
  truths:
    - "三個解析器皆為純資料轉換：無 LLM 呼叫、不查 rule_repository、無外部服務依賴（03-CONTEXT.md domain）"
    - "申報 XML 以 binary 讀檔、依宣告編碼依序嘗試 big5→cp950→big5hkscs→utf-8，全部失敗才報錯（D-01）"
    - "缺漏分級僅三種致命：缺 d1/d2、缺 d3、整案無 pdata（D-05）；d19 缺漏只警告（D-06）"
    - "未知欄位全數保留於 raw dict，d20-d26 明確認列為次診斷清單（D-02/D-03）"
    - "核減明細依 D-14d 18 欄順序實作，reader 層（編碼/分隔符/表頭）可注入參數，取得實體檔後只調參數不改欄位映射"
    - "SOAP 兩層策略：marker 偵測（高信度）→ 無標記才關鍵詞分類（低信度），關鍵詞無命中歸為未分類而非 subjective（D-10/D-11/D-12）"
  artifacts:
    - path: "src/elc_audit_engine/parsers/models.py"
      provides: "解析結果型別（frozen dataclass + 中文 docstring，沿用 RuleResult 慣例）"
    - path: "src/elc_audit_engine/parsers/submission_xml.py"
      provides: "parse_submission_xml(path) -> SubmissionParseResult，處理 Big5/CRLF/缺漏分級"
    - path: "src/elc_audit_engine/parsers/deduction.py"
      provides: "parse_deduction_file(path, ...) -> DeductionParseResult，18 欄 + 第 17 欄代碼拆分 + 雙數字格式正規化"
    - path: "src/elc_audit_engine/parsers/soap.py"
      provides: "parse_soap_text(text) -> SOAPDocument，marker/keyword 兩層 + 信度標記"
    - path: "src/elc_audit_engine/parsers/soap_keywords.py"
      provides: "由 soap-classifier.js 移植的 240+ 關鍵詞表（S/O/A/P + 權重）"
    - path: "tests/fixtures/submission_sample.xml"
      provides: "由 TOTFA.xml 抽取的代表性案例（d49 姓名已置換，D-19/D-20）"
    - path: "tests/fixtures/deduction_sample.csv"
      provides: "依 D-14d 官方範例 2 筆資料列建置的 fixture（身分證號已遮罩）"
    - path: "tests/test_submission_xml_parser.py"
      provides: "申報 XML 解析測試（編碼/CRLF/缺漏分級/拒收/raw 保留）"
    - path: "tests/test_deduction_parser.py"
      provides: "核減明細解析測試（欄序/日期西元分支/雙數字格式/第 17 欄拆分/reader 注入）"
    - path: "tests/test_soap_parser.py"
      provides: "SOAP 分段測試（marker 變體/keyword 分類/未分類/信度/關鍵詞量）"
  key_links:
    - from: "tests/test_submission_xml_parser.py"
      to: "src/elc_audit_engine/parsers/submission_xml.py"
      via: "from elc_audit_engine.parsers.submission_xml import parse_submission_xml"
      pattern: "SubmissionParseResult"
    - from: "src/elc_audit_engine/parsers/deduction.py"
      to: "src/elc_audit_engine/rule_repository/loaders/dates.py"
      via: "from elc_audit_engine.rule_repository.loaders.dates import parse_flexible_date"
      pattern: "parse_flexible_date"
---

<objective>
Wave 1（Phase 3 唯一一輪）：交付三個解析器（申報 XML／核減明細／SOAP），把三種輸入轉成下游（Phase 4/5/7）可用的結構化 Python 物件。

依 03-CONTEXT.md 的決策：純資料轉換層，無 LLM、無規則庫查詢；缺漏分級（D-05/D-06）；未知欄位保留（D-02）；次診斷升為一級欄位（D-03）；核減 reader 可注入（D-14b-rev）；SOAP 兩層＋信度（D-10/D-11）；關鍵詞移植自 soap-classifier.js 並修正「無命中→subjective」為「未分類」（D-12）。

Output: 5 個 parser 模組（models/submission_xml/deduction/soap/soap_keywords）、2 個 fixture（XML/CSV）、3 個測試檔，全部測試綠。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-parsers/03-CONTEXT.md
@src/elc_audit_engine/rule_repository/models.py
@src/elc_audit_engine/rule_repository/loaders/dates.py
@officialdocument/soapclass/soap-classifier.js
@TOTFA.xml
</context>

<interfaces>
<!-- 既有慣例（Phase 1/2 建立），Phase 3 必須延續 -->

日期解析（Phase 2 建立，直接重用）:
```python
from elc_audit_engine.rule_repository.loaders.dates import parse_flexible_date
parse_flexible_date("20210623")  # -> "2021-06-23"（8 碼西元）
parse_flexible_date("1120718")   # -> "2023-07-18"（7 碼民國）
```

型別慣例（frozen dataclass + 工廠 + 中文 docstring，比照 RuleResult）:
```python
@dataclass(frozen=True)
class RuleResult: ...
def not_found(code: str) -> RuleResult: ...
```

真實檔事實（TOTFA.xml，03-CONTEXT.md 實測）:
- 宣告 `encoding="Big5"`、CRLF 行尾；tdata 14 欄、633×ddata（dhead+dbody）、2624 pdata
- dbody 100% 出現欄位：d3 d8 d9 d11 d14 d15 d17 d18 d19 d28 d29 d30 d39 d40 d41 d49
- d19=主診斷（ICD-10）；d20-d26=次診斷（出現率 73.3%→0.2%，值如 S67191A/E782/K644）
- 規格衝突：C11 樣本 d3 無註解、D-16 稱 d3=樣本註記，但真實檔 d3=病歷號（M220518024）；依「真實檔優先」原則以病歷號建模並記錄差異
- 申復明細 18 欄（D-14d）：日期全為西元 8 碼；欄 1 零填補 10 碼 vs 欄 14 純數字；欄 17 `代碼-說明`；欄 16 自由中文（原樣保留）
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 解析結果型別（models.py）＋申報 XML 解析器（submission_xml.py）</name>
  <files>src/elc_audit_engine/parsers/models.py, src/elc_audit_engine/parsers/submission_xml.py, tests/fixtures/submission_sample.xml, tests/test_submission_xml_parser.py</files>
  <read_first>
  - src/elc_audit_engine/rule_repository/models.py（frozen dataclass 慣例）
  - TOTFA.xml 開頭 60 行（真實結構）
  </read_first>
  <automated>
    - 解析 tests/fixtures/submission_sample.xml：cases>=3、每案 raw dict 含全部欄位、次診斷清單正確
    - Big5 暫存檔（含中文姓名）可解析；cp950/utf-8 回退可解析；全失敗拋 SubmissionXmlError
    - CRLF 內容可解析
    - 缺 d1/d2、缺 d3、無 pdata 三種案例各自進 rejected 且原因明確；缺 d19 進 cases 且帶警告
    - d20-d26 非空值依序進 secondary_diagnoses；unknown 欄位（如 d57/p24）保留於 raw
    - 不 import rule_repository（純轉換層）
  </automated>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 核減明細解析器（deduction.py）</name>
  <files>src/elc_audit_engine/parsers/deduction.py, tests/fixtures/deduction_sample.csv, tests/test_deduction_parser.py</files>
  <read_first>
  - 03-CONTEXT.md D-14d 的 18 欄表格
  - src/elc_audit_engine/rule_repository/loaders/dates.py
  </read_first>
  <automated>
    - 解析 tests/fixtures/deduction_sample.csv（2 筆官方範例列）：18 欄欄序正確
    - 欄 1「0000000300」→ 300；欄 14「300」→ 300（雙格式正規化）
    - 欄 3「202106」原樣保留；欄 4/7/8/12/15 走 8 碼西元 → ISO 日期
    - 欄 17「A-檢驗結果確實於時效內上傳」→ code=A / desc=檢驗結果確實於時效內上傳；無「-」時 code=整串
    - 欄 16 追扣原因原樣保留
    - 有表頭列/無表頭列皆可解析（has_header 自動偵測）
    - 分隔符可注入（逗號/Tab）；編碼可注入（big5/utf-8 皆可）
    - 欄數不符列進 rejected（含列號與原因），不中斷整檔
  </automated>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SOAP 分段器（soap.py + soap_keywords.py）</name>
  <files>src/elc_audit_engine/parsers/soap.py, src/elc_audit_engine/parsers/soap_keywords.py, tests/test_soap_parser.py</files>
  <read_first>
  - officialdocument/soapclass/soap-classifier.js（關鍵詞結構與演算法）
  - 03-CONTEXT.md D-10/D-11/D-12
  </read_first>
  <automated>
    - soap_keywords 關鍵詞總數 >= 240，四類皆 > 50，含代表性詞（血壓/感冒/抗生素/疼痛）
    - marker 變體：`S:`、`S)`、`S：`、`【S】`、`主訴：` 均可定位；method=marker、confidence=high
    - marker 文件的首個標記前內容歸 unclassified
    - 無標記文字（JS demo 範例）走關鍵詞：method=keyword、confidence=low、四類皆有內容
    - 無關鍵詞命中的句子歸 UNKNOWN（修正 D-12，不預設 subjective）
    - 關鍵詞權重：主觀 1.0 / 客觀 0.95 / 評估 1.0 / 計劃 0.95（比對 JS 原始值）
  </automated>
</task>

<task type="auto">
  <name>Task 4: 真實檔回放測試（選用性 skip）</name>
  <files>tests/test_submission_xml_parser.py</files>
  <read_first>
  - tests/test_doc_converter.py（功能探測 skip 慣例）
  </read_first>
  <automated>
    - TOTFA.xml 存在時（os.path.isfile 探測）：解析出 633 cases、0 rejected、pdata 總數 2624、tdata 14 欄
    - TOTFA.xml 不存在時優雅 skip
  </automated>
</task>

</tasks>
