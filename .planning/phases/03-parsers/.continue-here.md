---
context: phase
phase: 03-parsers
task: null
total_tasks: null
status: ready_for_planning
last_updated: 2026-08-03T07:39:23.680Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **核減「輸入」≠ 申復「輸出」** — `電子申復格式及填表說明門診.doc`／`門診申復上傳格式作業說明.doc`／`30396_*申復清單` **全部都是院所要產出的申復輸出格式**（Phase 7 範疇），不是健保署發來的核減輸入格式。本 session 的 D-14 原本就是照輸出規格建模輸入檔而寫錯的。**結構性防範：** 動核減解析器前，先確認你參照的欄位表出自 `03-CONTEXT.md` 的 **D-14d**（18 欄申復明細資料檔），而非任何 `officialdocument/電子申復文件格式/` 底下的規格書。

- [ ] CONSTRAINT: **絕不 import/呼叫 `pageindex`** — 該 PyPI 套件是雲端 SaaS client，會讓病歷資料離開本機，違反 D2。樹狀索引已於 `src/elc_audit_engine/rule_repository/docx_tree/` 本地自建。**結構性防範：** commit 前 `grep -rn "pageindex" src/ tests/` 必須為空（`2201816` 已從 pyproject.toml 移除該依賴）。

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 照官方輸出規格建模輸入檔 | 把申復 XML 規格書當成核減檔規格，寫死錯誤 schema | blocking | 核減欄位一律以 D-14d 為唯一來源；其他文件僅可作語意對照 |
| import pageindex | 雲端 SaaS，違反 D2 病患資料不出本機 | blocking | commit 前 grep 驗證為空 |
| 自行擴大 PHI 清洗範圍 | fixture 去識別化只洗 `d49` 姓名，是使用者知情後的明示決定 | blocking | `d3`/`d11`/`d19`-`d26` 保留真實值；下游 agent 不得自行加洗 |
| 重複討論已結案決定 | 本 session 曾在 `/gsd-discuss-phase 3` 已完成後，又重新推導了一次核減格式 | advisory | 動手前先 `git log --oneline -10` 與讀 `03-CONTEXT.md`，確認該議題是否已有決策編號 |
| 假設健保檔案用民國年 | 申復明細資料檔的日期**全為西元 8 碼**（`20210623`），非民國 | advisory | 重用 `loaders/dates.py` 的 `parse_flexible_date()`，並加測試釘住此檔走西元分支 |

<current_state>
Phase 3（解析器）**context 已蒐集完成，尚未規劃** —— 目錄下有 `03-CONTEXT.md`、`03-DISCUSSION-LOG.md`，**沒有 PLAN.md**（故非執行中狀態）。

工作區乾淨（`git status` 無未提交項目），測試 **51 passed / 1 skipped**（skip 為 soffice 轉檔守衛，屬正常）。

本 session 的主軸不是寫程式，而是**修正 `03-CONTEXT.md` 中關於核減輸入格式的一連串錯誤假設**。使用者陸續提供三份新證據（官方問答集、VPN 下載畫面實況、申復明細欄位範例圖），每一份都推翻前一個假設。最終三個解析器都有了實作依據。

另外補提交了先前積壓的 P0/P1 修正（6 個原子 commit，`6e3718b`..`d5f8ea4`）。
</current_state>

<completed_work>

Completed Tasks:
- `/gsd-discuss-phase 3`（4 個灰色地帶）— Done（`5e1cd5c`、`678c8ef`）
- 核減輸入 vs 申復輸出釐清，新增 D-14a/D-14b — Done（`0f1df83`）
- 抽樣樣本檔為 CSV，新增 D-14c — Done（`e605d26`）
- 申復明細資料檔 18 欄欄位表，新增 D-14d — Done（`73f71d4`）
- 核減解析器納入範圍 D-14b-rev + progress.md 同步 — Done（`269adcc`）
- 積壓的 P0/P1 修正 6 個原子 commit — Done（`6e3718b` P0-2、`2593ce3` P0-1、`67ffc3c` P1-3、`d97046a` P1-4、`2201816` P1-6、`d5f8ea4` 調查文件）
</completed_work>

<remaining_work>

- `/gsd-plan-phase 3` — 尚未開始（**下一步**）
- Phase 3 執行：三個解析器
  1. 申報 XML parser（真實樣本 `TOTFA.xml`，633 案）
  2. 核減／申復明細 parser（依 D-14d 的 18 欄）
  3. SOAP parser（移植 240+ 關鍵詞表）
</remaining_work>

<decisions_made>

- **D-14a**：核減輸入與申復輸出是兩回事；`電子申復格式及填表說明門診.doc` 屬輸出規格，不得用來建模輸入檔
- **D-14c**：抽樣樣本檔為 CSV，兩種院所型態都拿得到；抽樣函為 PDF，不列入 Phase 3
- **D-14d**：登錄申復明細資料檔 18 欄官方欄位表 —— 日期為西元非民國、金額有零填補與純數字兩種格式並存、身分證號已遮罩但出生日期未遮罩、第 17 欄為 `代碼-說明` 複合欄、欄 5/6/10/11 為與申報 XML 的完整 join key
- **D-14b-rev**：核減解析器**納入** Phase 3 實作範圍（取代原本延後的 D-14b）；ROADMAP Phase 3 Goal 與三項成功條件**維持原樣不需修改**
- **P0-2**（breaking）：`get_rule()` 基礎設施故障改拋 `RuleRepositoryError`，不再降級成 `not_found()` —— 否則 Phase 5 會把故障誤讀成「查無規則依據」，對醫師謊報臨床結論
</decisions_made>

<blockers>
- 無阻擋項。三個解析器均有實作依據，可直接進規劃。
</blockers>

## Required Reading (in order)

1. `.planning/phases/03-parsers/03-CONTEXT.md` — **最重要**。D-01~D-21 完整決策；特別注意 D-14a/D-14c/D-14d/D-14b-rev 這組是本 session 反覆修正後的最終結論，D-14/D-14b 已作廢（保留刪除線供追溯）
2. `.planning/phases/03-parsers/03-DISCUSSION-LOG.md` — 4 個灰色地帶的討論脈絡
3. `.planning/intel/constraints.md` — C5（錯誤處理分級）、C11（欄位表，**已證實為子集非完整 schema**）、C8（p8/p9 字數，**描述有誤，以官方問答集 Q15 為準：每欄 1000 中文字／合計 2000**）
4. `.planning/ROADMAP.md` 第 59 行起 — Phase 3 Goal 與三項成功條件（**不需修改**）
5. `src/elc_audit_engine/rule_repository/models.py` — `RuleResult` 契約（Phase 3 解析器**不呼叫** `get_rule()`，但需知道下游型別）

## Critical Anti-Patterns (do NOT repeat these)

- [ANTI-PATTERN]: 用申復輸出規格建模核減輸入 → 核減欄位只認 D-14d
- [ANTI-PATTERN]: import `pageindex` → commit 前 grep 驗證為空
- [ANTI-PATTERN]: 擴大 fixture PHI 清洗範圍 → 只洗 `d49`，其餘保留（使用者明示決定）
- [ANTI-PATTERN]: 假設日期為民國年 → 申復明細全為西元 8 碼
- [ANTI-PATTERN]: 重複推導已結案議題 → 先讀 `03-CONTEXT.md` 的決策編號與 `git log`

## Infrastructure State

- **測試**：51 passed / 1 skipped（`uv run pytest -q`，約 128s）。skip 為 soffice 轉檔守衛，環境無法真實轉檔時的正常行為
- **ruff**：**未安裝**（`uv run ruff check .` → `Failed to spawn: ruff`）。專案記憶標示 `lint=ruff check` 但實際不可用；本 session 的 6 個 commit 僅有 pytest 驗證
- **llama.cpp server**：localhost:8080（Phase 2 建置期使用；Phase 3 解析器**不需要**，純資料轉換無 LLM 呼叫）
- **資料產物**：`data/db/rules.sqlite3`（payment 2,669 / drug 11,273 / rule_mapping 13,942）、`data/db/docx_trees.json`（32 檔 1,633 節點）、ChromaDB `data/rag/`（282 chunks，P1-3 重建後）
- **PHI**：`TOTFA.xml`（348 位病患）已 gitignore；`*.xml` 全域忽略 + `!tests/fixtures/**/*.xml` 例外

<context>
本 session 學到的最重要一件事：**這個專案的官方文件很容易誤導**。三次假設被推翻，全都是同一個模式 —— 文件寫的是「院所要送出去什麼」，我卻讀成「院所會收到什麼」。核減／抽樣是**輸入**，申復是**輸出**，兩者在健保體系裡是不同時間點的不同檔案，但檔名和欄位代號長得很像（都有 `d1`/`d2`/`p1`）。

另一個模式：使用者手上有大量本機沒有的實務知識（VPN 畫面長怎樣、實際收到什麼檔）。遇到格式不明時，**問使用者比翻文件有效**。

Phase 3 規劃時的核心判斷：三個解析器的證據強度不同 ——
- 申報 XML：**最強**，633 案真實檔可統計驗證
- 核減申復明細：**中等**，欄位順序確定但無實體檔，reader 層須參數化
- SOAP：**中等**，有 240+ 關鍵詞表可移植，但真實病歷樣本尚未取得

建議規劃時把「reader 層可注入參數」當成核減解析器的硬需求，這樣拿到實體檔只調參數、不動欄位映射。
</context>

<next_action>
Start with: `/gsd-plan-phase 3`

前置條件全部滿足 —— context 齊備、工作區乾淨、測試全綠、無阻擋項。
規劃時務必先讀 `03-CONTEXT.md` 的 D-14a/D-14c/D-14d/D-14b-rev（本 session 的最終結論），不要採用已作廢的 D-14/D-14b。
</next_action>
