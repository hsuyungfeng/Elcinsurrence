---
context: phase
phase: 05-three-way-comparator
task: 05-01
total_tasks: 1
status: complete
last_updated: 2026-08-03T17:35:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [x] **infra 故障不得吞成「查無規則」** — `get_rule` 的 `RuleRepositoryError`（DB 故障）在 `compare_case` 中穿透（P0-2）；只有 `found=False`（正常查無）才標未知醫令「查無規則依據，建議人工查核」（C5）。
- [x] **LLM 失敗不得阻斷整案** — 判定/補強失敗降級「待人工」或空清單（C5），`manual_review` 旗標供 Phase 6。
- [x] **候選補強不得憑空編造** — C2：只能基於既有線索擴寫；無線索時 `prompt_only=True` 提示型；每條附 `article_location`。
- [x] **絕不 import/呼叫 `pageindex`** — commit 前 `grep -rn "pageindex" src/ tests/` 維持為空。

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 把 DB 故障降級成未知醫令 | Phase 6 會對醫師謊報「查無規則」 | blocking | RuleRepositoryError 穿透；測試 `test_compare_case_rule_repository_error_propagates` |
| 測試依賴真實 LLM server | CI/沙箱無 llama.cpp 時測試掛 | blocking | judge_fn/narrative_fn 注入替身；LLM 解析/重試邏輯以 mock 驗證（D-08） |
| 檢核項憑空拆分 | 無結構化語料硬拆適應症/頻率 → 幻構 | advisory | D-01：檢核項＝規則全文＋出處；Phase 8 真實樣本回放後再評估結構化 |

<current_state>
Phase 5（三方比對器）**COMPLETE** — 05-01 單輪交付，29 新測試全綠，全套件 139 passed / 5 skipped。
</current_state>

<completed_work>

- **05-CONTEXT.md** — D-01~D-08 設計決策（檢核項＝規則全文／證據組裝／LLM 判定可注入／三級純函式／候選補強可注入／錯誤語意／病歷缺席降級／零 LLM 依賴測試）
- **05-01-PLAN.md** — 單輪 Wave 1 規劃（3 tasks）
- **執行**：`comparator/models.py`（Verdict/CheckItem/Judgment/OrderJudgment/CandidateNarrative/CaseComparisonResult）、`evidence.py`（build_evidence_blocks）、`support.py`（classify_support）、`judger.py`（LLMJudger＋create_judger）、`narratives.py`（LLMNarrativeGenerator＋create_generator）、`comparator.py`（compare_case）、`__init__.py` 對外 API、`tests/test_comparator.py`（29 測試）
- **05-01-SUMMARY.md** — 交付摘要＋真實 TOTFA 首案煙霧測試
</completed_work>

<remaining_work>

- **Phase 5 已全部完成**
- **下一步：`/gsd-plan-phase 6`（輸出一：病歷補強報告）** — 依賴 Phase 5（已完成）；消費 CaseComparisonResult，產出 Markdown checkbox 逐條審格式（D9）
- 非阻擋待辦：LLM 判定評測 30 組金標準（Phase 8，C6 第 3 層）、結構化檢核項萃取評估（Phase 8）
</remaining_work>

<decisions_made>

- **D-01**：檢核項＝規則全文＋出處（article_full_text or payment_text）
- **D-02**：build_evidence_blocks 組裝 SOAP＋半年病史（每類至多 3 筆、500 字截斷）
- **D-03**：LLMJudger JSON 強制＋重試一次＋失敗降級待人工（C1/C5）
- **D-04**：classify_support 三級純函式（充分/薄弱/裸奔）＋manual_review
- **D-05**：LLMNarrativeGenerator 缺口 1~3 條、C2 約束（prompt_only 提示型、附出處）
- **D-06**：RuleRepositoryError 穿透；found=False 才標未知醫令（P0-2）
- **D-07**：timeline=None → records_degraded=True（C5）
- **D-08**：judge_fn/narrative_fn/rule_lookup 全可注入，測試零 LLM 依賴
</decisions_made>

<blockers>
- 無阻擋項。
</blockers>
