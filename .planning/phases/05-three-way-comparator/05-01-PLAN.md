---
phase: 05-three-way-comparator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/elc_audit_engine/comparator/__init__.py
  - src/elc_audit_engine/comparator/models.py
  - src/elc_audit_engine/comparator/evidence.py
  - src/elc_audit_engine/comparator/support.py
  - src/elc_audit_engine/comparator/judger.py
  - src/elc_audit_engine/comparator/narratives.py
  - src/elc_audit_engine/comparator/comparator.py
  - tests/test_comparator.py
autonomous: true
requirements: [REQ-three-way-comparator]

must_haves:
  truths:
    - "逐檢核項判定支持/部分支持/無記載＋引用原文（C1）；強制 JSON，失敗重試一次，仍失敗降級待人工（C5）"
    - "醫令支持度三級分類充分/薄弱/裸奔（D7/D-04），分級為純函式不依賴 LLM"
    - "缺口生成 1~3 條候選補強敘述，僅基於既有線索擴寫、無線索時提示型、每條附規則出處（C2/D-05）"
    - "RuleRepositoryError（DB 故障）穿透不吞；get_rule found=False 才標未知醫令（P0-2/D-06）"
    - "timeline=None（病歷缺席）時只用當次 SOAP，records_degraded=True（C5/D-07）"
    - "judger/generator 可注入，測試零 LLM 依賴（D-08）"
  artifacts:
    - path: "src/elc_audit_engine/comparator/models.py"
      provides: "Verdict 常數＋CheckItem＋Judgment＋OrderJudgment＋CandidateNarrative＋CaseComparisonResult（frozen dataclass＋中文 docstring）"
    - path: "src/elc_audit_engine/comparator/evidence.py"
      provides: "build_evidence_blocks(case, soap_doc, timeline) — 病歷段落組裝＋長度上限"
    - path: "src/elc_audit_engine/comparator/support.py"
      provides: "classify_support(judgments) — 三級分類純函式"
    - path: "src/elc_audit_engine/comparator/judger.py"
      provides: "LLMJudger（JSON 判定＋重試＋待人工降級）＋create_judger 注入點"
    - path: "src/elc_audit_engine/comparator/narratives.py"
      provides: "generate_narratives（LLM 或注入；C2 約束）"
    - path: "src/elc_audit_engine/comparator/comparator.py"
      provides: "compare_case(case, soap_doc, timeline, ...) 主流程 → CaseComparisonResult"
    - path: "tests/test_comparator.py"
      provides: "證據組裝/三級分類/判定流程/未知醫令/DB 故障穿透/病歷缺席/候選補強/注入替身測試"
  key_links:
    - from: "src/elc_audit_engine/comparator/comparator.py"
      to: "src/elc_audit_engine/rule_repository/__init__.py"
      via: "from elc_audit_engine.rule_repository import get_rule"
      pattern: "get_rule"
    - from: "src/elc_audit_engine/comparator/evidence.py"
      to: "src/elc_audit_engine/parsers/soap.py"
      via: "from elc_audit_engine.parsers.soap import parse_soap_text"
      pattern: "SOAPDocument"
---

<objective>
Wave 1（Phase 5 唯一一輪）：交付三方比對器核心，滿足 REQ-three-way-comparator
三項驗收：逐檢核項判定＋引用原文（C1）、三級分類（D7）、缺口候選補強（C2）。

依 05-CONTEXT.md：檢核項=規則全文（D-01）、證據組裝含上限（D-02）、LLM 判定
可注入＋JSON＋重試＋待人工（D-03）、三級純函式（D-04）、候選補強可注入＋C2
（D-05）、RuleRepositoryError 穿透（D-06）、病歷缺席降級（D-07）、零 LLM
依賴測試（D-08）。

Output: 6 個 comparator 模組＋對外 API＋1 個測試檔，全部測試綠。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-three-way-comparator/05-CONTEXT.md
@.planning/intel/constraints.md
@src/elc_audit_engine/rule_repository/__init__.py
@src/elc_audit_engine/rule_repository/models.py
@src/elc_audit_engine/rule_repository/mapping/llm_client.py
@src/elc_audit_engine/parsers/models.py
@src/elc_audit_engine/parsers/soap.py
@src/elc_audit_engine/record_aggregator/models.py
</context>

<interfaces>
<!-- 既有介面，Phase 5 消費 -->

規則庫（Phase 2，查詢路徑零 LLM）:
```python
get_rule(code, db_path=None) -> RuleResult   # found=False 查無；sqlite 故障拋 RuleRepositoryError（P0-2）
```

Phase 3 解析輸出:
```python
SubmissionCase.record_no / primary_diagnosis / secondary_diagnoses / orders: tuple[OrderRecord, ...]
OrderRecord.code / seq / category / frequency / route / days
parse_soap_text(text) -> SOAPDocument   # sections S/O/A/P、method、confidence
```

Phase 4 彙整輸出:
```python
build_timeline(provider, patient_id, ...) -> AggregationResult
PatientTimeline.visits/labs/exams/imaging / window_start / window_end / degraded
VisitRecord.soap_text / LabRecord.result / ImagingRecord.impression
```

LLM 薄封裝（Phase 2 建立，Phase 5 重用）:
```python
llm_client.chat_completion(system_prompt, user_prompt, max_tokens=None, temperature=None) -> str
llm_client.smoke_test() -> str
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 型別（models.py）</name>
  <files>src/elc_audit_engine/comparator/models.py</files>
  <read_first>
  - src/elc_audit_engine/rule_repository/models.py（frozen dataclass 慣例）
  </read_first>
  <automated>
    - Verdict 常數：SUPPORTED/PARTIAL/UNSUPPORTED/MANUAL（支持/部分支持/無記載/待人工）
    - CheckItem（rule_text/rule_source/rule_location）＋Judgment（verdict/quote/reason）
    - OrderJudgment（order/rule_found/rule_source/check_item/judgment/support_level/manual_review/note）
    - CandidateNarrative（text/rule_location/prompt_only）
    - CaseComparisonResult（case/order_judgments/records_degraded/unknown_orders/manual_review_orders）
  </automated>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 證據組裝＋三級分類（evidence.py/support.py）</name>
  <files>src/elc_audit_engine/comparator/evidence.py, src/elc_audit_engine/comparator/support.py, tests/test_comparator.py</files>
  <read_first>
  - 05-CONTEXT.md D-02/D-04
  </read_first>
  <automated>
    - build_evidence_blocks：含當次 SOAP S/O/A/P 段落；timeline 提供時含半年病史四類；每類至多 3 筆、每筆截斷 500 字
    - timeline=None 時不產生病史區塊（只 SOAP）
    - classify_support：三級判定正確（充分/薄弱/裸奔）；任一待人工 → manual_review=True
  </automated>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LLM 判定＋候選補強＋主流程（judger.py/narratives.py/comparator.py）</name>
  <files>src/elc_audit_engine/comparator/judger.py, src/elc_audit_engine/comparator/narratives.py, src/elc_audit_engine/comparator/comparator.py, tests/test_comparator.py</files>
  <read_first>
  - 05-CONTEXT.md D-03/D-05/D-06/D-07/D-08
  - src/elc_audit_engine/rule_repository/mapping/llm_client.py
  </read_first>
  <automated>
    - LLMJudger：JSON 解析成功→Judgment；格式錯誤→換措辭重試一次；仍失敗→待人工
    - create_judger(judge_fn=None)：注入替身走注入路徑（不碰網路）
    - compare_case：每筆 order 呼叫 get_rule（可注入 db_path/rule_lookup）
      - found=False → OrderJudgment(rule_found=False, note=查無規則依據，建議人工查核, support_level=None)
      - RuleRepositoryError 穿透（pytest.raises）
      - 判定→三級分類→薄弱/裸奔時 generate_narratives（可注入）
      - timeline=None → records_degraded=True
    - 候選補強 1~3 條、每條附規則出處、無線索時 prompt_only=True
  </automated>
</task>

</tasks>
