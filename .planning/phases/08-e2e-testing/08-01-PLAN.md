---
phase: 08-e2e-testing
plan: 01
type: execute
wave: 1
depends_on: [05-three-way-comparator, 06-output-reinforcement-report, 07-output-appeal-draft]
files_modified:
  - src/elc_audit_engine/comparator/support.py
  - src/elc_audit_engine/generators/appeal.py
  - src/elc_audit_engine/eval/gold_standard.py
  - src/elc_audit_engine/eval/__init__.py
  - src/elc_audit_engine/pipeline.py
  - tests/test_comparator.py
  - tests/test_gold_standard.py
  - tests/test_e2e_pipeline.py
  - tests/fixtures/llm_gold_standard_30.json
  - scripts/replay_gold_standard.py
autonomous: true
requirements: [REQ-e2e-testing]

must_haves:
  truths:
    - "五層測試策略全數涵蓋並可執行（C6）：單元（已齊）＋規則庫驗收（已齊）＋LLM 判定金標準 30 組＋端到端 3 案例＋真實樣本回放介面"
    - "LLM 判定金標準 30 組：fixture（支持12/部分支持9/無記載9）＋可注入 judge_fn 的 evaluate harness＋真實 LLM 回放 CLI（health guard，C6-3）"
    - "端到端 3 案例（充分/薄弱/裸奔各一）經真實管線：compare_case→write_report→審核 decisions→build_appeal_draft→write_appeal（C6-4）"
    - "E2E-01 修正：classify_support 任一『部分支持』→ 薄弱（原歸充分使薄弱不可達，D7 三級缺一角）；測試一併更新"
    - "run_case_pipeline 單一入口：注入層（rule_lookup/judge_fn/narrative_fn/decisions）可替換，保留真實樣本替換空間"
  artifacts:
    - path: "src/elc_audit_engine/eval/gold_standard.py"
      provides: "GoldCase＋load_gold_standard＋evaluate（準確率/per_verdict/mismatches，judge_fn 可注入）"
    - path: "tests/fixtures/llm_gold_standard_30.json"
      provides: "30 組檢核項×病歷段落金標準（支持12/部分支持9/無記載9）"
    - path: "scripts/replay_gold_standard.py"
      provides: "真實 LLM 回放 CLI（health guard；換模型回歸基準）"
    - path: "src/elc_audit_engine/pipeline.py"
      provides: "run_case_pipeline：比對→補強報告＋審核軌跡→逐筆申復草稿"
    - path: "tests/test_e2e_pipeline.py"
      provides: "充分/薄弱/裸奔三案例端到端斷言"
    - path: "src/elc_audit_engine/comparator/support.py"
      provides: "E2E-01 修正：部分支持→薄弱"
  key_links:
    - from: "src/elc_audit_engine/eval/gold_standard.py"
      to: "src/elc_audit_engine/comparator/judger.py"
      via: "from elc_audit_engine.comparator.judger import JudgeFn"
      pattern: "JudgeFn"
    - from: "src/elc_audit_engine/pipeline.py"
      to: "src/elc_audit_engine/comparator/comparator.py"
      via: "from elc_audit_engine.comparator import compare_case"
      pattern: "compare_case"
    - from: "src/elc_audit_engine/pipeline.py"
      to: "src/elc_audit_engine/generators/appeal.py"
      via: "from elc_audit_engine.generators import build_appeal_draft, write_appeal"
      pattern: "build_appeal_draft"
    - from: "tests/test_e2e_pipeline.py"
      to: "src/elc_audit_engine/pipeline.py"
      via: "from elc_audit_engine.pipeline import run_case_pipeline"
      pattern: "run_case_pipeline"
---

<objective>
Wave 1（Phase 8 唯一一輪）：把 C6 五層測試補齊 — LLM 判定金標準 30 組
（fixture＋harness＋回放 CLI）＋端到端 3 案例（充分/薄弱/裸奔，經真實
管線），並修正 Phase 8 發現的 E2E-01（部分支持→薄弱）。五層全數可執行、
全套件全綠。

Output: eval 套件（金標準）＋ pipeline 單一入口＋ 2 個新測試檔＋ 1 個
fixture＋ 1 個回放 CLI＋ classify_support 小修正，全部測試綠。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-e2e-testing/08-CONTEXT.md
@.planning/intel/constraints.md（C6 五層）
@src/elc_audit_engine/comparator/support.py
@src/elc_audit_engine/comparator/comparator.py
@src/elc_audit_engine/generators/appeal.py
@src/elc_audit_engine/generators/tracking.py
@tests/test_comparator.py（E2E-01 鎖定測試位置）
</context>

<interfaces>
既有介面，Phase 8 消費:

```python
# Phase 5：比對
compare_case(case, soap_doc, timeline, *, rule_lookup, judge_fn, narrative_fn)
  -> CaseComparisonResult
classify_support(judgments) -> (support_level, manual)   # 本階段修正

# Phase 6：輸出＋審核軌跡
write_report(output_dir, case_record_no, comparison, decisions, timeline, reviewed_at)
render_tracking(comparison, decisions, reviewed_at) -> str

# Phase 7：申復草稿
build_appeal_draft(record, *, is_appealing, claimed_points, timeline,
                   rule_text, rule_location, evidence, has_attachment)
adopted_narratives_from_tracking(tracking)   # 本階段加 order_code 鍵
write_appeal(output_dir, case_seq, draft, *, file_stem)
```

設定（Phase 1）:
```python
OUTPUT_DIR  # env-overridable，管線預設輸出目錄
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 金標準 30 組＋harness＋回放 CLI（C6-3）</name>
  <files>src/elc_audit_engine/eval/gold_standard.py, src/elc_audit_engine/eval/__init__.py, tests/fixtures/llm_gold_standard_30.json, scripts/replay_gold_standard.py, tests/test_gold_standard.py</files>
  <read_first>
  - 08-CONTEXT.md D-01/D-02
  - src/elc_audit_engine/comparator/judger.py（JudgeFn）
  </read_first>
  <automated>
    - fixture 恰 30 筆、id 唯一、expected_verdict ∈ {支持,部分支持,無記載}、分佈 12/9/9
    - load_gold_standard 回傳 GoldCase tuple；缺檔/壞檔拋明確例外
    - evaluate(fake_judge) 全部答對 → accuracy=1.0；部分答錯 → 準確率與 mismatches 正確
    - replay CLI 具 health guard：伺服器未啟動 exit 1 且輸出說明；importable 供測試
  </automated>
</task>

<task type="auto" tdd="true">
  <name>Task 2: E2E-01 修正（部分支持→薄弱）＋鎖定測試</name>
  <files>src/elc_audit_engine/comparator/support.py, tests/test_comparator.py</files>
  <read_first>
  - 08-CONTEXT.md E2E-01/D-05
  </read_first>
  <automated>
    - classify_support([部分支持]) → 薄弱（不再充分）
    - classify_support([支持]) → 充分、[無記載] → 裸奔、[支持,無記載] → 薄弱（既有規則不變）
    - 原 test_classify_support_partial_counts_as_support 改為斷言薄弱
  </automated>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 端到端管線＋3 案例（C6-4）</name>
  <files>src/elc_audit_engine/pipeline.py, src/elc_audit_engine/generators/appeal.py, tests/test_e2e_pipeline.py</files>
  <read_first>
  - 08-CONTEXT.md D-03/D-04
  - src/elc_audit_engine/generators/appeal.py（adopted_narratives_from_tracking）
  </read_first>
  <automated>
    - adopted_narratives_from_tracking 輸出具 order_code 鍵（向後相容）
    - run_case_pipeline：compare→write_report→render_tracking→逐筆 appeal（依 order_code 過濾證據、規則全文注入、RuleRepositoryError 不阻斷）
    - 三案例參數化：充分（支持）／薄弱（部分支持）／裸奔（無記載）各一
    - 每案例斷言：報告徽章、候選補強、軌跡 JSON 狀態、appeal md/json（p1-p9、四段）
    - 同一流水號多筆核減 → file_stem 加醫令碼不覆寫
  </automated>
</task>

</tasks>
