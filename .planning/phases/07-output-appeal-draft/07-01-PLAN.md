---
phase: 07-output-appeal-draft
plan: 01
type: execute
wave: 1
depends_on: [03-parsers, 04-record-aggregator, 05-three-way-comparator, 06-output-reinforcement-report]
files_modified:
  - src/elc_audit_engine/generators/appeal.py
  - src/elc_audit_engine/generators/__init__.py
  - tests/test_appeal.py
autonomous: true
requirements: [REQ-output-appeal-draft]

must_haves:
  truths:
    - "純組裝層：無 LLM、無規則庫查詢（07-CONTEXT.md domain；規則全文由呼叫端注入）"
    - "D10 四段組裝：①案情摘要 ②醫療必要性 ③規則依據 ④病歷佐證，每筆核減醫令獨立生成"
    - "字數上限依官方問答集 Q15：每欄 1000／合計 2000；裁剪優先 ④→②（C4），①③骨架不動"
    - "P6 不申覆強制填 0 為 pure function 硬檢查（C3/Q13）；申復點數≤核減上界（D-15）"
    - "輸出 `申復草稿_{案件流水號}.md` ＋ `appeal_{流水號}.json`（C7）"
  artifacts:
    - path: "src/elc_audit_engine/generators/appeal.py"
      provides: "build_appeal_draft 四段組裝＋字數控制器＋P6 硬檢查＋render_appeal_markdown/render_appeal_json＋write_appeal"
    - path: "src/elc_audit_engine/generators/appeal.py"
      provides: "adopted_narratives_from_tracking：審核軌跡 JSON → 採用/編輯後採用敘述（D-08）"
    - path: "tests/test_appeal.py"
      provides: "四段結構/案情摘要內容/必要性降級/規則依據/字數裁剪（④→②）/p8p9 切分/P6 硬檢查/每筆獨立/檔案命名/JSON 欄位測試"
  key_links:
    - from: "src/elc_audit_engine/generators/appeal.py"
      to: "src/elc_audit_engine/parsers/models.py"
      via: "from elc_audit_engine.parsers.models import DeductionRecord"
      pattern: "DeductionRecord"
    - from: "src/elc_audit_engine/generators/appeal.py"
      to: "src/elc_audit_engine/record_aggregator/models.py"
      via: "from elc_audit_engine.record_aggregator.models import PatientTimeline"
      pattern: "PatientTimeline"
    - from: "src/elc_audit_engine/generators/appeal.py"
      to: "src/elc_audit_engine/generators/tracking.py"
      via: "from .tracking import STATUS_ADOPT, STATUS_ADOPT_EDITED"
      pattern: "STATUS_ADOPT"
---

<objective>
Wave 1（Phase 7 唯一一輪）：交付申復理由草稿組裝器，滿足 REQ-output-appeal-draft
五項驗收：四段組裝式結構（D10）、字數控制器（C4/Q15：每欄 1000／合計 2000、
④→② 優先裁剪）、P6 強制填 0 硬檢查（C3）、每筆核減醫令獨立生成、
輸出 `申復草稿_{案件流水號}.md` ＋ `appeal_{流水號}.json`（C7）。

依 07-CONTEXT.md D-01~D-09。Output: 1 個 generators 模組（appeal.py）＋
對外 API 擴充＋1 個測試檔，全部測試綠。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-output-appeal-draft/07-CONTEXT.md
@.planning/intel/decisions.md（D10）
@.planning/intel/constraints.md（C3/C4/C7/C8）
@officialdocument/電子申復文件格式/電子申復格式及填表說明門診.pdf（p1-p9 欄位）
@officialdocument/電子申復文件格式/申復作業電子化作業問答集(院所版).pdf（Q13/Q15）
@src/elc_audit_engine/parsers/models.py（DeductionRecord）
@src/elc_audit_engine/record_aggregator/models.py（PatientTimeline）
@src/elc_audit_engine/generators/tracking.py（審核軌跡）
@config/settings.py（OUTPUT_DIR）
</context>

<interfaces>
既有介面，Phase 7 消費:

```python
# Phase 3：核減明細一列（每筆核減醫令）
DeductionRecord(case_class, case_seq, visit_date, fee_year_month,
                order_seq, order_code, non_reimbursed_amount,
                deduction_reason, appeal_item_code, appeal_item_desc,
                institution_note, ...)

# Phase 4：半年病史時間軸
PatientTimeline(patient_id, window_start, window_end,
                visits, labs, exams, imaging, ...)

# Phase 6：審核軌跡 JSON（tracking.render_tracking 產出）
#   entries[]: {order_code, narrative_text, status, edited_text, rule_location}

# 呼叫端注入（零規則庫查詢）：
rule_text / rule_location   # get_rule(code) 的 article_full_text/payment_text + location
evidence                    # adopted_narratives_from_tracking(tracking) 輸出
```

設定（Phase 1）:
```python
OUTPUT_DIR  # env-overridable，write_appeal 預設輸出目錄
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 四段組裝與 P6 硬檢查（appeal.py 核心）</name>
  <files>src/elc_audit_engine/generators/appeal.py, tests/test_appeal.py</files>
  <read_first>
  - 07-CONTEXT.md D-02/D-04/D-07/D-08
  - src/elc_audit_engine/parsers/models.py（DeductionRecord）
  </read_first>
  <automated>
    - build_appeal_draft 產出四段（①案情摘要 ②醫療必要性 ③規則依據 ④病歷佐證）
    - ①含費用年月/就醫日期/案件分類/流水號/醫令/追扣原因/核減上界
    - ②timeline=None 時降級文字；有 timeline 時含就診/檢驗摘要
    - ③含規則全文＋出處；rule_text=None → 「查無規則依據，建議人工查核」
    - ④只含採用/編輯後採用敘述（edited_text 優先）
    - resolve_p6_points：不申覆 → 0（即使 claimed>0）；申覆 → claimed
    - validate_appeal_claim：claimed>核減上界／claimed<0／申覆缺點數 → validation_errors
  </automated>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 字數控制器＋p8/p9 切分（C4/Q15）</name>
  <files>src/elc_audit_engine/generators/appeal.py, tests/test_appeal.py</files>
  <read_first>
  - 07-CONTEXT.md D-03
  - officialdocument/電子申復文件格式/申復作業電子化作業問答集(院所版).pdf Q15
  </read_first>
  <automated>
    - 合計 ≤2000（每欄 ≤1000）；超過時 ④ 先裁、② 後裁（①③ 不動）
    - ④ 裁切＝由尾端移除證據列；② 由尾端壓縮
    - 裁完仍超 → over_limit=True（建議 p7=Y）
    - split_reason：≤1000 → p9 空；1500 字 → p8=1000、p9=500
  </automated>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 渲染與檔案輸出（D-05/D-06）</name>
  <files>src/elc_audit_engine/generators/appeal.py, tests/test_appeal.py</files>
  <read_first>
  - 07-CONTEXT.md D-05/D-06
  </read_first>
  <automated>
    - render_appeal_markdown：標題＋案件資訊＋四段＋p8/p9＋⚠區（裁剪/錯誤/降級）
    - render_appeal_json：p1-p9 欄位＋sections＋word_stats＋validation_errors
    - write_appeal 預設寫 `申復草稿_{case_seq}.md`＋`appeal_{case_seq}.json`
    - 兩筆不同核減醫令 → 獨立草稿（file_stem 不覆寫）
    - 不申覆 → JSON p6=0 且 p8/p9 空
  </automated>
</task>

</tasks>
