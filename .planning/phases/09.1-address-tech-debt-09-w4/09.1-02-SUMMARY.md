---
phase: 09.1-address-tech-debt-09-w4
plan: 2
subsystem: ui
tags: [index.html, generateAppealDraft, render_appeal_json, W4, static-regression]

# Dependency graph
requires:
  - phase: 09.1-address-tech-debt-09-w4 (plan 01)
    provides: /api/appeal/generate 回應改回 render_appeal_json 標準契約（sections/word_stats/p1-p9 + status/case_id/rule_found）
provides:
  - static/index.html generateAppealDraft 消費 data.sections/data.p8_reason1/data.p9_reason2/data.rule_found
  - tests/test_ingest.py 前端契約回歸測試（舊鍵全文零殘留、新鍵在場）
affects: [09.1-03 (前端落盤 appeal_json 下游), Phase 10 (電子申復), Phase 11 (appeal_print)]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "前端 generate 消費一律以 render_appeal_json 契約鍵驅動（sections.text / p8_reason1 / p9_reason2）"
    - "字數上限警告改由前端本地字串長度計算，不依賴後端布林旗標"

key-files:
  created: []
  modified:
    - static/index.html
    - tests/test_ingest.py

key-decisions:
  - "None - followed plan as specified（D-04 依裁示直行）"

patterns-established:
  - "W4 契約橋回歸測試模式：全文子字串層級斷言舊鍵零殘留（含註解）+ 新契約鍵在場"

requirements-completed: [W4]

# Metrics
duration: 2min
completed: 2026-08-08
---

# Phase 09.1 Plan 2: W4 前端 generate 消費適配 Summary

**前端 generateAppealDraft 改讀 render_appeal_json 標準契約鍵（data.sections / data.p8_reason1 / data.p9_reason2），字數警告改由本地字串長度計算，rule_found 提示保留，並以全文子字串回歸測試鎖定舊鍵（appeal_sections/xml_p8_p9_valid/data.reason1/data.reason2）零殘留**

## Performance

- **Duration:** 2 min
- **Started:** 2026-08-08T06:14:01Z
- **Completed:** 2026-08-08T06:15:47Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- `generateAppealDraft` 四段 box 改由 `data.sections[i].text` 填充（`const sec = data.sections[i]`，維持 `sec ? sec.text : ''` 防呆；維持 `document.getElementById(id).value` 屬性賦值，未引入 innerHTML——T-092-01 防線不破）
- 字數顯示改以 `(data.p8_reason1 || '').length` / `(data.p9_reason2 || '').length` 本地計算，超限警告由本地 `<= 1000` 判斷，不再讀 `xml_p8_p9_valid`
- `if (!data.rule_found)` 的 aSec3 查無規則提示邏輯原樣保留；`authFetch` 包裝、`alert(data.message)` 錯誤路徑、既有版面不變
- `tests/test_ingest.py` 新增 `test_index_html_generate_consumes_standard_contract`，以既有 :174-181 靜態回歸模式斷言舊鍵全文零殘留（含註解）＋新契約鍵在場

## Task Commits

> 本執行於主工作樹（sandbox .git 唯讀），未 commit；以下為建議給 orchestrator 的 commit 訊息。

1. **Task 1: generateAppealDraft 改讀標準契約（D-04）** - `refactor(09.1-02): index.html generate 消費適配 render_appeal_json 標準契約`
2. **Task 2: 前端契約回歸測試同步** - `test(09.1-02): index.html 契約回歸測試（舊鍵零殘留、sections/p8/p9/rule_found 在場）`

**Plan metadata（orchestrator 執行）:** `docs(09.1-02): complete W4 前端消費適配 plan`

## Files Created/Modified
- `static/index.html` - `generateAppealDraft` 消費段（:551-570）改讀 `data.sections`/`data.p8_reason1`/`data.p9_reason2`；字數警告本地計算；`rule_found` 提示保留；:551 註解同步為 sections 語義
- `tests/test_ingest.py` - 新增 `test_index_html_generate_consumes_standard_contract`（置於 :183-195 innerHTML 回歸測試之後，index.html 靜態回歸測試群）

## Decisions Made
None - followed plan as specified

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Task 1 初版註解殘留舊鍵名 `xml_p8_p9_valid`**
- **Found during:** Task 1（verify）
- **Issue:** 初版改寫的註解寫「不再讀 xml_p8_p9_valid」，grep verify 命中該行；且 Task 2 的「全文子字串不含 xml_p8_p9_valid」斷言會因此紅
- **Fix:** 註解措辭改為「超限警告改由本地判斷」，不再提及舊鍵名
- **Files modified:** static/index.html（:559-560）
- **Verification:** `grep -n "data\.appeal_sections\|xml_p8_p9_valid\|data\.reason1\|data\.reason2" static/index.html` 輸出「舊鍵已清乾淨」；pytest 22 passed
- **Committed in:** 併入 Task 1（orchestrator commit）

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** 必要修正，避免新回歸測試自相矛盾；無 scope creep。

## Issues Encountered
None（pytest 耗時 32-35s，略超 plan 預期 30s，係首次 cold cache＋paddle 導入之環境因素，非實現問題）

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- 前端 generate 消費與後端標準契約（09.1-01）完全對齊：前端可直接把回應落盤為 `appeal_{流水號}.json` 餵下游
- 契約回歸測試已鎖定，後續（09.1-03 submission 轉換層 / Phase 10-11）不必再處理前端舊鍵兼容
- 待 orchestrator 對 09.1-02 執行 commit 並推進 STATE/ROADMAP

## Self-Check: PASSED
- [x] `static/index.html` 已改寫（:551-570 消費段讀標準契約）——檔案存在、grep 舊鍵零命中
- [x] `tests/test_ingest.py` 已新增回歸測試——`grep -c "def test_index_html_generate_consumes_standard_contract"` = 1
- [x] plan 級 verification `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_ingest.py -q` 全綠（22 passed）
- [x] 未修改 STATE.md/ROADMAP.md/REQUIREMENTS.md；未動 server.py/src/（09.1-01 所有）/scripts/（09.1-03 所有）
- [x] 未觸碰 pre-existing untracked 檔（.planning/config.json、reasonix.toml、.reasonix/）

---
*Phase: 09.1-address-tech-debt-09-w4*
*Completed: 2026-08-08*
