---
phase: 09.1-address-tech-debt-09-w4
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - static/index.html
  - tests/test_ingest.py
autonomous: true
requirements: [W4]

must_haves:
  truths:
    - "前端 generate 消費改讀標準契約欄位（sections/p8_reason1/p9_reason2），不再引用已移除的舊鍵（appeal_sections/reason1/xml_p8_p9_valid）"
    - "rule_found 提示邏輯（aSec3 查無規則提示）保留，authFetch 包裝不變"
  artifacts:
    - path: "static/index.html"
      provides: "generateAppealDraft 消費 data.sections/data.p8_reason1/data.p9_reason2"
    - path: "tests/test_ingest.py"
      provides: "前端契約回歸測試（靜態斷言舊鍵消失、新鍵在場）"
  key_links:
    - from: "index.html generateAppealDraft"
      to: "/api/appeal/generate 回應"
      via: "authFetch（:334 統一包裝）→ data.sections / data.p8_reason1 / data.p9_reason2 / data.rule_found"
---

<objective>
W4 前端消費適配（D-04）：讓 `static/index.html` 的 generate 消費段改讀 `render_appeal_json` 標準契約欄位，並同步測試。

Purpose: 後端（09.1-01）回應改為單一標準契約後，前端若仍讀舊鍵會全部空白——本次把前端與契約對齊，使前端可直接把回應落盤為 `appeal_{流水號}.json` 餵下游。
Output: `index.html` generate 消費段改寫＋`tests/test_ingest.py` 前端契約回歸測試。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-address-tech-debt-09-w4/09.1-CONTEXT.md
@.planning/phases/09.1-address-tech-debt-09-w4/09.1-PATTERNS.md

<!-- 介面合約（已由 grep/讀碼核實，executor 不需再探索） -->
<interfaces>
static/index.html（單檔 inline 樣板，CSP inline script 例外仍在）:
- `generateAppealDraft()`（:528-567）目前消費：
  - 四段 box：`const sec = data.appeal_sections[i]` → `document.getElementById(id).value = sec ? sec.text : ''`（:551-556）
  - 字數：`data.xml_p8_p9_valid`（:559）＋ `(data.reason1 || '').length` / `(data.reason2 || '').length`（:561）
  - 查無規則：`if (!data.rule_found)` 於 aSec3 前置提示文案（:562-566）
- `authFetch`（:334）統一包裝；失敗 `alert(data.message)`（:546-549）
- 既有版面：四段 `<textarea id="aSec1..aSec4">`、字數 `<span id="aCharCount">`

render_appeal_json 標準契約新鍵（後端 09.1-01 交付，本 plan 依此適配）:
- `data.sections`：list，每項 `{key, title, text, trimmed}`（text 即原四段文案）
- `data.p8_reason1` / `data.p9_reason2`：申復理由一／二（取代 reason1/reason2）
- `data.word_stats.total_chars`（合計字數）、`data.word_stats.over_limit`（取代 over_limit 鍵）
- `data.rule_found`：保留（server 補充鍵）
- 已移除舊鍵：appeal_sections / reason1 / reason2 / xml_p8_p9_valid / p6_points / total_char_count

tests/test_ingest.py:
- :174-181 既有 static/index.html 靜態回歸模式（Path("static/index.html").read_text 後斷言字串），本 plan 比照此模式新增契約回歸測試
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: generateAppealDraft 改讀標準契約（D-04）</name>
  <files>static/index.html</files>

  <read_first>
    - static/index.html（:528-567 generateAppealDraft 消費段、:334 authFetch 包裝）
  </read_first>
  <action>改寫 index.html `generateAppealDraft` 的消費段（:551-566）：① 四段 box 改讀 `data.sections[i].text`（`const sec = data.sections[i]`；sections[i] 含 trimmed 欄位，僅取 text；sec 可能為 undefined 時維持 `sec ? sec.text : ''` 防呆）；② 字數字串改以 `data.p8_reason1`/`data.p9_reason2` 計算長度（`(data.p8_reason1 || '').length`、`(data.p9_reason2 || '').length`），warn 文案改由本地判斷 `(data.p8_reason1||'').length <= 1000 && (data.p9_reason2||'').length <= 1000`（不再讀 data.xml_p8_p9_valid）；③ `if (!data.rule_found)` 的 aSec3 查無規則提示邏輯保留不動；④ `authFetch` 包裝、`alert(data.message)` 錯誤路徑、既有版面風格不變。**不得**用 innerHTML 拼接 `sections[i].text`（維持 P1-5 textContent/.value 防線）。不得改動本函式以外的頁面結構。
  <verify>
    <automated>grep -n "data\.appeal_sections\|xml_p8_p9_valid\|data\.reason1\|data\.reason2" static/index.html || echo "舊鍵已清乾淨"</automated>
  </verify>
  <done>四段由 data.sections 填充、字數由 p8_reason1/p9_reason2 本地計算、rule_found 提示保留；grep 確認舊鍵引用為零（命令輸出無命中）。
  </done>
</task>

<task type="auto">
  <name>Task 2: 前端契約回歸測試同步（D-04 測試同步）</name>
  <files>tests/test_ingest.py</files>

  <read_first>
    - tests/test_ingest.py（:174-181 既有 index.html 靜態回歸測試模式）
    - static/index.html（Task 1 改寫後的 generateAppealDraft 消費段）
  </read_first>
  <action>在 tests/test_ingest.py 新增一個測試函式（比照既有 :174-181 靜態回歸模式，讀 `Path("static/index.html").read_text(encoding="utf-8")`）：斷言 HTML 全文**不含** `appeal_sections`、`xml_p8_p9_valid`、`data.reason1`、`data.reason2`（以子字串存在性斷言），且**含** `data.sections[`、`data.p8_reason1`、`data.p9_reason2`、`data.rule_found`。函式命名比照既有 `test_index_html_no_external_resources` 風格（如 `test_index_html_generate_consumes_standard_contract`），置於該檔 index.html 靜態回歸測試群附近。
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_ingest.py -q</automated>
  </verify>
  <done>新回歸測試全綠；既有 test_ingest.py 全部測試仍綠。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→瀏覽器渲染（sections.text 含病歷摘要/自由文字） | 服務端回傳內容經 JS 注入 DOM |
| 靜態檔回歸測試 | 讀取 git 版控資產 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-092-01 | Tampering（XSS） | sections.text 渲染進 textarea | mitigate | 一律用 `document.getElementById(id).value = sec.text`（屬性賦值，非 innerHTML）；維持 P1-5 既有 DOM API + textContent 防線；本 plan 明令禁用 innerHTML 拼接 sections.text |
| T-092-02 | Information Disclosure | 標準契約回應渲染 | accept | 頁面為既有本機內部工具（server 綁 127.0.0.1），渲染內容與後端契約一致，無新增暴露面 |
| T-092-03 | Spoofing | rule_found 提示文案 | accept | 服務端 get_rule 產出唯讀布林，客戶端不可控制，純提示用途 |
</threat_model>

<verification>
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_ingest.py -q
（應 < 30s）
</verification>

<success_criteria>
- index.html 不再引用 appeal_sections / xml_p8_p9_valid / data.reason1 / data.reason2
- 四段、字數、查無規則提示均以標準契約鍵驅動，且 rule_found 邏輯保留
- tests/test_ingest.py 新增契約回歸測試全綠，既有測試不迴歸
</success_criteria>

<output>
完成後建立 `.planning/phases/09.1-address-tech-debt-09-w4/09.1-02-SUMMARY.md`
</output>
