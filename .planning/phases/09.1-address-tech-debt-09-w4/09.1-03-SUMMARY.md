---
phase: 09.1-address-tech-debt-09-w4
plan: 3
subsystem: generators
tags: [appeal-print, submission-contract, case-store, honest-degradation, w5, pure-function]

# Dependency graph
requires:
  - phase: 11-paper-appeal-print
    provides: submission 契約 dict（field_mapping.py:122-127 消費鍵、缺欄誠實降級 warnings 語意、render_appeal_print/write_appeal_print 簽名）
  - phase: 09.1-address-tech-debt-09-w4
    provides: 09.1-01 `_to_appeal_case` 透傳 id_number（rec.id_number 遮罩後 4 碼）與 render_appeal_json 標準契約
provides:
  - build_submission_from_case(payload) -> (submission, warnings) 標準轉換純函式（generators/appeal_print 層）
  - scripts/build_appeal_print.py case_payload 分支改經轉換層（取代 raw payload 直接當 submission 的語義錯誤）
  - 端到端證明：完整鍵輸入患者層無 warnings、真實路徑缺欄 warnings 欄名明確且 id_number 照印可 join
affects: [10-real-machine-gate, 12-x-submission-upload, server.py 未來 W5 接線]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 純函式轉換層「缺欄誠實留空＋warnings 欄名、不捏造」（承襲 11-01 誠實降級）
    - 已有 orders 直通 vs 單筆案件 order_code/order_name 構造的 orders 分支
    - 遮罩值照印＋「疑似未遮罩」防呆（比照 field_mapping.py:147-150）

key-files:
  created:
    - src/elc_audit_engine/generators/appeal_print/case_to_submission.py
    - tests/test_case_to_submission.py
  modified:
    - scripts/build_appeal_print.py

key-decisions:
  - "submit_date 缺欄 warning 欄名採「原申報日期」（HEADER_KEYS 語意；plan 未明示該欄名）"
  - "orders：payload 已有 orders list 時直通（SubmissionCase 風格、直通非捏造）；否則單筆案件 order_code/order_name 構造；無任何醫令資料 → 空 list"
  - "端到端測試走 render_appeal_print 純函式段（不需 soffice）；沙箱 soffice 可用故無需 skip"

patterns-established:
  - "Pattern 1: 轉換層缺欄 warnings 僅記欄名不記值（T-093-01 PHI 不入 warnings/日誌），函式零 logging/print"
  - "Pattern 2: 測試端到端以「轉換層 → render_appeal_print 純函式段」證明可直接餵 build_appeal_print"

requirements-completed: [W5]

# Metrics
duration: 18min
completed: 2026-08-08
---

# Phase 09.1 Plan 3: CaseStore→submission 標準轉換層（D-05）Summary

**CaseStore appeal payload→submission 契約 dict 標準轉換純函式（8 鍵缺欄誠實留空＋warnings 欄名、id_number 遮罩照印不重建），CLI case_payload 分支改經轉換層，端到端證明完整鍵輸入可完整 join、真實路徑缺欄可歸因**

## Performance

- **Duration:** 18 min
- **Started:** 2026-08-08T06:04:00Z
- **Completed:** 2026-08-08T06:21:45Z
- **Tasks:** 2
- **Files modified:** 3（2 新增＋1 修改）

## Accomplishments

- 新增 `build_submission_from_case(payload) -> tuple[dict, list[str]]` 純函式：submission 鍵對齊 field_mapping.py:122-127 契約 8 鍵（case_class/case_seq/id_number/patient_name/primary_diagnosis/clinic/submit_date/orders），缺欄留空（None/空 list）＋warnings 欄名（案件分類/流水號/身份證字號/姓名/傷病名稱/審查科別/原申報日期），不捏造（承襲 11-01）
- orders 分支：已有 orders list 直通（SubmissionCase 風格）／單筆案件 order_code/order_name 構造（name=None 省略該鍵、order_seq 映射至 seq）／無資料則空 list
- id_number 遮罩值照印（含 `*`）、無 `*` 且長度 ≥8 加「疑似未遮罩」warning 不阻斷（比照 field_mapping.py:147-150）；函式零 logging/print（PHI 不入日誌，T-093-01）
- `scripts/build_appeal_print.py` case_payload 分支改經轉換層（取代 raw payload 直接當 submission 的語義錯誤）；CLI 參數契約與 usage 不變、case_payload 仍選填（2 參維持 submission=None 誠實降級）；case_warnings 與 render warnings 合併以「警告：」逐條印欄名（不印值）
- 端到端測試證明：完整鍵輸入（含 orders/total_qty/points/seq）轉換後直餵 render_appeal_print → 患者層無 warnings（身份證字號/姓名/傷病名稱/審查科別/數量/金額）；真實 `_to_appeal_case` 鍵集 → warnings 含案件分類/姓名/傷病名稱/審查科別、id_number 照印可 join
- 沙箱真實 CLI 手動驗證：3 參形式以真實鍵集產出 3 頁 PDF，stdout「警告：」僅欄名無值，PDF 每聯含 `A123****`（id_number 照印 join 成功）

## Task Commits

_sandbox 具唯讀 .git，未 commit；建議 commit 清單（由 orchestrator 依序提交）：_

1. **Task 1: 標準轉換層 build_submission_from_case（D-05）**
   - `test(09.1-03): add failing CaseStore→submission 轉換層 tests（TDD RED）`（tests/test_case_to_submission.py）
   - `feat(09.1-03): build_submission_from_case 標準轉換層（D-05 誠實降級）`（case_to_submission.py）
2. **Task 2: CLI 接入轉換層＋端到端**
   - `feat(09.1-03): CLI case_payload 分支改經轉換層＋端到端測試（D-05）`（scripts/build_appeal_print.py、tests/test_case_to_submission.py）

**Plan metadata:** `docs(09.1-03): complete CaseStore→submission 標準轉換層 plan`

## Files Created/Modified

- `src/elc_audit_engine/generators/appeal_print/case_to_submission.py` - 標準轉換純函式 `build_submission_from_case`：8 契約鍵映射、缺欄誠實留空＋warnings 欄名、id_number 防呆、orders 構造/直通、visit_date/fee_year_month 直通；零 I/O 零 logging
- `tests/test_case_to_submission.py` - 10 測試：完整鍵/缺欄降級/契約 8 鍵/真實鍵集/id_number 防呆/orders 分支＋2 端到端（render_appeal_print 純函式段）
- `scripts/build_appeal_print.py` - case_payload 分支改經 `build_submission_from_case`，case_warnings 與 render warnings 合併印出（欄名不印值）

## Decisions Made

- **submit_date 缺欄 warning 欄名採「原申報日期」**：plan 對 submit_date 缺欄的 warning 欄名未明示（「...」），採用 HEADER_KEYS 既有欄名語意，與 field_mapping 缺欄歸因精神一致。
- **orders 既有資料直通優先**：payload 已有 orders（list[dict]，SubmissionCase 風格）時直通不重造——符合「既有資料直通、非捏造」且不破壞 test_appeal_print.py `test_cli_build_appeal_print_three_args_submission`（captured submission == case_payload）與 soffice 端到端既有測試（test_appeal_print.py 不得改動之硬性限制）。真實 `_to_appeal_case` 鍵集（無 orders、僅 order_code/order_name）走單筆構造路徑。
- **端到端用例不標 `requires_soffice`**：走 render_appeal_print 純函式段（fill_template 為 zip 注入，不需 soffice），與既有 `-k` 分組語意相容。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] 轉換層第一版 `_WARN_FIELD` 缺 `orders` 鍵致 KeyError**
- **Found during:** Task 1（GREEN 階段實作後首次完整測試前）
- **Issue:** 實作以「for key in CONTRACT_KEYS」統一走缺欄檢查，而 `_WARN_FIELD` 字典僅含 7 個帶 warning 欄名的契約鍵（orders 缺欄的 warning 欄名 plan 未定義），真實鍵集輸入（orders 缺席）會 `KeyError: 'orders'`
- **Fix:** 缺欄檢查迴圈改為僅遍歷 `_WARN_FIELD`（7 鍵），orders 獨立分支處理（直通/構造/空 list），不觸發 warnings（plan 未規定 orders 缺欄 warning）
- **Files modified:** src/elc_audit_engine/generators/appeal_print/case_to_submission.py
- **Verification:** `uv run pytest tests/test_case_to_submission.py tests/test_appeal_print.py -q` 47 passed
- **Committed in:** 未 commit（sandbox 唯讀 .git；併入建議 Task 1 feat commit）

---

**Total deviations:** 1 auto-fixed（1 bug）
**Impact on plan:** 屬實作期內部修正，無 scope creep，計畫行為不受影響。

## Issues Encountered

- 無。沙箱 soffice 可用，soffice 依賴測試（test_appeal_print.py 既有 `-k e2e/copies` 與 CLI soffice 測試）全綠，無需 skip 註記。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- W5 標準轉換層就緒：落盤的 appeal_json＋CaseStore payload 可直接餵 `build_appeal_print`（CLI 已接入轉換層）；缺欄誠實降級＋warnings 欄名可歸因、id_number 遮罩照印可 join
- 下一計劃 09.1-04 可接線 server 端（W5 完整數據流）或進入 phase 驗收；soffice 端到端已在沙箱全綠，可由 orchestrator 直接採信

---
*Phase: 09.1-address-tech-debt-09-w4*
*Completed: 2026-08-08*

## Self-Check: PASSED

- [x] created files 存在：case_to_submission.py / test_case_to_submission.py / build_appeal_print.py（modified）/ 09.1-03-SUMMARY.md
- [x] `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_case_to_submission.py tests/test_appeal_print.py -q` → **47 passed**（含 test_appeal_print.py 37 回歸全綠、soffice 依賴測試未 skip）
- [x] TDD RED→GREEN：RED 於 collection 階段失敗（轉換層不存在），GREEN 後 10 passed
- [x] 真實 CLI 手動驗證：3 參形式產出 3 頁 PDF，warnings 僅欄名（案件分類/姓名/傷病名稱/審查科別/原申報日期…）、PDF 每聯含 `A123****`（id_number 照印 join）
- [x] 未改動 server.py / static / test_appeal_print.py / STATE.md / ROADMAP.md / REQUIREMENTS.md；未 commit（sandbox 唯讀 .git）
