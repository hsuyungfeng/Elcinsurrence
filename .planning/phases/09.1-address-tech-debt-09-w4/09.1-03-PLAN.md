---
phase: 09.1-address-tech-debt-09-w4
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/elc_audit_engine/generators/appeal_print/case_to_submission.py (new)
  - tests/test_case_to_submission.py (new)
  - scripts/build_appeal_print.py
autonomous: true
requirements: [W5]

must_haves:
  truths:
    - "CaseStore appeal payload 可經標準轉換函式產出 11-01 submission 契約 dict（case_class/case_seq/id_number/patient_name/primary_diagnosis/clinic/submit_date/orders），缺欄誠實留空＋warnings、不捏造（D-05）"
    - "真實路徑缺欄可歸因（warnings 欄名明確）且 id_number 可 join；完整鍵輸入可完整 join"
  artifacts:
    - path: "src/elc_audit_engine/generators/appeal_print/case_to_submission.py"
      provides: "build_submission_from_case(payload) -> tuple[dict, list[str]]"
    - path: "tests/test_case_to_submission.py"
      provides: "轉換層單元＋端到端測試"
  key_links:
    - from: "scripts/build_appeal_print.py"
      to: "build_submission_from_case"
      via: "case_payload_json 分支（:83-99）改經轉換層"
    - from: "build_submission_from_case 輸出"
      to: "render_appeal_print submission 參數"
      via: "端到端測試（test_case_to_submission.py）"
---

<objective>
W5 標準轉換層（D-05）：建立「CaseStore appeal payload → submission 契約 dict」的標準轉換函式，補 `case_class`/`patient_name`/`orders` 等欄位，缺欄誠實留空＋warning、不捏造（承襲 11-01 誠實降級原則）。

Purpose: `_to_appeal_case`（server.py:382-405）產出的 appeal payload 目前缺 `case_class`（join key 一半）、`patient_name=None`、無 `orders`；plan 01 Task 3 補全後真實 payload 可含 `id_number`（rec.id_number 遮罩值）但仍無 case_class/patient_name/orders。建立標準轉換層後，缺欄以誠實留空＋warnings 歸因（欄名明確、不因缺鍵而全空無據），完整鍵輸入可完整 join——落盤的 appeal_json＋submission 可直接餵 `build_appeal_print`。
Output: 新檔案 `case_to_submission.py`＋新測試 `test_case_to_submission.py`＋`scripts/build_appeal_print.py` 接入轉換層。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-address-tech-debt-09-w4/09.1-CONTEXT.md
@.planning/phases/09.1-address-tech-debt-09-w4/09.1-PATTERNS.md
@.planning/phases/11-paper-appeal-print/11-CONTEXT.md

<!-- 介面合約（已由 grep/讀碼核實，executor 不需再探索） -->
<interfaces>
field_mapping.py（submission 契約權威，只讀不改）:
- submission dict 鍵（docstring :122-127）：case_class / case_seq / id_number（遮罩後 4 碼）/ patient_name / primary_diagnosis / clinic / orders；orders = list[dict]，每項可含 code / total_qty / points / seq / name
- `_str_or_empty(value)`（:70-74）：None/空 → ""；其餘 → str
- 缺欄語意（:144-177）：每缺一欄 `warnings.append("身份證字號"/"姓名"/"傷病名稱"/"審查科別"/...)`，不捏造；id_number 防呆（:147-150）：無 `*` 且 len≥8 → warning「疑似未遮罩」
- `_find_order_by_seq(submission, order_seq)`（:97-106）：以 `str(order.get("seq")) == str(order_seq)` 找行
- `build_rows(payload, facility, submission=...)`：患者層欄位一律來自 submission（payload 無身份證字號/姓名/數量/金額鍵）

appeal_print/__init__.py:
- `render_appeal_print(payload: dict, facility: dict, *, template_odt_path: str, submission: dict | None = None) -> tuple[bytes, list[str]]`（:59-102，純函式不落盤）

scripts/build_appeal_print.py:
- usage（:4）：`python scripts/build_appeal_print.py <appeal_json_path> [case_payload_json_path] [output_pdf_path]`
- case_payload_json 分支（:83-99）：`submission = json.load(f)` 直接把 raw CaseStore payload 當 submission → 本次改為經 build_submission_from_case 轉換
- 最終以 `render_appeal_print(payload, facility, template_odt_path=..., submission=submission)` 呼出（:145）

server.py `_to_appeal_case`（:382-405）產出的 CaseStore appeal payload（**plan 01 Task 3 補全後**）:
- 鍵：id/demo/case_seq/record_no/patient_name(None)/order_code/order_name/deduct_amount/deduction_reason/visit_date/soap/multisource_evidence/source，**並含 id_number（rec.id_number 遮罩後 4 碼透傳）**
- 轉換層輸入契約裁示：真實 payload **仍無** case_class/patient_name/orders——缺欄誠實留空＋warnings 歸因（欄名明確），不因缺鍵而全空無據；id_number 可 join（truth 2 已如實措辭，見 must_haves）

tests/test_appeal_print.py 可複用模式:
- `_submission(**overrides)`（:53-73）完整 submission 樣本；`_payload(**overrides)`（:76-96）appeal JSON 樣本；「base dict + overrides」構造器慣例
- OFFICIAL_ODT 常量（:29-32）；`-k mapping` 測試分組慣例（soffice 依賴測試以 `requires_soffice` mark 區隔）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立標準轉換層 build_submission_from_case（D-05）</name>
  <files>src/elc_audit_engine/generators/appeal_print/case_to_submission.py (new), tests/test_case_to_submission.py (new)</files>

  <read_first>
    - src/elc_audit_engine/generators/appeal_print/field_mapping.py（:70-74 _str_or_empty、:122-177 submission 契約鍵與缺欄語意）
    - src/elc_audit_engine/generators/appeal_print/__init__.py（:59-102 render_appeal_print 簽名與 submission 參數）
    - tests/test_appeal_print.py（:41-96 _submission/_payload「base dict + overrides」構造器模式）
  </read_first>
  <behavior>
    - Test 1: 含完整鍵的 CaseStore 風格 payload（case_seq/order_code/order_name/visit_date/id_number 等）→ submission 含 case_seq、orders[0].code、orders[0].name、id_number 照印，warnings 為空
    - Test 2: 缺 case_class/patient_name/id_number 的 payload → submission 對應鍵留空（None 或省略）且 warnings 含 "案件分類"/"姓名"/"身份證字號" 欄名，且輸出不含任何推導/捏造值
    - Test 3: 契約 8 鍵 {case_class, case_seq, id_number, patient_name, primary_diagnosis, clinic, submit_date, orders} ⊆ 輸出 dict 鍵集合，且輸出不含任何推導/捏造值（visit_date/fee_year_month 屬既有資料直通、非捏造，允許存在於 submission）
  </behavior>
  <action>新建 `src/elc_audit_engine/generators/appeal_print/case_to_submission.py`，匯出 `build_submission_from_case(payload: dict) -> tuple[dict, list[str]]`（回傳 (submission, warnings)）。submission 鍵對齊 field_mapping.py:122-127 契約（case_class/case_seq/id_number/patient_name/primary_diagnosis/clinic/submit_date/orders），orders 為 list[dict]、每項可含 code/total_qty/points/seq/name。映射規則：case_seq ← payload["case_seq"]；orders 由單筆案件構造 `[{"code": payload["order_code"], "name": payload["order_name"]}]`（name 為 None 時省略該鍵）；payload 若有 order_seq/id_number/visit_date/fee_year_month 則依序映射至 orders[0]["seq"]/submission["id_number"]/["visit_date"]/["fee_year_month"]（submission 可含契約鍵之外資料，端到端用途——屬既有資料直通、非捏造；warnings 不因多餘鍵而觸發）；case_class/patient_name/primary_diagnosis/clinic/submit_date 由 payload 對應鍵複製（缺則留空並 `warnings.append("案件分類"/"姓名"/"傷病名稱"/"審查科別"/...)`，比照 field_mapping.py:144-177 欄名語意）；id_number 缺 → warnings.append("身份證字號")；id_number 無 `*` 且 len≥8 → warnings.append("身份證字號（疑似未遮罩，請確認來源）")（比照 field_mapping.py:147-150 防呆，不阻斷）。**禁止**推導/捏造任何缺失值（承襲 11-01）。函式內不得有任何 logging/print（PHI 不入日誌）。新建 `tests/test_case_to_submission.py`，以 test_appeal_print.py「base dict + overrides」模式構造 CaseStore 風格 payload 輸入。
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_case_to_submission.py -q</automated>
  </verify>
  <done>build_submission_from_case 存在且三項 behavior 測試全綠；缺欄 warnings 明確、契約 8 鍵俱在（缺欄留空）、輸出不含推導/捏造值（visit_date/fee_year_month 為既有資料直通、非捏造）、無 PHI 值入日誌（函式無任何日誌呼叫）。
  </done>
</task>

<task type="auto">
  <name>Task 2: CLI 接入轉換層＋端到端（D-05 可直接餵 build_appeal_print）</name>
  <files>scripts/build_appeal_print.py, tests/test_case_to_submission.py</files>

  <read_first>
    - scripts/build_appeal_print.py（:1-100 usage 與 CLI 結構、:83-99 case_payload 分支、:145 render 呼叫）
    - src/elc_audit_engine/generators/appeal_print/case_to_submission.py（Task 1 產物，本次接入）
    - tests/test_appeal_print.py（:23-36 requires_soffice mark 與 OFFICIAL_ODT 常量）
  </read_first>
  <action>scripts/build_appeal_print.py 的 case_payload_json 分支（:83-99）：讀入 raw CaseStore payload 後以 `build_submission_from_case(raw)` 取得 (submission, warnings)，取代目前把 raw payload 直接當 submission 傳給 render_appeal_print 的做法（:145）；warnings 依 11-03 CLI 既有慣例印到 stdout（欄名清單，不印值），CLI 參數契約與 usage（:4）不變，`case_payload_json` 仍為選填（缺時 submission=None 維持誠實降級）。tests/test_case_to_submission.py 新增端到端用例：以含完整鍵的 CaseStore 風格 payload（case_seq/order_code/order_name/visit_date/id_number/case_class/patient_name/primary_diagnosis/clinic）走 `build_submission_from_case` → `render_appeal_print(payload, facility, template_odt_path=OFFICIAL_ODT, submission=converted)`，斷言回傳為 (bytes, warnings)、bytes 非空、warnings 不含患者層欄名（證明患者層 join/映射成功）；缺欄案例（以 plan 01 真實 payload 鍵集為輸入，即含 id_number、缺 case_class/patient_name/orders）對照斷言 warnings 含欄名（證明真實路徑缺欄可歸因且 id_number 照印）。OFFICIAL_ODT 常量與 `_facility` 可自 test_appeal_print.py 複製（或 import tests 內 helper；以複製最小量為宜）。此用例走 render_appeal_print 純函式段，不觸發 soffice（soffice 依賴的 write 段測試維持既有 `requires_soffice` mark 與 `-k` 分組語意，不得改動 test_appeal_print.py）。
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_case_to_submission.py tests/test_appeal_print.py -q</automated>
  </verify>
  <done>CLI 改經轉換層消費 case payload；轉換後 submission 可直接餵 render_appeal_print（完整鍵輸入患者層 warnings 消失、真實路徑缺欄 warnings 欄名明確）；test_appeal_print.py 回歸全綠（soffice 依賴測試維持既有 skip/mark 語意）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CaseStore payload（本機 PHI 儲存）→ 轉換層 | 含 patient_name/id_number（遮罩）等 PHI 的 dict 跨入純函式 |
| 轉換層 → render_appeal_print / 落盤 PDF | submission dict 跨入輸出通道 |
| scripts CLI → 轉換層 | 使用者提供的 case_payload JSON 檔 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-093-01 | Information Disclosure | 轉換層處理 PHI 鍵（patient_name/id_number） | mitigate | 函式為純記憶體轉換，內部零 logging/print（Task 1 明令）；warnings 僅記欄名（身份證字號/姓名），不記值；CLI warnings 印欄名不印值（比照 11-03 既有呈現） |
| T-093-02 | Tampering | id_number 遮罩語意 | mitigate | 遮罩值（含 `*`）照印；無 `*` 且長度≥8 依 field_mapping.py:147-150 防呆加 warning，不靜默照印；轉換層不得重建/推導遮罩（與 T-11-05 同源） |
| T-093-03 | Elevation of Privilege | 轉換層誤寫 CaseStore | accept | 純函式唯讀輸入 dict，無狀態、無副作用、不觸 CaseStore |
| T-093-04 | Denial of Service | 端到端測試讀官方模板 | accept | 模板為 git 版控資產（OFFICIAL_ODT），單次讀取開銷可忽略 |
</threat_model>

<verification>
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_case_to_submission.py tests/test_appeal_print.py -q
（應 < 30s）
</verification>

<success_criteria>
- `build_submission_from_case(payload) -> (submission, warnings)` 存在於 generators/appeal_print 層，行為符合 D-05 誠實降級
- scripts/build_appeal_print.py 的 case_payload 分支改經轉換層，CLI 參數契約不變
- 轉換後 submission 可直接餵 render_appeal_print（端到端測試證明）；完整鍵輸入患者層欄位完整 join、真實路徑缺欄 warnings 欄名明確可歸因且 id_number 可 join
- PHI 值不入任何日誌/輸出（僅欄名入 warnings）
</success_criteria>

<output>
完成後建立 `.planning/phases/09.1-address-tech-debt-09-w4/09.1-03-SUMMARY.md`
</output>
