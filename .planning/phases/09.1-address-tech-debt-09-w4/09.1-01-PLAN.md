---
phase: 09.1-address-tech-debt-09-w4
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/elc_audit_engine/case_store/states.py
  - tests/test_case_states.py
  - server.py
  - tests/test_server_case_store_integration.py
autonomous: true
requirements: [W1, W4, W5]

must_haves:
  truths:
    - "刚导入（imported）的 appeal 案件可直接经 /api/appeal/generate 达到 appealed，不再抛 IllegalTransitionError"
    - "/api/appeal/generate 回应主体为 render_appeal_json 标准契约（sections/word_stats/p1-p9），仅并入 status/case_id/rule_found 三个补充键"
    - "非法转换（如 submitted→imported）与未知状态仍抛例外，语意不放松"
    - "真实 appeal 案件 payload 含 id_number（rec.id_number 遮罩值透传），patient_name 仍为 None——W5 转换层（09.1-03）患者层可 join"
  artifacts:
    - path: "src/elc_audit_engine/case_store/states.py"
      provides: "W1 合法边 imported→appealed（_TRANSITIONS 的 STATE_IMPORTED 目标含 STATE_APPEALED）"
    - path: "server.py"
      provides: "generate handler 回应改以 render_appeal_json 为主体"
    - path: "server.py"
      provides: "_to_appeal_case 透传 rec.id_number（遮罩值）供 W5 转换层 join"
  key_links:
    - from: "server.py generate_appeal_draft 回应"
      to: "render_appeal_json (appeal.py:480)"
      via: "jsonify(json.loads(render_appeal_json(draft)))"
    - from: "server.py:778 transition(case_id, 'appealed')"
      to: "states.py _TRANSITIONS 合法边"
      via: "STATE_IMPORTED → STATE_APPEALED"
    - from: "server.py _to_appeal_case 输出"
      to: "09.1-03 build_submission_from_case 输入"
      via: "id_number（rec.id_number 遮罩值）透传"
---

<objective>
修复 W1（状态机语义）与 W4（generate 契约桥）两处后端整合缺陷，并补 W5 数据流（_to_appeal_case 透传 rec.id_number）。

Purpose: 让 `/api/appeal/generate` 的正常用户流可达 `appealed` 状态（D-01），且端点回应回归 `render_appeal_json` 单一标准契约（D-03）——前端拿到即可直接落盘 `appeal_{流水号}.json` 喂 `build_appeal_xml`/`build_appeal_print`；同时让 `_to_appeal_case` 透传 `rec.id_number`（DeductionRecord 遮罩值），使 09.1-03 转换层在真实路径下患者层可 join（D-05 数据流补全）。
Output: `states.py` 合法边扩充＋`server.py` generate 回应契约重构＋`_to_appeal_case` id_number 透传＋对应测试同步（删除 fast-forward）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.1-address-tech-debt-09-w4/09.1-CONTEXT.md
@.planning/phases/09.1-address-tech-debt-09-w4/09.1-PATTERNS.md
@.planning/phases/09-his-servicing/09-CONTEXT.md

<!-- 介面合約（已由 grep/讀碼核實，executor 不需再探索） -->
<interfaces>
states.py（src/elc_audit_engine/case_store/states.py）:
- `_TRANSITIONS: dict[str, frozenset[str]]` 鍵集合必須恰為 ALL_STATES（test_case_states.py 遍歷斷言）
- `STATE_IMPORTED: frozenset({STATE_PARSED, STATE_FAILED})`（:48）→ 本次加入 STATE_APPEALED
- `STATE_APPEALED: frozenset({STATE_SUBMITTED, STATE_FAILED})`（:53）→ 不動（D-02，Phase 10 再做）
- `can_transition(from, to)` / `assert_transition_allowed(from, to)` / `IllegalTransitionError`（含 from/to/allowed 三屬性）/ `UnknownStateError` → 語意一律不得鬆動

server.py:
- `render_appeal_json(draft) -> str`（appeal.py:480-515，回傳 `json.dumps(payload, ensure_ascii=False, indent=2)`）；契約鍵：format/case_class/case_seq/order_seq/order_code/visit_date/fee_year_month/deduction_upper_bound/deduction_reason/is_appealing/p1_order_seq/p2_order_code/p3_change_seq/p4_rate/p5_quantity/p6_points/p7_attachment/p8_reason1/p9_reason2/sections/word_stats/validation_errors
- generate handler（server.py:703-802）：`draft = build_appeal_draft(record, ...)`（:765）；`rule_found = rule.found`（:754）；`transition(case_id, "appealed")` 唯一呼叫點（:778，catch CaseNotFoundError/IllegalTransitionError 只 warning 不阻斷回應）
- 既有回應鍵（:782-802）：status/case_id/case_seq/order_code/record_no/appeal_sections/reason1/reason2/p6_points/total_char_count/rule_found/xml_p8_p9_valid/over_limit/validation_errors
- `_to_appeal_case`（:382-405）：回傳 dict（:391-405）目前無 id_number 鍵 → 本次（Task 3）加入 `"id_number": rec.id_number`
- 錯誤慣例：`ApiError`（:113-119）→ errorhandler 脫敏（:213-215）；未處理例外統一 500 脫敏（:223-227）

tests/test_server_case_store_integration.py:
- fixtures：`setup_tmp_casestore`（:12-18）、`client`（:21-26，ELC_API_KEYS={"valid-key-123": "clinic_a"}，POST 帶 headers={"X-API-Key": "valid-key-123"}）
- `test_generate_appeal_draft_with_optional_case_id`（:159-186）：:167-170 三行 fast-forward transition 本次刪除；:182-183 斷言 status/case_id；:185-186 斷言 rec.state == "appealed"
- import 區（:1-9）：已 `import server`（:7）；Task 3 需加 `from elc_audit_engine.parsers.models import DeductionRecord`

tests/test_case_states.py:
- `_MAIN_LINE`（:27-33）為六主線；`imported→appealed` 是旁支，須新增獨立參數化/獨立用例（不放進 _MAIN_LINE）
- :71-81 submitted 封閉測試、:84-93 imported→submitted / failed→submitted 非法、:96-109 未知狀態拋 UnknownStateError —— 全部須繼續通過
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: W1 合法化 imported→appealed（D-01）</name>
  <files>src/elc_audit_engine/case_store/states.py, tests/test_case_states.py</files>

  <read_first>
    - src/elc_audit_engine/case_store/states.py（:45-68 轉換表、:71-124 例外與語意）
    - tests/test_case_states.py（:27-33 _MAIN_LINE、:71-109 submitted 封閉與未知狀態負向測試）
  </read_first>
  <behavior>
    - Test 1: can_transition(STATE_IMPORTED, STATE_APPEALED) 回傳 True
    - Test 2: assert_transition_allowed(STATE_IMPORTED, STATE_APPEALED) 不拋例外
    - Test 3（回歸）: STATE_APPEALED→STATE_SUBMITTED 仍合法（D-02 保留，Phase 10 再做）
    - Test 4（回歸）: submitted→imported 仍拋 IllegalTransitionError、未知狀態仍拋 UnknownStateError（不鬆動）
  </behavior>
  <action>在 states.py:48 `_TRANSITIONS` 的 `STATE_IMPORTED` 目標 frozenset 加入 `STATE_APPEALED`（保留 `STATE_PARSED`/`STATE_FAILED`）——這是本任務唯一生產碼改動。不得觸碰 `IllegalTransitionError`/`UnknownStateError`/`can_transition`/`assert_transition_allowed`/`requires_reason` 的任何語意與實作；不得動其他邊（尤其 :53 `STATE_APPEALED → STATE_SUBMITTED` 依 D-02 保留給 Phase 10）。tests/test_case_states.py 新增旁支用例 `test_imported_to_appealed_is_legal`（驗證 can_transition 為 True 且 assert_transition_allowed 不拋），刻意不加入 `_MAIN_LINE`（imported→appealed 屬旁支、非主線）。generate handler 為 appealed 邊唯一呼叫點（server.py:778），sampling 案件（走 parsed/reviewing/reviewed 邊）不觸發該呼叫，故合法化 imported→appealed 不污染 sampling 預審流語義（D-01 確認）。
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_case_states.py -q</automated>
  </verify>
  <done>can_transition(STATE_IMPORTED, STATE_APPEALED) 為 True；新增旁支用例通過；既有 IllegalTransitionError/UnknownStateError/submitted 封閉測試全數維持綠。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: W4 generate 回應改回 render_appeal_json 標準契約（D-03）＋整合測試同步</name>
  <files>server.py, tests/test_server_case_store_integration.py</files>

  <read_first>
    - server.py（:38 import 區、:703-802 generate_appeal_draft handler）
    - src/elc_audit_engine/generators/appeal.py（:480-515 render_appeal_json 契約鍵）
    - tests/test_server_case_store_integration.py（:12-26 fixtures、:159-186 目標測試）
  </read_first>
  <behavior>
    - Test 1: POST /api/appeal/generate 回傳的 JSON == json.loads(render_appeal_json(draft)) 的 dict ∪ {status: "success", case_id, rule_found}（單一契約，無舊鍵）
    - Test 2: 回應含 sections（每項 key/title/text/trimmed）、word_stats.total_chars、p8_reason1、p9_reason2、case_class
    - Test 3: imported 案件（不做任何 fast-forward transition）直接 generate 後 rec.state == "appealed"（W1 合法邊生效）
  </behavior>
  <action>重構 server.py generate_appeal_draft 回應段（:782-802）：主體改為 `jsonify(json.loads(render_appeal_json(draft)))`，再併入補充鍵 `status: "success"`、`case_id: case_id or None`、`rule_found: rule.found`（值來自 :754 的 `rule.found`，不在 AppealDraft 內）；**刪除**舊鍵 appeal_sections/reason1/reason2/p6_points/total_char_count/xml_p8_p9_valid（D-03 明示單一契約、不做雙契約、不做獨立轉換函數）。於 server.py import 區（現有 :38 `from elc_audit_engine.generators import build_appeal_draft` 附近）新增 `render_appeal_json` 的 import（自 elc_audit_engine.generators.appeal，比照 `build_appeal_draft` 的既有導入路徑慣例）。tests/test_server_case_store_integration.py：刪除 :167-170 三行 fast-forward transition，改為 imported 案件直接 generate 後斷言 rec.state=="appealed"；擴充 :179-186 回應斷言——data["status"]=="success"、data["case_id"]=="APP-2001"、data["rule_found"] 為 bool、data["sections"] 為 list 且每項含 key/title/text/trimmed、data["word_stats"]["total_chars"]、data["p8_reason1"]、data["p9_reason2"]、data["case_class"]。其餘測試（含 :147-156「轉換失敗僅 warning 不阻斷」語意）不得改動。
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_server_case_store_integration.py tests/test_case_states.py -q</automated>
  </verify>
  <done>generate 回應 JSON 恰為 render_appeal_json 契約＋三個補充鍵；無 fast-forward 下 imported→appealed 直接達成（rec.state=="appealed"）；舊鍵 appeal_sections/reason1/xml_p8_p9_valid 不再出現於回應；兩測試檔全綠。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: _to_appeal_case 透傳 rec.id_number（W5 數據流補全，供 09.1-03 轉換層 join）</name>
  <files>server.py, tests/test_server_case_store_integration.py</files>

  <read_first>
    - server.py（:382-405 _to_appeal_case 構造、:846 appeal 匯入端點呼叫點）
    - src/elc_audit_engine/parsers/models.py（:152-200 DeductionRecord dataclass；id_number 為欄 9「健保署已遮罩後 4 碼」，:166 註解、:189 欄位）
    - tests/test_server_case_store_integration.py（:1-9 import 區與 fixture 慣例）
  </read_first>
  <behavior>
    - Test 1: server._to_appeal_case(1, DeductionRecord(id_number="A123****", case_seq="201", order_code="14050B")) 輸出 dict 含 id_number == "A123****"（遮罩值照印）
    - Test 2（回歸）: rec.id_number 缺省（None）時輸出 dict["id_number"] is None（不捏造、不留空字串）
  </behavior>
  <action>在 server.py `_to_appeal_case`（:382-405）的回傳 dict（:391-405）加入 `"id_number": rec.id_number,`——`DeductionRecord.id_number` 為欄 9「健保署已遮罩後 4 碼」（models.py:166/189，PHI 但為遮罩值）；僅透傳不重組、不做遮罩推導（承襲 field_mapping.py:19-22 語意）。**不得**在本函式同時補 patient_name/case_class/orders 等鍵——那些屬 plan 03 轉換層缺欄誠實留空＋warning 的範圍（D-05 缺欄歸因），server 層只補單一 join 鍵（id_number）。tests/test_server_case_store_integration.py 新增 `test_to_appeal_case_passthroughs_id_number`（檔頭 import 區加 `from elc_audit_engine.parsers.models import DeductionRecord`）：① `server._to_appeal_case(1, DeductionRecord(id_number="A123****", case_seq="201", order_code="14050B"))` 斷言輸出 `id_number == "A123****"`；② `server._to_appeal_case(1, DeductionRecord(case_seq="201", order_code="14050B"))`（id_number 缺省 None）斷言輸出 `id_number is None`。其餘既有測試（含 :159-186 generate 測試）不得改動。
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_server_case_store_integration.py -q</automated>
  </verify>
  <done>_to_appeal_case 輸出含 id_number 鍵、值恰為 rec.id_number（None 時為 None）；新測試兩斷言全綠；既有 test_server_case_store_integration.py 全部測試仍綠。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API（/api/appeal/generate） | 未信任輸入（case_seq/order_code/deduction_reason/case_id）跨入；回應含病歷摘要 sections |
| API→CaseStore（transition） | 伺服器內；狀態轉換合法性由轉換表把關 |
| API→appeal.py（render_appeal_json） | 伺服器內；本計畫只消費其標準契約 |
| CaseStore/DeductionRecord→_to_appeal_case | rec.id_number（遮罩 PHI）透傳入 payload dict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-091-01 | Information Disclosure | generate 回應含病歷摘要 sections | mitigate | 回應主體即既有 `render_appeal_json` 的既定契約（appeal.py:480-515，不新增欄位）；transition 失敗維持 server.py:780 既有 `app.logger.warning` 只記 case_id 與異常物件，不記 draft/sections 內容 |
| T-091-02 | Elevation of Privilege | 狀態機 imported→appealed 合法化 | mitigate | 僅擴 `_TRANSITIONS` 合法邊集合（states.py:48 單行）；`can_transition`/`assert_transition_allowed` 對未知狀態仍拋 `UnknownStateError`、對非法邊仍拋 `IllegalTransitionError`（:98-119 不觸碰）；test_case_states.py 的 submitted 封閉（:71-81）與未知狀態（:96-109）測試保留且納入 verify 全綠 |
| T-091-03 | Spoofing | rule_found 補充鍵 | accept | 服務端 `get_rule` 產出的唯讀布林（:754），客戶端不可控制，低價值 |
| T-091-04 | Denial of Service | json.loads(render_appeal_json(draft)) 二次解析 | accept | 輸入為本機 `json.dumps` 產出的 str，非外部輸入；單筆案件規模，開銷可忽略 |
| T-091-05 | Information Disclosure | `_to_appeal_case` 透傳 id_number（遮罩 PHI） | mitigate | 值為 `DeductionRecord.id_number`（健保署已遮罩後 4 碼，models.py:166/189），與 field_mapping.py:19-22 既有 PHI 遮罩語意一致；僅透傳不重組完整字號；該 payload 已是前端列表既有消費資料（appeal 匯入端點 :846），不新增暴露面 |
</threat_model>

<verification>
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_case_states.py tests/test_server_case_store_integration.py -q
（應 < 30s；若 uv cache 唯讀，用 UV_CACHE_DIR=/tmp/uv-cache 環境變數）
</verification>

<success_criteria>
- `_TRANSITIONS[STATE_IMPORTED]` 含 `STATE_APPEALED`，其餘邊不變（appealed→submitted 依 D-02 保留）
- `/api/appeal/generate` 回應 == `render_appeal_json` 契約 dict ∪ {status, case_id, rule_found}，舊鍵全刪
- test_server_case_store_integration.py 刪除 fast-forward 後 `rec.state == "appealed"` 直接成立
- 非法轉換/未知狀態例外語意未鬆動（test_case_states.py 既有負向測試全綠）
- `_to_appeal_case` 輸出含 `id_number`（rec.id_number 遮罩值），patient_name 仍為 None（W5 轉換層補欄、缺欄歸因）
</success_criteria>

<output>
完成後建立 `.planning/phases/09.1-address-tech-debt-09-w4/09.1-01-SUMMARY.md`
</output>
