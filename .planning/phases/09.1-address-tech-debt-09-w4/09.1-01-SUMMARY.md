---
phase: 09.1-address-tech-debt-09-w4
plan: 1
subsystem: backend
tags: [state-machine, appeal, contract, flask, json]
requires:
  - phase: 09-his-servicing
    provides: CaseStore 狀態機（states.py 轉換表）、/api/appeal/generate handler、render_appeal_json 標準契約（appeal.py）
provides:
  - W1 合法邊 imported→appealed（D-01）：狀態機單行擴充，generate handler 為唯一呼叫點、sampling 流不污染
  - W4 契約橋（D-03）：/api/appeal/generate 回應主體改回 render_appeal_json 標準契約（sections/word_stats/p1-p9）＋僅併 status/case_id/rule_found 三補充鍵；舊鍵整段移除
  - W5 數據流前置（D-05）：_to_appeal_case 透傳 rec.id_number（遮罩後 4 碼）供 09.1-03 轉換層 join
affects: [09.1-address-tech-debt-09-w4 (plan 02/03), phase 10, static/index.html 前端適配]
tech-stack:
  added: []
  patterns:
    - "單一契約回應：handler 以 render_appeal_json 為主體、json.loads 二次解析後併補充鍵（T-091-04 accept）"
    - "W5 透傳：僅透傳遮罩 PHI（rec.id_number）、不重組不推導，與 field_mapping.py:19-22 語意一致"
key-files:
  created: []
  modified:
    - src/elc_audit_engine/case_store/states.py
    - tests/test_case_states.py
    - server.py
    - tests/test_server_case_store_integration.py
key-decisions:
  - "p6_points/case_seq/order_code 是 render_appeal_json 標準契約內建鍵（appeal.py:485/487/499），非舊鍵——測試舊鍵清單僅列真正舊鍵（appeal_sections/reason1/reason2/total_char_count/xml_p8_p9_valid/頂層 over_limit/record_no）"
  - "generate 回應的 case_class 誠實為 None（record.case_class 缺省）——屬 09.1-03 轉換層補欄範圍，非本 plan 捏造"
patterns-established:
  - "D-01 確認句：imported→appealed 合法邊的 server 呼叫點唯一在 /api/appeal/generate，sampling 案件走 parsed/reviewing/reviewed 邊不觸發，故全局轉換表合法化不污染預審流"
requirements-completed: [W1, W4, W5]
duration: 14min
completed: 2026-08-08
---

# Phase 09.1 Plan 1: 狀態機語義（W1）＋generate 契約橋（W4）＋id_number 透傳（W5）Summary

**W1 合法化 imported→appealed（states.py 單行擴充）、/api/appeal/generate 回應改回 render_appeal_json 單一標準契約（sections/word_stats/p1-p9＋status/case_id/rule_found 三補充鍵、舊鍵全刪）、_to_appeal_case 透傳 rec.id_number 遮罩值——三處整合缺陷一次對齊，並同步測試（移除 fast-forward hack）**

## Performance

- **Duration:** 14 min
- **Started:** 2026-08-08T05:58:00Z（估）
- **Completed:** 2026-08-08T06:12:37Z
- **Tasks:** 3 / 3
- **Files modified:** 4

## Accomplishments

- **W1（D-01）**：`_TRANSITIONS[STATE_IMPORTED]` 加入 `STATE_APPEALED`（保留 `STATE_PARSED`/`STATE_FAILED`）；新增旁支用例 `test_imported_to_appealed_is_legal`（刻意不加入 `_MAIN_LINE`）。`appealed→submitted` 依 D-02 保留給 Phase 10；`IllegalTransitionError`/`UnknownStateError`/submitted 封閉語意全數不鬆動（test_case_states.py 27 passed）。
- **W4（D-03）**：generate handler 回應改為 `json.loads(render_appeal_json(draft))` 為主體＋僅併 `status/case_id/rule_found` 三補充鍵；舊鍵（appeal_sections/reason1/reason2/total_char_count/xml_p8_p9_valid/頂層 over_limit/record_no）整段移除，同時刪除 handler 內不再使用的 `record_no` 變數。前端可直接落盤 `appeal_{流水號}.json` 餵 build_appeal_xml/build_appeal_print。
- **W5（D-05 前置）**：`_to_appeal_case` 回傳 dict 加 `"id_number": rec.id_number`（健保署已遮罩後 4 碼，僅透傳不重組；None 時照印 None）。新測試 `test_to_appeal_case_passthroughs_id_number` 雙斷言通過；patient_name/case_class/orders 不在本 plan 補（屬 09.1-03）。
- **測試同步**：test_server_case_store_integration.py:167-170 三行 fast-forward（含註解）刪除，改為 imported 案件直接 generate 後 `rec.state == "appealed"`（W1 合法邊直接生效）；generate 測試擴充斷言標準契約鍵與舊鍵缺席。

## Task Commits

每個 task 依 TDD（RED→GREEN）執行；sandbox `.git` 唯讀，未實際 commit，以下為建議 commit（orchestrator 代提交）：

1. **Task 1: W1 合法化 imported→appealed** - `feat(09.1-01): legalize imported→appealed transition (D-01)`
   - files: src/elc_audit_engine/case_store/states.py, tests/test_case_states.py
2. **Task 2: W4 generate 回應改回 render_appeal_json 標準契約** - `refactor(09.1-01): generate response uses render_appeal_json contract (D-03)`
   - files: server.py, tests/test_server_case_store_integration.py
3. **Task 3: _to_appeal_case 透傳 rec.id_number** - `feat(09.1-01): passthrough rec.id_number in _to_appeal_case (W5)`
   - files: server.py, tests/test_server_case_store_integration.py

**Plan metadata:** `docs(09.1-01): complete 09.1-01 plan`（含 SUMMARY.md）

## Files Created/Modified

- `src/elc_audit_engine/case_store/states.py` - `STATE_IMPORTED` 目標 frozenset 加 `STATE_APPEALED`（僅此一處生產碼改動＋D-01 註解）
- `tests/test_case_states.py` - 新增 `test_imported_to_appealed_is_legal` 旁支用例
- `server.py` - import 加 `render_appeal_json`；generate 回應段重構為標準契約＋三補充鍵；`_to_appeal_case` 加 `id_number` 透傳鍵；移除未使用 `record_no` 變數
- `tests/test_server_case_store_integration.py` - 刪 fast-forward 三行；擴充 generate 契約斷言；新增 `test_to_appeal_case_passthroughs_id_number`；import 加 `DeductionRecord`

## Decisions Made

- `p6_points`/`case_seq`/`order_code` 保留為標準契約鍵（見 Deviations 第 1 項）——以 `render_appeal_json`（appeal.py:485/487/499）為權威，回應必須等價於標準契約＋三補充鍵，符合 D-03「單一契約、不做雙契約」。
- generate 回應中 `case_class is None` 是誠實缺欄（record 未提供 case_class），非捏造；屬 09.1-03 轉換層補欄範圍。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - 測試斷言] Plan 舊鍵清單誤列標準契約內建鍵（p6_points/case_seq/order_code）**
- **Found during:** Task 2（W4 generate 契約測試）
- **Issue:** Plan action/task_guidance 將 `p6_points`/`case_seq`/`order_code` 列為「舊鍵整段移除」，但三者皆是 `render_appeal_json` 標準契約的內建鍵（appeal.py:485 `case_seq`、:487 `order_code`、:499 `p6_points`）。若移除，回應不再等價於標準契約，直接違反 D-03「單一契約、前端落盤 appeal_json 即可餵 build_appeal_xml/build_appeal_print」（後兩者消費 case_seq/order_code）。
- **Fix:** 測試改為斷言 `p6_points`（int≥0）/`case_seq`（=="201"）/`order_code`（=="14050B"）為標準契約鍵且值正確；真正舊鍵清單僅保留 `appeal_sections`/`reason1`/`reason2`/`total_char_count`/`xml_p8_p9_valid`/頂層 `over_limit`/`record_no`。生產碼無此問題（回應主體即 render_appeal_json 輸出）。
- **Files modified:** tests/test_server_case_store_integration.py
- **Verification:** generate 契約測試通過；回應含 sections/word_stats/p8_reason1/p9_reason2/case_class 且舊鍵缺席。
- **Committed in:** Task 2（建議 commit）

---

**Total deviations:** 1 auto-fixed (Rule 1)
**Impact on plan:** 修正後回應精確等價「render_appeal_json 契約 ∪ {status, case_id, rule_found}」，反而更貼合 D-03 單一契約目標。無 scope creep。

## Issues Encountered

- **sandbox 唯讀 filesystem（環境限制，非回歸）**：`tests/test_server_case_store_integration.py` 中 `test_import_sampling_cases_persists_to_casestore` 與 `test_duplicate_import_reports_conflicts` 因 `OSError: [Errno 30] Read-only file system: 'data/uploads/raw/...'`（sampling import 寫檔）失敗；與本 plan 改動無關（未觸碰 /api/sampling/import 路徑）。orchestrator 環境（可寫 data/）下應全綠。另 pytest cacheprovider 亦因唯讀無法寫 `.pytest_cache`（測試以 `-p no:cacheprovider` 執行，不影響結果）。
- 其餘全數通過：`tests/test_case_states.py` 27 passed；`tests/test_server_case_store_integration.py` 8 passed（排除上述 2 環境性失敗）。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- W1 狀態機語義已對齊：正常 appeal 用戶流 imported→appealed 可達，無 fast-forward hack。
- W4 契約橋就緒：前端（09.1-02 適配 static/index.html）可直接消費標準契約欄位；generate 回應即 `appeal_{流水號}.json` 內容＋三補充鍵。
- W5 數據流前置完成：`_to_appeal_case` 已含 id_number 遮罩值——09.1-03 的 `build_submission_from_case` 可在真實路徑下 join 患者層；patient_name/case_class/orders 缺欄誠實留空＋warning 屬 09.1-03 範圍。
- 待辦：appealed→submitted（D-02，Phase 10 電子上傳門控）。

---
*Phase: 09.1-address-tech-debt-09-w4*
*Completed: 2026-08-08*

## Self-Check: PASSED

- **Files exist:** 4 modified files + SUMMARY.md all present on disk (`FOUND` confirmed for server.py, src/elc_audit_engine/case_store/states.py, tests/test_case_states.py, tests/test_server_case_store_integration.py).
- **Tests:** `tests/test_case_states.py` 27 passed（含新增 `test_imported_to_appealed_is_legal` 與既有負向測試）；`tests/test_server_case_store_integration.py` 8 passed（含改寫 generate 契約測試與新增 `test_to_appeal_case_passthroughs_id_number`）。唯二失敗為 sandbox 唯讀 filesystem 環境限制（`data/uploads/raw` 不可寫，sampling import 測試），與本 plan 無關、非回歸。
- **Commits:** sandbox `.git` 唯讀，未 commit——建議 commit 清單已列於本文 Task Commits 節，由 orchestrator 代提交。
- **No stubs introduced:** 改動無佔位值/硬編碼空值；`case_class is None` 為誠實缺欄（09.1-03 補欄範圍）。
- **Threat scan:** 無新增 network endpoints/auth paths/file access patterns/schema change——無 Threat Flags。
