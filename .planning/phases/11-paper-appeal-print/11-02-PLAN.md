---
phase: 11-paper-appeal-print
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/elc_audit_engine/generators/appeal_print/template.py
  - src/elc_audit_engine/generators/appeal_print/__init__.py
  - src/elc_audit_engine/generators/__init__.py
  - officialdocument/電子申復文件格式/30396_1_1050105-1門診診療費用申復清單_print_base.odt
  - officialdocument/電子申復文件格式/30396_1_1050105-1門診診療費用申復清單_print_base.sha256
  - tests/test_appeal_print.py
autonomous: true
requirements:
  - REQ-paper-appeal-print
must_haves:
  truths:
    - 官方 ODT 經一次性布局壓縮成基準模板後，soffice 轉 PDF 收斂為 3 頁（每聯 1 頁；醫令 >15 行時 3×N）
    - 使用者可對一案件產出 `申復清單_{stem}.pdf`，版面含官方 14 主表資料欄（含傷病名稱欄，11-01 實測契約）＋7 頭表欄且關鍵文本（代號字碼/院所名稱/案件分類/流水號/醫令代碼/傷病名稱）可於 PDF 文本層驗證
    - 三聯版式差異正確（第二聯含「核定/複核/初核/審查委員」留空欄；第一/三聯無，系統不填）
    - 直接消費既有 AppealDraft 資料（render_appeal_print 純函式＋write_appeal_print 薄包裝），不重建資料模型
  artifacts:
    - path: "src/elc_audit_engine/generators/appeal_print/template.py"
      provides: "build_print_base：官方 ODT → 壓縮基準模板（版面參數收斂、sha256 產出；收斂失敗出口見 Task 1）"
      exports: ["build_print_base", "BASE_TEMPLATE_SHA256_PATH"]
    - path: "officialdocument/電子申復文件格式/30396_1_1050105-1門診診療費用申復清單_print_base.odt"
      provides: "壓縮基準模板（git 入庫版控資產，每聯 1 頁收斂）"
    - path: "src/elc_audit_engine/generators/appeal_print/__init__.py"
      provides: "render_appeal_print（純函式，回傳 (filled ODT bytes, warnings)）＋write_appeal_print（safe_filename＋makedirs＋soffice 轉 PDF，回傳 (pdf_path, warnings)）"
      exports: ["render_appeal_print", "write_appeal_print"]
    - path: "src/elc_audit_engine/generators/__init__.py"
      provides: "對外匯出 render_appeal_print/write_appeal_print"
  key_links:
    - from: "write_appeal_print"
      to: "odt_fill.fill_template → *_print_base.odt"
      via: "verify_template_hash（print_base.sha256 比對）＋soffice --headless --convert-to pdf"
      pattern: "soffice.*--convert-to.*pdf"
    - from: "render_appeal_print"
      to: "field_mapping.build_rows/paginate → odt_fill.fill_template"
      via: "純函式鏈（payload/facility/submission in → (filled ODT bytes, warnings) out）"
      pattern: "build_rows\\(.*paginate"
---

<objective>
解決 D-03「一次列印三頁」的版面風險（RESEARCH Open Q#3，本 phase 最大技術風險）：以一次性 `template.py` 把官方 ODT 壓縮成每聯一頁的 `*_print_base.odt` 基準模板（Golden＝官方 `30396_4` PDF，收斂測試斷言頁數=3）並 git 入庫；新增 `render_appeal_print`（純函式，回傳 filled ODT bytes＋缺欄 warnings）與 `write_appeal_print`（薄包裝：safe_filename＋makedirs＋soffice 轉 PDF，比照 `write_appeal` 模式）並自 `generators/__init__.py` 對外匯出；以 pypdf 端到端測試（頁數=3×N、關鍵欄位文本）、三聯版式差異測試（copies）與安全測試（注入/檔名穿越）補齊驗證。

Purpose: 11-01 已確保「欄位對」與「注入對」，此 plan 確保「版面對」——官方 ODT 若不壓縮直接轉 PDF 是 9 頁（每聯 3 頁），D-03 三頁收斂全在此 plan。
Output: 壓縮基準模板（入 git）、`render_appeal_print`/`write_appeal_print` API、e2e/copies/security 測試全綠。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-paper-appeal-print/11-RESEARCH.md
@.planning/phases/11-paper-appeal-print/11-PATTERNS.md
@.planning/phases/11-paper-appeal-print/11-VALIDATION.md
@.planning/phases/11-paper-appeal-print/11-01-SUMMARY.md
@src/elc_audit_engine/generators/appeal.py
@src/elc_audit_engine/generators/appeal_print/field_mapping.py
@src/elc_audit_engine/generators/appeal_print/odt_fill.py
@src/elc_audit_engine/generators/__init__.py
@src/elc_audit_engine/rule_repository/docx_tree/doc_converter.py
@src/elc_audit_engine/safe_paths.py
@officialdocument/電子申復文件格式/30396_4_無刪除線1050105-PDF門診診療費用申復清單-.pdf

<interfaces>
<!-- 依賴 11-01 產出之接口（詳見 11-01-SUMMARY.md） -->
From src/elc_audit_engine/generators/appeal_print/field_mapping.py:
  build_rows(payload: dict, facility: dict, *, submission: dict | None = None) -> tuple[list[dict], list[str]]
  build_header(facility: dict, payload: dict, submission: dict | None, *, report_type: str = "送核") -> dict
  paginate(order_rows: list[dict], per_page: int = 15) -> list[list[dict]]
  # build_rows 產出 14 鍵 dict（案件分類/流水號/身份證字號/姓名/傷病名稱/醫令序/內容/數量/金額/理由/審核意見/補付數量/單價/補付金額）；
  # 第二個回傳值＝缺欄 warnings（欄位名清單），本 plan 沿 render/write 鏈傳出
From src/elc_audit_engine/generators/appeal_print/odt_fill.py:
  fill_template(template_odt_path, header_fields, pages, output_odt_path, *, expected_sha256=None) -> str
  verify_template_hash(template_path, expected_sha256) -> None（不符 raise ValueError）
  AppealPrintFillError(ValueError)

<!-- 既有工具鏈接口 -->
From src/elc_audit_engine/rule_repository/docx_tree/doc_converter.py:
  soffice_is_functional() -> bool（真實轉檔探測；--version 成功不代表可用）
  _build_convert_cmd(profile_dir, outdir, convert_to, source_path) -> list[str]
  # soffice -env:UserInstallation={profile.as_uri()} --headless --norestore --nolockcheck
  #   --convert-to {pdf} --outdir {outdir} {source}；profile 目錄須先 makedirs
From src/elc_audit_engine/generators/appeal.py（write_appeal 薄包裝模式）:
  stem = safe_filename(file_stem or (case_seq or "unknown"), "file_stem/case_seq")
  os.makedirs(output_dir, exist_ok=True)
From src/elc_audit_engine/safe_paths.py:
  safe_filename(value, field_name) -> str；UnsafeIdentifierError

<!-- Golden 版面基準（驗證目標） -->
官方 30396_4_無刪除線...pdf：每聯 1 頁、資料列行高約 27pt、總 3 頁。
官方 ODT 直轉 PDF：9 頁（每聯 3 頁）——溢出點：標題後 3 空段（約 170pt）、頭表前後約 90~170pt 空隙、
主表差約 1 行、說明表含 7 條說明＋frame（高 2in）過高。
RESEARCH 實驗（固定列高 0.36in＋字號 6pt＋邊距 0.05in）仍 9 頁——精確參數需以 bbox 量測迭代收斂，
收斂失敗出口見 Task 1（迭代上限 10 輪＋上報 orchestrator 轉 checkpoint:decision，不靜默放寬 3 頁判準）。

<!-- 本 plan 產出之接口契約（供 11-03 消費，簽名不得再改） -->
From src/elc_audit_engine/generators/appeal_print/__init__.py:
  render_appeal_print(payload, facility, *, template_odt_path, submission=None) -> tuple[bytes, list[str]]
    # (filled ODT bytes, warnings)；warnings＝build_rows 缺欄欄位名清單，沿鏈傳出供 CLI 呈現
  write_appeal_print(output_dir, file_stem, payload, facility, *, template_odt_path, submission=None,
                     soffice_timeout=120) -> tuple[str, list[str]]
    # (pdf_path, warnings)；PDF 檔名 申復清單_{stem}.pdf；模板 sha256 校驗內建（T-11-06）

<!-- RESEARCH A2 切換條件（-k e2e 中文文本斷言 fallback） -->
pypdf 中文提取屬未實證假設（RESEARCH A2）。e2e 測試設 helper extract_pdf_text(pdf_path)：
優先 pypdf 提取；若目標中文鍵文本提取結果為空/拋例外（A2 未成立）→ 自動切換 pdftotext（poppler，
本機已驗證，soffice 環境同屬系統套件）或 filled ODT XML 層（zipfile+ET 解析 content.xml，比照 -k copies）；
兩路皆無法提取才 fail。copies 測試已迴避 A2（ODT XML 層斷言），e2e 對齊同一策略。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: template.py 壓縮基準模板產生器＋收斂測試（soffice 轉 PDF 頁數=3）</name>
  <files>src/elc_audit_engine/generators/appeal_print/template.py, officialdocument/電子申復文件格式/30396_1_1050105-1門診診療費用申復清單_print_base.odt, officialdocument/電子申復文件格式/30396_1_1050105-1門診診療費用申復清單_print_base.sha256, tests/test_appeal_print.py</files>
  <read_first>
    - src/elc_audit_engine/generators/appeal_print/template.py（本任務產出；先建立再填內容）
    - 11-RESEARCH.md（Pitfall 1 溢出版面、Open Q#3 壓縮參數、A1 fallback、Code Examples 1/2、Validation -k e2e）
    - 11-PATTERNS.md（template.py 應用規則、build_docx_trees.py 一次性建置模式）
    - src/elc_audit_engine/rule_repository/scripts/build_docx_trees.py（one-shot build：讀設定→執行→makedirs→寫出→列印摘要）
    - src/elc_audit_engine/generators/appeal_print/odt_fill.py（fill_template 注入邏輯可重用）
    - officialdocument/電子申復文件格式/30396_1_1050105-1門診診療費用申復清單.odt（來源模板）
    - officialdocument/電子申復文件格式/30396_4_無刪除線1050105-PDF門診診療費用申復清單-.pdf（Golden：每聯 1 頁）
  </read_first>
  <behavior>
    - Test 1（base, @requires_soffice）: build_print_base 產出之基準模板經 soffice 轉 PDF，pypdf 斷言總頁數=3（每聯 1 頁）
    - Test 2（base）: 產物 `*_print_base.odt` 存在、content.xml 可被 zipfile+ET 解析（含 9 tables 結構不破壞）
    - Test 3（base）: 產物 sha256 與同目錄 `*_print_base.sha256` 內容一致
  </behavior>
  <action>
    建立 template.py（一次性布局壓縮工具，產物入 git、不於每次 render 重複調參）：
    - `build_print_base(src_odt: str, out_odt: str, *, sha256_out: str) -> str`：以官方 ODT（30396_1，D-02 指定之可編輯套版來源）為輸入，輸出壓縮基準模板。壓縮操作（以 RESEARCH Pitfall 1 溢出點為目標，參數於實現時以 bbox 量測迭代）：
      - 縮減頁面邊距（RESEARCH 實驗 0.05in 起）；刪除標題段後 3 個空段（約 170pt 回收）
      - 收縮頭部資訊表前後空隙（約 90~170pt 回收）
      - 固定主表資料列（row2~16）行高至使 15 行＋表頭＋合計列恰好單頁（官方 PDF 約 27pt/行為基準，實驗 0.36in 仍溢出→須下修；以「主表合計列與說明表同頁」為收斂條件）
      - 壓縮說明表：縮字號、移除/縮小 frame（高 2in 過高）、保留第二聯說明表 row1「核定/複核/初核/審查委員」欄位結構
      - 收斂判準（Golden＝30396_4）：soffice 轉 PDF 後總頁數=3 且每聯的合計列與說明表落在同頁
      - 寫入 `sha256_out`（產物 ODT 的 sha256 值，供 odt_fill.verify_template_hash 生成前校驗，A5/T-11-06）
      - 參數迭代過程與最終採用的參數值記錄於 template.py 模組 docstring（供日後人工複核/重製）
    - **收斂失敗出口（A1 fallback，RESEARCH Open Q#3）**：收斂迭代上限 **10 輪**（邊距/列高/字號/frame 之參數組合）。10 輪內未達 3 頁判準 → **停止收斂**，將已試參數與每輪 bbox/頁數量測數據全部寫入 11-02-SUMMARY.md，Task 1 標記「未收斂」並上報 orchestrator 轉 `checkpoint:decision`（D-03 需重新詮釋，RESEARCH A1 fallback）；**不得靜默放寬 3 頁判準**、不得產出偽收斂模板入 git。
    - 實際執行 `build_print_base` 產出 `officialdocument/電子申復文件格式/30396_1_1050105-1門診診療費用申復清單_print_base.odt` 與 `..._print_base.sha256`（**兩者皆 git 入庫**——officialdocument/ 未被 .gitignore；唯一非 data/output 的產出例外，比照官方模板版控資產）
    - tests/test_appeal_print.py 補上 -k base 三個 behavior 測試（Test 1 標 @requires_soffice，soffice 不可用時 skip）
    - 收斂迭代期間使用系統 `pdfinfo`/`pdftotext`（本機已裝）或 pypdf 量測；測試以 pypdf 斷言
  </action>
  <verify>
    <automated>uv run pytest tests/test_appeal_print.py -k base -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_appeal_print.py -k base -x` 全綠（Test 1 在 soffice 可用環境實際執行並通過；soffice 缺席時該測試 skip 且其他 2 個綠）
    - `git status` 顯示 `*_print_base.odt` 與 `*_print_base.sha256` 為可追蹤狀態（未被 .gitignore 排除）
    - template.py docstring 記載最終收斂參數（邊距/列高/字號/frame 處理）與迭代結果
    - 若 10 輪內未收斂：11-02-SUMMARY.md 已記錄全部迭代參數與 bbox/頁數數據、未產出偽收斂模板，且 Task 1 已標記未收斂並上報 orchestrator（不靜默放寬 3 頁判準）
  </acceptance_criteria>
<done>uv run pytest tests/test_appeal_print.py -k base -x 全綠（soffice 可用環境 Test 1 實跑）；*_print_base.odt 與 *_print_base.sha256 已入 git（未被 gitignore）；template.py docstring 記載收斂參數；收斂失敗時已依出口上報（11-02-SUMMARY 記錄迭代數據，不靜默放寬 3 頁）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: render_appeal_print／write_appeal_print＋generators 匯出＋e2e/security 測試</name>
  <files>src/elc_audit_engine/generators/appeal_print/__init__.py, src/elc_audit_engine/generators/__init__.py, tests/test_appeal_print.py</files>
  <read_first>
    - src/elc_audit_engine/generators/appeal_print/__init__.py（本任務產出；先建立再填內容）
    - src/elc_audit_engine/generators/appeal.py（render_appeal_markdown 純函式＋write_appeal 薄包裝模式，lines 418-550）
    - src/elc_audit_engine/generators/__init__.py（現有匯出結構與 __all__ 維護方式）
    - src/elc_audit_engine/generators/appeal_print/field_mapping.py（build_rows/build_header/paginate）
    - src/elc_audit_engine/generators/appeal_print/odt_fill.py（fill_template/verify_template_hash）
    - src/elc_audit_engine/rule_repository/docx_tree/doc_converter.py（soffice_is_functional/_build_convert_cmd/convert 錯誤語意）
    - 11-RESEARCH.md（Code Example 3 soffice 轉 PDF、Pattern 2、A2 fallback、Validation -k e2e/-k security）
    - 11-PATTERNS.md（render/write 應用規則、PHI 安全錯誤、一案一檔案）
  </read_first>
  <behavior>
    - Test 1（e2e, @requires_soffice）: write_appeal_print 產出 PDF 存在，pypdf 斷言頁數=3、提取文本含「代號字碼」「測試醫療院所」「案件分類」值與醫令代碼（如 01015C）。**A2 fallback**：文本提取經 helper `extract_pdf_text(pdf_path)`——pypdf 提取空/例外（A2 未成立）時自動切換 `pdftotext`（poppler）或 filled ODT XML 層斷言同等文本，兩路皆無法提取才 fail
    - Test 2（e2e, @requires_soffice）: 15 行內醫令不分頁（頁數=3）；16 行醫令分頁（頁數=6）
    - Test 3（security）: write_appeal_print 的 file_stem 含 `../` 或非法字元 → UnsafeIdentifierError，且外部目錄未被寫入
    - Test 4（security）: 欄位值含 `<script>` 與 `&` → render_appeal_print 仍成功回傳 (bytes, warnings)（fill 階段），soffice 可用時 write_appeal_print 轉檔成功（不 raise）
  </behavior>
  <action>
    填實 `src/elc_audit_engine/generators/appeal_print/__init__.py`：
    - `render_appeal_print(payload: dict, facility: dict, *, template_odt_path: str, submission: dict | None = None) -> tuple[bytes, list[str]]`：純函式（**無專案目錄副作用**）——build_header＋build_rows→paginate→以 `tempfile.TemporaryDirectory()`（系統 tmp，非專案資料目錄）呼叫 fill_template 產出 filled ODT，再以 zipfile/Path 讀回 bytes；回傳 `(filled_odt_bytes, warnings)`（warnings＝build_rows 缺欄欄位名清單，沿 render→write→CLI 鏈傳出，11-03 消費）。docstring 明註「不寫入 data/output 或任何專案資料目錄」——`tempfile` 系統 tmp 目錄用完即清，消除「純函式卻寫盤」矛盾
    - `write_appeal_print(output_dir: str | os.PathLike[str], file_stem: str, payload: dict, facility: dict, *, template_odt_path: str, submission: dict | None = None, soffice_timeout: int = 120) -> tuple[str, list[str]]`：薄包裝，比照 write_appeal（lines 518-550）——
      - stem = safe_filename(file_stem, "file_stem")（P1-3/T-11-04）
      - os.makedirs(output_dir, exist_ok=True)
      - 以 verify_template_hash 比對模板 sha256（讀 `*_print_base.sha256`，T-11-06；官方模板未壓縮版本不校驗 hash）
      - filled_odt_bytes, warnings = render_appeal_print(...) → 寫暫存 filled .odt（output_dir 內或 tmp_path）→ soffice 轉 PDF（比照 doc_converter.py `_build_convert_cmd`：`-env:UserInstallation` 導向 tmp profile、`--headless --norestore --nolockcheck --convert-to pdf`、timeout 120s；returncode≠0 或輸出檔不存在 → raise AppealPrintFillError，訊息含失敗階段不含 PHI）
      - 回傳 `(pdf_path, warnings)`；PDF 檔名 `申復清單_{stem}.pdf`
    - 自訂錯誤：`render_appeal_print`/`write_appeal_print` 的輸入校驗（payload/facility 非 dict）raise TypeError/ValueError（含欄位名）
    - `src/elc_audit_engine/generators/__init__.py`：`from .appeal_print import render_appeal_print, write_appeal_print` 並加入 `__all__`
    - tests/test_appeal_print.py 補上 -k e2e 與 -k security 四個 behavior 測試（e2e 標 @requires_soffice）；fixture 用 `sample_appeal_draft()`（11-01）產出 payload dict、tmp_path 隔離輸出；e2e 中文文本斷言走 helper `extract_pdf_text`（A2 fallback：pypdf→pdftotext→filled ODT XML 層，切換條件見 <context>），測試內註記實際採用路徑；security Test 4 對 render_appeal_print 解包 (bytes, warnings)
  </action>
  <verify>
    <automated>uv run pytest tests/test_appeal_print.py -k "e2e or security" -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_appeal_print.py -k "e2e or security" -x` 全綠（soffice 缺席時 e2e 兩項 skip、security 兩項必綠）
    - `uv run python -c "from elc_audit_engine.generators import render_appeal_print, write_appeal_print"` 成功
    - grep 驗證 write_appeal_print 使用 safe_filename 且 PDF 檔名以 `申復清單_` 前綴；render_appeal_print 使用 tempfile.TemporaryDirectory（無專案資料目錄寫入）
    - e2e 中文文本斷言非單點依賴 pypdf（helper 含 pdftotext/ODT XML fallback，A2 切換條件已文件化）
    - `uv run pytest tests/test_appeal_print.py -x` 全綠（11-01 的 mapping/odt/base 不因本任務回歸）
  </acceptance_criteria>
<done>uv run pytest tests/test_appeal_print.py -k "e2e or security" -x 全綠（soffice 缺席僅 e2e 兩項 skip）；generators 可 import render_appeal_print/write_appeal_print（回傳 (bytes|pdf_path, warnings)）；write_appeal_print 用 safe_filename 且 PDF 以 申復清單_ 前綴命名；render_appeal_print 以系統 tmp 組 bytes 不寫專案目錄；e2e 文本斷言有 A2 fallback</done>
</task>

<task type="auto">
  <name>Task 3: copies 三聯版式差異測試（第二聯含核定欄、第一/三聯無、系統不填＋合計列值）</name>
  <files>tests/test_appeal_print.py</files>
  <read_first>
    - 11-VALIDATION.md（Per-Task Verification Map 11-02-03、Manual-Only Verifications 第 2 項）
    - 11-RESEARCH.md（Open Q#1 差異證據：聯2說明 row1 有「核定|複核|初核|審查委員」；Q5 合計/說明僅末頁）
    - src/elc_audit_engine/generators/appeal_print/odt_fill.py（fill_template 對說明表不應有任何寫入；合計列語義見 11-01 Task 2）
    - officialdocument/電子申復文件格式/30396_1_1050105-1門診診療費用申復清單.odt（三聯說明表結構）
  </read_first>
  <action>
    在 tests/test_appeal_print.py 補上 -k copies 測試（不需 soffice，ODT XML 層斷言最穩，迴避 pypdf 中文提取未實證的 A2 風險）：
    - 以官方 ODT（或壓縮基準模板）跑 render_appeal_print 產出 filled ODT bytes，zipfile+ET 解析 content.xml
    - 斷言第二聯說明表（聯組第 2 組的說明 table）row1 的 4 個 cell 文本恰為「核定」「複核」「初核」「審查委員」
    - 斷言第一聯與第三聯說明表不含上述 4 個標題 cell（文本為空）
    - 斷言該欄位系統未填值（核定/複核/初核/審查委員對應的填寫 cell 文本為空——系統不產出健保署複核結果，與 Deferred「第三聯複核結果回填」區隔）
    - 斷言各聯末頁合計列（row17）值（11-01 Task 2 語義）：cell[0]＝「合計」、數量欄 cell[2]＝該聯資料行數加總（人次）、金額與補付數量/單價/補付金額 cell 為空——與 -k odt 的合計列值斷言互相印證（SC1 逐欄一致）
    - 斷言資料列 cell[4]（傷病名稱欄）文本與 build_rows 的「傷病名稱」鍵值一致（14 鍵注入不逐欄錯位，SC1 防護）
    - 若 11-01-SUMMARY 的使用者裁示為「以官方模板第二聯為準」，測試註解引用該裁示
  </action>
  <verify>
    <automated>uv run pytest tests/test_appeal_print.py -k copies -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_appeal_print.py -k copies -x` 全綠
    - 測試直接引用官方模板結構（非寫死 9 tables 索引的脆弱斷言；以「聯組內說明表」定位）
    - -k copies 斷言末頁合計列值（「合計」＋人次加總＋補付欄空）與資料列 cell[4] 傷病名稱值，與 -k odt/-k mapping 一致
    - `uv run pytest tests/test_appeal_print.py -x` 全綠（mapping/odt/base/e2e/security/copies 全數通過或預期 skip）
  </acceptance_criteria>
<done>uv run pytest tests/test_appeal_print.py -k copies -x 全綠；測試以聯組內說明表定位（非寫死索引）；末頁合計列值已斷言（「合計」＋人次＋補付欄空）；資料列 cell[4] 傷病名稱已斷言；uv run pytest tests/test_appeal_print.py -x 全組全綠</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 模板資產 → template.py / write_appeal_print | 官方 ODT（git 版控）與壓縮基準模板（本 plan 產出後入 git）為供應鏈資產；竄改影響版面與資料注入 |
| filled ODT → soffice | 注入後 ODT 為不可信中間產物；soffice 為外部工具（subprocess） |
| 產出 PDF → 檔案系統 | 含 PHI（姓名/身分證字號/病歷內容），寫入 data/output（已 gitignore） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | render_appeal_print / fill_template | mitigate | 全部值經 ET 文本節點（11-01 已實作）；本 plan 的 behavior Test 4（`<script>`/`&`）驗證不破壞 ODT 且轉檔成功 |
| T-11-02 | Information Disclosure | write_appeal_print 輸出 | mitigate | PDF/暫存 filled .odt 一律寫入 data/output/*（.gitignore 已含）或 pytest tmp_path；`git status` 不得出現產出檔；render_appeal_print 組 bytes 走系統 tmp（TemporaryDirectory，非專案目錄） |
| T-11-03 | Information Disclosure | write_appeal_print 錯誤處理 | mitigate | 錯誤訊息只記失敗階段/模板路徑，不記 payload/欄位值全文（AppealPrintFillError 模式） |
| T-11-04 | Tampering/Elevation | 輸出檔名 | mitigate | safe_filename 校驗後拒絕（P1-3）；behavior Test 3 斷言 `../` 拒且不寫出外部目錄 |
| T-11-05 | Integrity | 身份證字號 | accept | 11-01 field_mapping 已實現遮罩照印/留空（來源＝submission.id_number）；本 plan 無新增暴露面（e2e fixture 用假資料） |
| T-11-06 | Tampering | 基準模板（供應鏈） | mitigate | 產物入 git 版控；生成前 verify_template_hash 比對 `*_print_base.sha256`（A5）；本 plan 產出 sha256 檔並接線 |
| T-11-07 | Integrity | 輸出檔覆寫 | mitigate | 一案一檔 `申復清單_{stem}.pdf`；file_stem 由呼叫端控制（CLI 傳 case_seq 或 `{case_seq}_{order_seq}`），與 write_appeal 同機制 |
</threat_model>

<verification>
- Task 1: `uv run pytest tests/test_appeal_print.py -k base -x`
- Task 2: `uv run pytest tests/test_appeal_print.py -k "e2e or security" -x`
- Task 3: `uv run pytest tests/test_appeal_print.py -k copies -x`
- Wave 收束: `uv run pytest tests/test_appeal_print.py -x`
- 既有套件不回歸: `uv run pytest tests/ -k "not appeal_print" -q`

<!-- VALIDATION.md 對齊註記（orchestrator 批准後更新 VALIDATION.md frontmatter/Per-Task Map；本 plan 的 task→test 映射以此為準） -->
- 11-02-01 → `-k base`（VALIDATION.md 現表列 -k security 為錯位，實為 Task 2）
- 11-02-02 → `-k e2e or security`（VALIDATION.md 表列 -k e2e）
- 11-02-03 → `-k copies`
- VALIDATION.md frontmatter `nyquist_compliant`/`wave_0_complete` 由 orchestrator 於批准後更新
</verification>

<success_criteria>
- 壓縮基準模板入 git，soffice 轉 PDF 收斂為 3 頁（每聯 1 頁；>15 行時 3×N）——Success Criteria 1 的版面前置條件達成（收斂失敗依 A1 出口上報，不靜默放寬）
- render_appeal_print（純函式，回傳 bytes＋warnings）＋write_appeal_print（薄包裝，回傳 pdf_path＋warnings）自 generators 對外匯出，直接消費 AppealDraft/既有 payload——Success Criteria 2 達成
- e2e 測試斷言頁數與關鍵欄位文本（A2 fallback 已預置）、copies 測試斷言第二聯核定欄差異、合計列值與資料列 cell[4] 傷病名稱、security 測試斷言注入與檔名穿越防線——Success Criteria 1/3 的可自動化部分達成
</success_criteria>

<output>
After completion, create `.planning/phases/11-paper-appeal-print/11-02-SUMMARY.md`
（含壓縮參數收斂結果與 e2e 測試實測頁數）
</output>
