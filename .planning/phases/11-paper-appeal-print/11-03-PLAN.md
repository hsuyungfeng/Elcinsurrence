---
phase: 11-paper-appeal-print
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - config/facility.json
  - config/settings.py
  - scripts/build_appeal_print.py
  - tests/test_appeal_print.py
  - README.md
autonomous: true
requirements:
  - REQ-paper-appeal-print
must_haves:
  truths:
    - 院所層固定欄位（代號字碼/名稱/地址/負責醫師等）由 config/facility.json 提供，可經 FACILITY_CONFIG_PATH env 覆寫路徑，缺檔/缺必填欄 fail-fast（D-04）
    - 使用者可執行 `python scripts/build_appeal_print.py <appeal_json> [case_payload_json] [output_pdf]` 產出一案一 PDF（不觸 server.py）
    - 紙本申復清單列印通道有 README 使用說明；全套件（含既有 374 基線）綠
  artifacts:
    - path: "config/facility.json"
      provides: "院所層欄位（code/name/address/physician_name 等）"
    - path: "config/settings.py"
      provides: "FACILITY_CONFIG_PATH（env 可覆寫）＋load_facility_config()（缺檔 FileNotFoundError、缺必填欄 ValueError）"
      exports: ["FACILITY_CONFIG_PATH", "load_facility_config"]
    - path: "scripts/build_appeal_print.py"
      provides: "CLI 入口（比照 build_appeal_xml.py：argv 校驗、錯誤分層、safe_filename、輸出路徑、缺欄 warnings 呈現）"
      exports: ["main"]
  key_links:
    - from: "scripts/build_appeal_print.py"
      to: "settings.load_facility_config → generators.write_appeal_print"
      via: "讀 appeal JSON＋case payload → write_appeal_print(output_dir, stem, payload, facility, template_odt_path=base) → 解包 (pdf_path, warnings) 列印 warnings"
      pattern: "write_appeal_print\\("
    - from: "config/settings.py"
      to: "config/facility.json"
      via: "FACILITY_CONFIG_PATH（預設 config/facility.json，env 覆寫）"
      pattern: "FACILITY_CONFIG_PATH"
---

<objective>
收束紙本申復清單輸出通道的**院所層設定（D-04）**與**列印入口**：新增 `config/facility.json`＋`settings.py` 的 `FACILITY_CONFIG_PATH`/`load_facility_config()`（fail-fast，比照 `load_llama_config`）；新增 `scripts/build_appeal_print.py` CLI（比照 `build_appeal_xml.py` 逐行模式，不觸 server.py，缺欄 warnings 於成功訊息後列印）；README 補使用說明並執行全套件回歸。

Purpose: 讓院所端可以真正跑出 PDF——D-04 的固定院所欄位落地、Open Q#6 定案 CLI 先行。此 plan 完成後整條通道從「appeal JSON → 可列印 PDF」可用。
Output: facility config 載入機制、CLI 腳本、README 章節、全套件綠。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-paper-appeal-print/11-PATTERNS.md
@.planning/phases/11-paper-appeal-print/11-VALIDATION.md
@.planning/phases/11-paper-appeal-print/11-02-SUMMARY.md
@config/settings.py
@config/llama_config.json
@scripts/build_appeal_xml.py
@src/elc_audit_engine/generators/appeal_print/__init__.py
@src/elc_audit_engine/safe_paths.py
@tests/test_appeal_xml.py
@tests/conftest.py
@README.md

<interfaces>
<!-- 依賴 11-02 產出之接口（詳見 11-02-SUMMARY.md；簽名含 warnings，見 11-02 <context>） -->
From src/elc_audit_engine/generators/appeal_print/__init__.py:
  render_appeal_print(payload: dict, facility: dict, *, template_odt_path: str, submission: dict | None = None) -> tuple[bytes, list[str]]
    # (filled ODT bytes, warnings)；warnings＝build_rows 缺欄欄位名清單
  write_appeal_print(output_dir, file_stem, payload, facility, *, template_odt_path, submission=None, soffice_timeout=120) -> tuple[str, list[str]]
    # (pdf_path, warnings)；PDF 檔名 申復清單_{stem}.pdf；模板 sha256 校驗內建；warnings 供本 plan CLI 呈現
From src/elc_audit_engine/generators/__init__.py:
  render_appeal_print, write_appeal_print 已自 generators 對外匯出

<!-- 既有 CLI 模式（逐行可複製的參照） -->
From scripts/build_appeal_xml.py:
  main(argv=None) -> int：argv 校驗（0 或過多 → usage + return 1）→ 讀檔（OSError/JSONDecodeError → return 1）
  → 組裝（Exception → return 1）→ 預設輸出路徑經 safe_filename → 寫檔（AppealXmlEncodingError/OSError → return 1）
  → 成功 print 路徑 + return 0；`if __name__ == "__main__": raise SystemExit(main())`
From tests/test_appeal_xml.py（CLI 測試模式）:
  from scripts.build_appeal_xml import main 直接呼叫 main([...])，斷言 return code 與輸出檔存在；
  缺參數/缺檔 return 1（lines 118-149）

<!-- config 載入模式 -->
From config/settings.py:
  LLAMA_CONFIG_PATH = os.getenv("LLAMA_CONFIG_PATH", os.path.join(PROJECT_ROOT, "config/llama_config.json"))
  load_llama_config()：缺檔 raise FileNotFoundError（fail-fast）→ json.load 回傳 dict
From tests/test_config.py: monkeypatch settings.LLAMA_CONFIG_PATH → tmp_path 缺檔測試（lines 25-29 模式）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: config/facility.json＋settings.py load_facility_config（D-04，fail-fast）</name>
  <files>config/facility.json, config/settings.py, tests/test_appeal_print.py</files>
  <read_first>
    - config/settings.py（本任務修改；先讀現狀再改）
    - config/llama_config.json（既有 JSON config 資產格式）
    - 11-PATTERNS.md（config 應用規則：FACILITY_CONFIG_PATH env 覆寫＋load_facility_config 缺檔 fail-fast）
    - 11-RESEARCH.md（Open Q#7 定案：JSON＋env 覆寫路徑）
    - tests/test_config.py（monkeypatch 缺檔測試模式）
  </read_first>
  <behavior>
    - Test 1（config）: settings.FACILITY_CONFIG_PATH 指向不存在的路徑時呼叫 load_facility_config() → raise FileNotFoundError
    - Test 2（config）: facility.json 缺必填欄位（如 code）→ raise ValueError（含欄位名）
    - Test 3（config）: monkeypatch FACILITY_CONFIG_PATH 指向 tmp_path 的合法 facility.json → 回傳 dict 且含全部鍵
  </behavior>
  <action>
    修改 config/settings.py（比照 load_llama_config 模式逐字複製改寫）：
    - 新增 `FACILITY_CONFIG_PATH = os.getenv("FACILITY_CONFIG_PATH", os.path.join(PROJECT_ROOT, "config/facility.json"))`
    - 新增 `load_facility_config() -> dict`：缺檔 raise FileNotFoundError（fail-fast，訊息含路徑）；JSON 解析失敗 raise ValueError；必填欄位校驗（`code`、`name` 至少必填，缺則 raise ValueError 含欄位名）——比照 load_llama_config「缺檔立刻失敗、不靜默空值」哲學
    - 建立 config/facility.json：UTF-8、無註解、單一院所物件（比照 llama_config.json 格式），鍵含 code（代號字碼）、name（醫療院所名稱）、address、physician_name，值為測試用示例（院所端日後自行編輯）
    - tests/test_appeal_print.py 補上 -k config 三個 behavior 測試（用 monkeypatch 覆寫 settings.FACILITY_CONFIG_PATH，不寫入正式 config）
  </action>
  <verify>
    <automated>uv run pytest tests/test_appeal_print.py -k config -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_appeal_print.py -k config -x` 全綠
    - `uv run python -c "from config.settings import load_facility_config, FACILITY_CONFIG_PATH; print(FACILITY_CONFIG_PATH)"` 輸出預設路徑 config/facility.json 且 load_facility_config() 回傳含 code/name 的 dict
    - grep 驗證 load_facility_config 含必填欄位校驗（code/name）
    - `uv run pytest tests/ -k "not appeal_print" -q` 既有套件不回歸（settings.py 為共享模組）
  </acceptance_criteria>
<done>uv run pytest tests/test_appeal_print.py -k config -x 全綠；load_facility_config() 缺檔 raise FileNotFoundError、缺必填欄 raise ValueError；既有 settings 測試不回歸</done>
</task>

<task type="auto">
  <name>Task 2: scripts/build_appeal_print.py CLI 入口＋CLI 端到端測試（Open Q#6 CLI 先行）</name>
  <files>scripts/build_appeal_print.py, tests/test_appeal_print.py</files>
  <read_first>
    - scripts/build_appeal_print.py（本任務產出；先建立再填內容）
    - scripts/build_appeal_xml.py（main() 模式全文，逐行可複製）
    - src/elc_audit_engine/generators/appeal_print/__init__.py（write_appeal_print 簽名與錯誤類型；回傳 (pdf_path, warnings)）
    - src/elc_audit_engine/safe_paths.py（UnsafeIdentifierError/safe_filename）
    - config/settings.py（load_facility_config、OUTPUT_DIR）
    - tests/test_appeal_xml.py（CLI 測試模式 lines 118-149）
    - 11-PATTERNS.md（CLI 應用規則）
  </read_first>
  <action>
    建立 scripts/build_appeal_print.py（比照 build_appeal_xml.py 的 main() 結構，不觸 server.py）：
    - `main(argv: list[str] | None = None) -> int`：
      - 位置參數：`<appeal_json_path> [case_payload_json_path] [output_pdf_path]`，接受 1~3 個參數。長度 2 時第 2 個為 output_pdf_path（後向兼容 build_appeal_xml 習慣）；長度 3 時第 2 個為 case_payload_json_path、第 3 個為 output_pdf_path。0 個或 >3 個 → 印 usage 至 stderr + return 1
      - 讀 appeal JSON（OSError/JSONDecodeError → stderr + return 1；非 dict → return 1，比照 build_appeal_xml.py 的 json.load 分層）
      - 讀 case_payload JSON（若有提供；OSError/JSONDecodeError → return 1；該檔為 CaseStore payload 或 SubmissionCase dict，可含 patient_name/primary_diagnosis/clinic/orders/id_number——即 11-01 submission 契約）
      - `facility = load_facility_config()`（FileNotFoundError/ValueError → stderr + return 1）
      - 決定輸出：未提供 output_pdf 時，輸出目錄用 settings.OUTPUT_DIR（data/output，已 gitignore），stem 由 appeal JSON 的 case_seq 與 order_seq 組出（`{case_seq}_{order_seq}`），先經 safe_filename（UnsafeIdentifierError → stderr + return 1）；template 路徑指向 `officialdocument/電子申復文件格式/30396_1_1050105-1門診診療費用申復清單_print_base.odt`（11-02 產物）
      - 呼叫 `pdf_path, warnings = write_appeal_print(output_dir, stem, payload, facility, template_odt_path=template, submission=submission)`；捕獲 UnsafeIdentifierError/AppealPrintFillError（含子類）/OSError/ValueError → stderr 人話訊息 + return 1（訊息不印 PHI 值全文，T-11-03）
      - 成功 → `print(f"已成功輸出申復清單 PDF：{pdf_path}")`；**若 warnings 非空，於成功訊息後逐條 `print(f"警告：{w}")`（缺欄誠實降級不隱藏，warnings 為 11-01 build_rows 的缺欄欄位名清單）**；+ return 0
      - `if __name__ == "__main__": raise SystemExit(main())`
    - tests/test_appeal_print.py 補上 -k cli 測試（比照 test_appeal_xml.py 的 CLI 測試）：
      - 缺參數 → return 1；缺檔案路徑 → return 1；不安全 stem（case_seq 含 `../`）→ return 1 且外部目錄未寫入
      - 完整流程（@requires_soffice）→ return 0 且輸出 PDF 存在、pypdf 頁數=3
      - **warnings 呈現**：不帶 case_payload 執行（submission 缺席）→ return 0 且 stdout 含「警告：」開頭行（缺欄欄位名誠實呈現，不隱藏、不 raise）。**soffice 隔離（monkeypatch 取代）**：本測試不標 @requires_soffice、不觸 soffice——以 `monkeypatch.setattr(scripts.build_appeal_print, "write_appeal_print", fake_write)` 替換模組內匯入的 write_appeal_print，`fake_write` 回傳 `(tmp_path/"fake.pdf", warnings)`（不實際轉檔、不回寫 data/output），如此 soffice 缺席時 warnings 呈現邏輯照常驗證（比照 test_config.py monkeypatch 模式；fake_write 簽名與 write_appeal_print 一致以讓 main 解包 (pdf_path, warnings)）
      - 用 fixture 產出 appeal JSON 與（可選）case payload JSON 於 tmp_path；以 monkeypatch 覆寫 settings.OUTPUT_DIR 指向 tmp_path
  </action>
  <verify>
    <automated>uv run pytest tests/test_appeal_print.py -k cli -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_appeal_print.py -k cli -x` 全綠（soffice 缺席時僅完整流程測試 skip；warnings 呈現測試經 monkeypatch 隔離 write_appeal_print，不觸 soffice、soffice 缺席時仍執行）
    - `uv run python scripts/build_appeal_print.py`（無參數）→ 印 usage 且 exit code 1
    - grep 驗證 scripts/build_appeal_print.py 未 import server／未觸碰任何既有端點
    - grep 驗證 scripts/build_appeal_print.py 解包 write_appeal_print 回傳 warnings 並列印（「警告：」前綴）
    - `uv run pytest tests/test_appeal_print.py -x` 全綠（mapping/odt/base/e2e/security/copies/config/cli 全數通過或預期 skip）
  </acceptance_criteria>
<done>uv run pytest tests/test_appeal_print.py -k cli -x 全綠；無參數執行印 usage 且 exit 1；scripts/build_appeal_print.py 未 import server；缺欄 warnings 於成功訊息後以「警告：」列印（stdout 斷言）；warnings 呈現測試以 monkeypatch 隔離 write_appeal_print（不觸 soffice）</done>
</task>

<task type="auto">
  <name>Task 3: README 使用說明＋全套件回歸驗證</name>
  <files>README.md, tests/test_appeal_print.py</files>
  <read_first>
    - README.md（現有結構與文件語氣；先讀再插入新章節）
    - scripts/build_appeal_print.py（Task 2 產出，使用方式）
    - .gitignore（data/output 已 ignore 之確認）
  </read_first>
  <action>
    在 README.md 新增「紙本申復清單列印」章節（比照既有章節語氣）：
    - 用途：未串接 HIS 或選擇紙本作業的院所，由 `appeal_{流水號}.json` 產出官方三聯式「門診醫療費用點數申復清單」可列印 PDF（與 Markdown/JSON/申復 XML 並行的輸出通道，不互相取代）
    - 前置條件：本機 LibreOffice（soffice，D-01）、`config/facility.json` 已填院所欄位（可經 FACILITY_CONFIG_PATH env 覆寫路徑）
    - 指令範例：`python scripts/build_appeal_print.py data/output/appeal_001.json`（基本）、`python scripts/build_appeal_print.py data/output/appeal_001.json data/cases_payload.json`（帶 case payload join 患者層欄位）、可選第三參數指定輸出 PDF 路徑
    - 行為說明：三聯一次列印（每聯 1 頁；醫令 >15 行自動分頁為 3×N 頁）；身份證字號照印遮罩值（submission.id_number）、join 不到的患者層欄位（身份證字號/姓名/傷病名稱/審查科別/數量/金額）留空且不捏造，CLI 會於成功訊息後以「警告：」列出缺欄欄位名；第二聯「中央健康保險署填列」核定/複核/初核/審查委員欄留空供健保署填寫
    - PHI 注意：PDF 輸出於 data/output/*（已 .gitignore，P0-3）
    - 若 tests/test_appeal_print.py 在收束時有任何 flaky 測試，修正後重跑；最後執行全套件回歸
  </action>
  <verify>
    <automated>uv run pytest</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` 全套件綠（含既有基線與本 phase 全部 -k 組；soffice 缺席時僅 requires_soffice 測試 skip）
    - README.md 含「紙本申復清單列印」章節，內含指令範例、前置條件、PHI 注意
    - grep 驗證 README 章節含 `scripts/build_appeal_print.py` 與 `FACILITY_CONFIG_PATH` 關鍵字
  </acceptance_criteria>
<done>uv run pytest 全套件綠（含既有基線）；README 含「紙本申復清單列印」章節（指令範例/前置條件/PHI 注意）</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI 輸入檔案 → build_appeal_print.py | appeal JSON／case payload JSON 為外部輸入（可含 PHI 與自由中文），路徑與內容均不可信 |
| config/facility.json → load_facility_config | 院所層設定檔（本地檔案）；缺檔/缺欄必須 fail-fast，不得靜默空值產出看似正常的 PDF |
| CLI 輸出 → 檔案系統 | PDF 含 PHI，寫入 data/output（gitignore）；錯誤訊息不得洩露 PHI 全文 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | write_appeal_print 鏈（CLI 呼叫端） | mitigate | 注入防線在 odt_fill（11-01/11-02 已實作）；CLI 僅傳遞資料不碰 XML 結構 |
| T-11-02 | Information Disclosure | CLI 輸出 | mitigate | 未指定輸出路徑時寫入 settings.OUTPUT_DIR（data/output，.gitignore）；README 明載 PHI 注意（P0-3） |
| T-11-03 | Information Disclosure | CLI 錯誤訊息 | mitigate | 全部 except 分支只印失敗階段/路徑/錯誤類別，不印 appeal JSON 內容或欄位值全文（ASVS V9）；缺欄 warnings 僅印欄位名（非值） |
| T-11-04 | Tampering/Elevation | CLI 輸出檔名 | mitigate | stem 一律經 safe_filename 校驗後拒絕（UnsafeIdentifierError → return 1）；CLI 測試斷言 `../` 拒且不寫出 |
| T-11-05 | Integrity | 身份證字號 | accept | field_mapping 已實現遮罩照印/留空（11-01）；CLI 無新增暴露面 |
| T-11-06 | Tampering | 基準模板路徑 | accept | template 路徑在 CLI 內為常數指向 git 版控資產；write_appeal_print 已內建 sha256 校驗（11-02），CLI 不再重複 |
| T-11-07 | Integrity | 輸出檔覆寫 | mitigate | CLI 預設 stem＝`{case_seq}_{order_seq}` 且先 safe_filename；同一 case_seq 多筆醫令以 order_seq 區隔，與 write_appeal 同機制 |
</threat_model>

<verification>
- Task 1: `uv run pytest tests/test_appeal_print.py -k config -x`
- Task 2: `uv run pytest tests/test_appeal_print.py -k cli -x`
- Task 3（Wave/Phase 收束）: `uv run pytest`（全套件，含既有基線）

<!-- VALIDATION.md 對齊註記（orchestrator 批准後更新 VALIDATION.md frontmatter/Per-Task Map；本 plan 的 task→test 映射以此為準） -->
- 11-03-01 → `-k cli`（VALIDATION.md 現表列 -k e2e 為錯位——e2e 屬 11-02；本 plan 無 e2e）
- 11-03-02 → `-k config`
- 11-01-01→`-k mapping`、11-01-02→`-k odt`、11-02-01→`-k base`、11-02-02→`-k e2e or security`、11-02-03→`-k copies`
- VALIDATION.md frontmatter `nyquist_compliant`/`wave_0_complete` 由 orchestrator 於批准後更新
</verification>

<success_criteria>
- 院所層欄位經 config/facility.json＋load_facility_config()（fail-fast）提供，env 可覆寫路徑（D-04）
- CLI 入口 `scripts/build_appeal_print.py` 可用：appeal JSON（＋可選 case payload）→ 一案一 PDF，不觸 server.py（Open Q#6 定案），缺欄 warnings 誠實呈現
- README 使用說明完成；全套件綠（含既有 374 基線）——Phase 11 三項 Success Criteria 全部可驗證
</success_criteria>

<output>
After completion, create `.planning/phases/11-paper-appeal-print/11-03-SUMMARY.md`
</output>
