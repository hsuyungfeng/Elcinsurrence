---
phase: 11-paper-appeal-print
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/elc_audit_engine/generators/appeal_print/__init__.py
  - src/elc_audit_engine/generators/appeal_print/field_mapping.py
  - src/elc_audit_engine/generators/appeal_print/odt_fill.py
  - tests/conftest.py
  - tests/test_appeal_print.py
  - pyproject.toml
  - uv.lock
autonomous: false
requirements:
  - REQ-paper-appeal-print
must_haves:
  truths:
    - 官方清單 14 個主表資料欄（含「傷病名稱」欄；見 <context> 實測表頭契約）＋7 個頭表欄與資料來源的逐欄對應已程式化（field_mapping），缺欄誠實降級不捏造
    - 每頁容量固定 15 行；醫令 >15 行時自動分頁（odt_fill），頁數欄遞增
    - 欄位值一律經 ET 文本節點注入（自動轉義），重打包 ODT 可被 soffice 正常開啟
  artifacts:
    - path: "src/elc_audit_engine/generators/appeal_print/field_mapping.py"
      provides: "build_rows（官方欄 ← AppealDraft/CaseStore payload/facility/submission 逐欄對應）＋paginate（15 行/頁分頁決定）＋build_header（頭表 7 欄）"
      exports: ["build_rows", "build_header", "paginate"]
    - path: "src/elc_audit_engine/generators/appeal_print/odt_fill.py"
      provides: "fill_template（ET 文本節點注入 content.xml＋zip 重打包＋分頁複製聯組）"
      exports: ["fill_template", "verify_template_hash"]
    - path: "tests/test_appeal_print.py"
      provides: "-k mapping / -k odt 測試（先紅後綠）"
    - path: "tests/conftest.py"
      provides: "facility_config、sample_appeal_draft fixtures；uv add --dev pypdf"
  key_links:
    - from: "field_mapping.build_rows"
      to: "AppealDraft／CaseStore payload／facility config／submission payload"
      via: "逐欄來源對應（案件分類←payload.case_class/流水號←payload.case_seq、身份證字號←submission.id_number 遮罩照印或留空、姓名←patient_name/傷病名稱←primary_diagnosis、join 不到留空）"
      pattern: "build_rows\\(.*facility"
    - from: "odt_fill.fill_template"
      to: "官方 ODT content.xml 表格單元格（table:table-cell → text:p）"
      via: "set_cell_text 以 p.text = value 寫入（ET 自動轉義）"
      pattern: "p\\.text = value"
---

<objective>
建立紙本申復清單的**欄位組裝層（field_mapping）**與**ODT 注入層（odt_fill）**：把 `AppealDraft`／`CaseStore payload`／facility config／submission 資料逐欄對應到官方清單 14 個主表資料欄（含傷病名稱欄）＋7 個頭表欄，缺欄以誠實降級處理（不捏造）；以 stdlib `xml.etree.ElementTree` 文本節點注入 `content.xml`、`zipfile` 重打包、醫令 >15 行自動分頁（D-06）。同時建立 Wave 0 測試脚手架（`tests/test_appeal_print.py`、`tests/conftest.py` fixtures、`uv add --dev pypdf`），並以 checkpoint 請使用者確認兩項決策：D-03 三聯版式差異（第二聯 vs 第三聯）與資料來源策略（join vs 留空）。

Purpose: 這是整條輸出通道的資料與版面正確性根基——官方欄位對應錯一欄，後續 PDF 逐欄核對（Success Criteria 1）必然失敗。
Output: `field_mapping.py`（`build_rows`/`build_header`/`paginate` 純函式）、`odt_fill.py`（`fill_template`/`verify_template_hash`）、Wave 0 測試脚手架、D-03 與資料來源策略之使用者確認。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-paper-appeal-print/11-CONTEXT.md
@.planning/phases/11-paper-appeal-print/11-RESEARCH.md
@.planning/phases/11-paper-appeal-print/11-PATTERNS.md
@.planning/phases/11-paper-appeal-print/11-VALIDATION.md
@src/elc_audit_engine/generators/appeal.py
@src/elc_audit_engine/generators/appeal_xml.py
@src/elc_audit_engine/parsers/models.py
@src/elc_audit_engine/case_store/store.py
@src/elc_audit_engine/safe_paths.py
@config/settings.py
@tests/conftest.py
@tests/test_appeal.py

<interfaces>
<!-- 關鍵契約（自 codebase 提取，executor 直接使用，不需再探索） -->

From src/elc_audit_engine/parsers/models.py:
  DeductionRecord（D-14d 18 欄）:
    - case_class（欄5，對應申報 XML d1，join key） / case_seq（欄6，對應 d2，join key）
    - id_number（欄9，健保署已遮罩後 4 碼，PHI——不得重建完整字號）
    - order_seq（欄10）/ order_code（欄11）/ visit_date（欄7）/ fee_year_month（欄3）
    - submit_date（欄4）/ non_reimbursed_amount（欄1）/ split_amount（欄14）/ deduction_reason（欄16）
    - appeal_item_code/desc（欄17）/ institution_note（欄18）/ institution_code（欄2）
  SubmissionCase:
    - case_class(d1) / case_seq(d2) / clinic(d8 就醫科別) / primary_diagnosis(d19 ICD-10) / patient_name(d49)
    - **無 id_number**（已核實 models.py 69-113）——身份證字號不走 SubmissionCase
  OrderRecord:
    - total_qty(p10 總量) / points(p12 點數) / code(p4 醫令代碼) / seq(p13 醫令序)
    - **無 order_name**（已核實 models.py 26-65）——「內容」欄不得引用不存在的鍵

From src/elc_audit_engine/generators/appeal.py:
  AppealDraft（frozen dataclass，本 phase 唯一資料輸入來源）:
    - case_class / case_seq / order_seq / order_code / visit_date / fee_year_month
    - deduction_upper_bound / deduction_reason / is_appealing / has_attachment
    - sections（AppealSection 四段）/ reason1(p8) / reason2(p9) / p6_points / total_chars
  write_appeal(output_dir, case_seq, draft, *, file_stem=None) -> tuple[str, str]:
    stem = safe_filename(file_stem or (case_seq or "unknown"), "file_stem/case_seq")
  render_appeal_json(draft) -> str：appeal_{流水號}.json 契約（CLI 的輸入檔格式）
    - **已核實 appeal.py 480-515：該 JSON 不含 id_number/total_qty/points/姓名/傷病名稱/審查科別**
    - 含鍵：format/case_class/case_seq/order_seq/order_code/visit_date/fee_year_month/deduction_upper_bound/deduction_reason/is_appealing/p1_order_seq/p2_order_code/p3_change_seq/p4_rate/p5_quantity/p6_points/p7_attachment/p8_reason1/p9_reason2/sections/word_stats/validation_errors
    - 患者層欄位（身份證字號/姓名/傷病名稱/審查科別/數量/金額）一律由 submission dict 提供（見下方契約）

From src/elc_audit_engine/generators/appeal_xml.py:
  draft_json_to_appeal_xml_fields(appeal_json: dict) -> tuple[dict, list[dict]]：欄位映射純函式模式
  AppealXmlEncodingError(ValueError)：錯誤訊息只記欄位名/碼位，不記 PHI 全文
  _add_fields(parent, fields)：None/空值跳過不輸出標籤

From src/elc_audit_engine/safe_paths.py:
  safe_filename(value, field_name="identifier") -> str（校驗後拒絕，UnsafeIdentifierError）
  UnsafeIdentifierError(ValueError)

From src/elc_audit_engine/case_store/store.py:
  CaseStore.get(case_id) -> CaseRecord；CaseRecord.payload: dict | None（上傳案件 JSON 快照）
  server.py _to_appeal_case 中 payload 的 patient_name 實測為 None；_to_sampling_case 有 patient_name
  _to_appeal_case 另有 order_name（規則庫名稱，失敗 fallback 為 order_code）與 deduct_amount（=non_reimbursed_amount）——僅作 submission.orders[].name 的可用來源，不得當成既有契約硬依賴

From config/settings.py:
  PROJECT_ROOT / OUTPUT_DIR（data/output，已 gitignore）/ load_llama_config() 缺檔 fail-fast 模式
  FACILITY_CONFIG_PATH 尚未存在（11-03 新增）——field_mapping 僅接受 facility: dict，不 import settings

官方模板結構（已實測驗證，30396_1/30396_3 ODT content.xml 精確解析；以下為權威契約。註：先前 round-1 計畫版本曾以「13 鍵」表述 build_rows 契約、不涵蓋傷病名稱欄；現行權威契約為 row1 表頭 14 cells＝13 標題＋1 covered（傷病名稱欄），資料列每列 15 cells＝14 資料欄＋1 單價續列留空）：
  9 個 table（每聯 3 個）：table0=頭表(1 row,17 cells)、table1=主表(18 rows)、table2=說明表(4 rows)
  主表 row0=大標題列：cell[0]「第一聯醫療院所存查」(x2)、cell[1]「保險對象」(x2)、cell[2]「傷病名稱」(x1, number-rows-spanned=2 跨 row0-1)、cell[3]「申復」(x5)、cell[4]「中央健康保險署填列」(x5)
  主表 row1=欄位表頭 14 個 cell（13 標題＋1 covered）：cell[0]案件分類|cell[1]流水號|cell[2]身份證字號|cell[3]姓名|cell[4]covered（row0「傷病名稱」cell 跨行佔用）|cell[5]醫令序|cell[6]內容|cell[7]數量|cell[8]金額|cell[9]理由|cell[10]審核意見|cell[11]補付數量|cell[12]單價(number-columns-spanned=2)|cell[13]補付金額
  主表 row2~row16=15 個空資料列（每列 15 cells＝14 資料欄＋1 續列）：cell[0]案件分類|cell[1]流水號|cell[2]身份證字號|cell[3]姓名|cell[4]傷病名稱|cell[5]醫令序|cell[6]內容|cell[7]數量|cell[8]金額|cell[9]理由|cell[10]審核意見|cell[11]補付數量|cell[12]單價|cell[13]單價續列（無欄位，留空跳過）|cell[14]補付金額
  主表 row17=合計列（8 cells）：cell[0]「合計」(x4)、cell[1]「人次」(x3)、cell[2..5]空、cell[6]「補付金額」(x3)、cell[7]空
  頭表 cells：代號字碼|(值)|醫療院所名稱|(值)|審查科別|(值)|原申報類別|□送核□補報|原申報日期|年月日|...|年度|(值)|月份|(值)|頁數|(值)
  聯間以 text:soft-page-break 分隔（2 次）；content.xml 根元素約 21~22 個 xmlns 前綴需逐一註冊
  第二聯說明表（聯2說明 table）row1 含「核定|複核|初核|審查委員」4 個標題 cell（VERIFIED）；第一/三聯說明表無此欄

<!-- submission payload 契約（build_rows 的 submission 參數；自由 dict，非 SubmissionCase dataclass）
     患者層欄位唯一來源。render_appeal_json 與 CaseStore appeal payload 皆不保證提供下列鍵，
     build_rows 一律以「有鍵即填、無鍵留空＋warning」處理，不猜測補全（T-11-05/誠實降級）。 -->
submission dict 可含：
  - case_class / case_seq（join key，與 payload 核對；不核對時直接信任 payload）
  - id_number：遮罩後 4 碼字串，呼叫端自 DeductionRecord.id_number 傳入；缺席 → ""＋warning「身份證字號」，**禁止重建完整字號**
  - patient_name（d49 姓名）/ primary_diagnosis（d19 傷病名稱，寫入主表 row2~16 cell[4]）/ clinic（d8 審查科別）
  - orders: list[dict]，每項對應 OrderRecord 具名屬性：code(p4 醫令代碼)/total_qty(p10 數量)/points(p12 金額)/seq(p13 醫令序)，依 seq＝醫令序對應 build_rows 行；對應不到行 → 該行數量/金額留空＋warning

<!-- RESEARCH Open Questions 落地對照（Q1-Q7 recommendation 全數落地；RESEARCH.md 的 (RESOLVED) 標記由研究階段補上便於覆核） -->
  - Q1 第二聯核定欄差異 → 本 plan Task 3 checkpoint（D-03 以官方模板第二聯為準）
  - Q2 官方清單欄位缺口 → 本 plan Task 1（submission 契約＋誠實留空；傷病名稱欄已實測存在於主表，14 資料欄全數對應）
  - Q3 布局壓縮參數 → 11-02 Task 1（A1：參數未收斂，收斂失敗出口見 11-02）
  - Q4 原申報類別/日期 → 本 plan Task 1 build_header（□送核 預設、submit_date 主）
  - Q5 合計/說明位置 → 本 plan Task 2（合計列與說明表僅末頁）
  - Q6 列印入口 → 11-03 Task 2（CLI 先行）
  - Q7 config 格式 → 11-03 Task 1（facility.json＋FACILITY_CONFIG_PATH）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 測試脚手架＋欄位來源盤點＋field_mapping.py 純函式（build_rows/build_header/paginate）</name>
  <files>tests/conftest.py, tests/test_appeal_print.py, pyproject.toml, uv.lock, src/elc_audit_engine/generators/appeal_print/__init__.py, src/elc_audit_engine/generators/appeal_print/field_mapping.py</files>
  <read_first>
    - src/elc_audit_engine/generators/appeal_print/field_mapping.py（本任務產出；先建立再填內容）
    - src/elc_audit_engine/parsers/models.py（DeductionRecord/SubmissionCase/OrderRecord 欄位來源）
    - src/elc_audit_engine/generators/appeal.py（AppealDraft 欄位＋_fmt 誠實降級模式）
    - src/elc_audit_engine/generators/appeal_xml.py（draft_json_to_appeal_xml_fields 欄位映射模式）
    - src/elc_audit_engine/case_store/store.py（CaseRecord.payload 結構）
    - src/elc_audit_engine/safe_paths.py（safe_filename 語意）
    - tests/test_appeal.py（_record/_draft fixture 構造模式）
    - tests/conftest.py（現有 fixture 模式）
    - 11-RESEARCH.md（Open Q#2 欄位缺口、Open Q#4 原申報類別/日期、Validation Architecture -k mapping）
    - 11-PATTERNS.md（field_mapping 應用規則、conftest 應用規則）
  </read_first>
  <behavior>
    - Test 1（mapping）: build_rows 對完整資料來源（payload＋submission 層＋facility）產出每行 14 鍵 dict：案件分類←payload.case_class、流水號←payload.case_seq、身份證字號←submission.id_number（遮罩值照印；缺席→""＋warning）、姓名←submission.patient_name、傷病名稱←submission.primary_diagnosis（缺席→""＋warning）、醫令序←payload.order_seq（p1_order_seq）、內容←payload.order_code（p2_order_code；submission.orders 對應行有 name 則附註「code name」）、數量←submission.orders[對應行].total_qty（p10）、金額←submission.orders[對應行].points（p12）、理由←reason1/reason2 合併、審核意見/補付數量/單價/補付金額←空字串（健保署填）
    - Test 2（mapping）: submission 缺席時**身份證字號**/姓名/傷病名稱（primary_diagnosis）/審查科別/數量/金額為空字串且回傳 warnings（含欄位名），不拋錯、不猜測補全
    - Test 3（mapping）: paginate(16 行, per_page=15) 回傳 [15 行, 1 行]
    - Test 4（mapping）: 頭表欄組裝（代號字碼←facility.code、醫療院所名稱←facility.name、審查科別←clinic、原申報類別預設「□送核」可覆寫、原申報日期←submit_date 主/fee_year_month 備援、年度/月份←fee_year_month 拆解、頁數由分頁層填）
  </behavior>
  <action>
    Wave 0 脚手架（先做）：
    - 執行 `uv add --dev pypdf`（PDF 驗證測試用，RESEARCH Standard Stack；soffice/pdftotext 為系統套件不經 uv）
    - 在 tests/conftest.py 新增 fixture：`facility_config`（monkeypatch `settings.FACILITY_CONFIG_PATH` 指向 tmp_path 下的測試用 facility.json，比照 test_config.py 的 monkeypatch 缺檔測試模式）、`sample_appeal_draft()`（構造 AppealDraft 的共用 factory，比照 test_appeal.py `_draft(**kwargs)`；缺省值包含 case_class/case_seq/order_seq/order_code/visit_date/fee_year_month/reason1/reason2/p6_points）
    - 建立 tests/test_appeal_print.py：Module 級 `requires_soffice` skip 標記（比照 test_doc_converter.py lines 17-20，import soffice_is_functional from elc_audit_engine.rule_repository.docx_tree.doc_converter）；先寫 -k mapping 的四個 behavior 測試（先紅），-k odt 測試在 Task 2 補上；-k config/copies/security/e2e/cli 由 11-02/11-03 依序添加（本任務不建立）
    - 建立 package `src/elc_audit_engine/generators/appeal_print/__init__.py`（僅 docstring 註明「Phase 11 紙本申復清單列印；render/write 由 11-02 填充」；讓 field_mapping/odt_fill 可被 import）
    欄位來源盤點（實作前先以 grep 確認，結論寫進 field_mapping.py 模組 docstring）：
    - grep server.py `_to_appeal_case` 與 `_to_sampling_case` 確認 CaseStore appeal payload 的 patient_name 為 None（已由 research 證實）——決策：患者層欄位（身份證字號/姓名/傷病名稱/審查科別/數量/金額）的來源是「呼叫端另行提供的 submission dict」（同一案件經申報 XML 匯入時才有），join key 為 case_class(d1)+case_seq(d2)；提供即填、缺即留空＋warning，永不從 AppealDraft 猜測補全
    - 身份證字號：render_appeal_json 無 id_number、SubmissionCase 亦無（已核實 models.py）——來源為 submission dict 的 `id_number` 鍵（遮罩後 4 碼，呼叫端自 DeductionRecord.id_number 傳入）；缺席即留空＋warning，**禁止重建完整字號**（T-11-05）
    實作 field_mapping.py（純函式，無 I/O、不 import settings/不觸 config；案件層欄位一律由 payload/submission/facility 參數推導，不寫死在 config——D-05）：
    - `build_rows(payload: dict, facility: dict, *, submission: dict | None = None) -> tuple[list[dict], list[str]]`：逐行產出 14 鍵 dict（鍵名即官方資料欄順序：案件分類/流水號/身份證字號/姓名/傷病名稱/醫令序/內容/數量/金額/理由/審核意見/補付數量/單價/補付金額；傷病名稱←submission.primary_diagnosis，缺席→""＋warning；值一律 str 或 ""）；warnings 累積缺欄欄位名。payload 即 appeal_{流水號}.json 的內容（render_appeal_json 格式，含 p1_order_seq/p2_order_code/p8_reason1/p9_reason2 等鍵；**無 id_number/姓名/數量/金額鍵——患者層欄位一律來自 submission，不得引用不存在的鍵**）；submission 為自由 dict，可含 case_class/case_seq/id_number（遮罩後 4 碼）/patient_name/primary_diagnosis/clinic/orders（orders 為 list[dict]，每項含 code(p4)/total_qty(p10)/points(p12)/seq(p13)，依 seq＝醫令序對應行；金額欄=points、數量欄=total_qty，對應不到行即留空＋warning）
    - `build_header(facility: dict, payload: dict, submission: dict | None, *, report_type: str = "送核") -> dict`：頭表 7 欄（代號字碼/醫療院所名稱/審查科別/原申報類別/原申報日期/年度/月份）；原申報類別輸出「□送核」或「□補報」（參數覆寫，RESEARCH Open Q#4 預設送核）；原申報日期優先 submit_date（ISO→民國或西元年月日依模板空欄語意）、備援 fee_year_month；年度/月份由 fee_year_month（YYYYMM）拆解
    - `paginate(order_rows: list[dict], per_page: int = 15) -> list[list[dict]]`（RESEARCH A3：官方版式即 15 行/頁，壓縮後容量不變，固定不增）
    - 每個缺欄位（submission 缺席或鍵值 None/空）回傳 "" 並在 warnings 記錄欄位名；Reason 欄組裝採 p8/p9 非空段合併（reason1 為主、reason2 接續，均空則 ""）
  </action>
  <verify>
    <automated>uv run pytest tests/test_appeal_print.py -k mapping -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_appeal_print.py -k mapping -x` 全綠（4 個 behavior 測試通過）
    - `uv run pytest --collect-only tests/test_appeal_print.py` 收集成功且含 requires_soffice skip 標記
    - `uv run python -c "import pypdf"` 成功（dev dep 已安裝）
    - tests/conftest.py 含 facility_config 與 sample_appeal_draft 兩個 fixture
    - field_mapping.py 模組 docstring 記載欄位來源決策（patient_name=None 實測、join key d1+d2、id_number 經 submission.id_number 傳入且缺席留空、傷病名稱經 submission.primary_diagnosis、遮罩 id 不重建）
    - grep 驗證：field_mapping.py 無 `import settings`、無任何字串模板插值（無 `{{` 佔位）
    - `uv run pytest tests/ -k "not appeal_print" -q` 既有套件不回歸（pypdf 加入 dev deps 不影響既有 374 基線）
  </acceptance_criteria>
<done>uv run pytest tests/test_appeal_print.py -k mapping -x 全綠；conftest 含 facility_config/sample_appeal_draft；pypdf 已入 dev deps；field_mapping 提供 build_rows/build_header/paginate（submission 契約含 id_number/primary_diagnosis/total_qty/points），無 import settings、無字串插值</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: odt_fill.py——ET 文本節點注入 content.xml＋zip 重打包＋超行分頁（D-06）</name>
  <files>src/elc_audit_engine/generators/appeal_print/odt_fill.py, tests/test_appeal_print.py</files>
  <read_first>
    - src/elc_audit_engine/generators/appeal_print/odt_fill.py（本任務產出；先建立再填內容）
    - officialdocument/電子申復文件格式/30396_1_1050105-1門診診療費用申復清單.odt（模板結構：9 tables、主表 18 rows、soft-page-break×2、mimetype 首條目）
    - 11-RESEARCH.md（Code Examples 1/2：22 命名空間註冊、set_cell_text、mimetype ZIP_STORED；Pattern 3 分頁；Pitfall 2/3/4）
    - 11-PATTERNS.md（odt_fill 應用規則：ET 文本節點紀律、PHI 安全錯誤、分頁規則）
    - src/elc_audit_engine/generators/appeal_xml.py（ET 序列化預檢＋fail-fast 錯誤語意）
    - src/elc_audit_engine/generators/appeal_print/field_mapping.py（Task 1 產出：build_rows/paginate/build_header 的 dict 鍵）
  </read_first>
  <behavior>
    - Test 1（odt）: fill_template 注入後，以 zipfile+ET 解析產出 ODT 的 content.xml，斷言主表 row2 的 14 個資料欄 cell 文本（cell[0..12]＋cell[14]；cell[13] 單價續列留空）與傳入資料一致（含案件分類/流水號/身份證字號/姓名/傷病名稱/醫令序/內容）
    - Test 2（odt）: 16 行醫令 → 產出 ODT 含 2 組聯結構（聯數×2 的頭表＋主表），頁數欄文本依序「1/2」「2/2」，合計列與說明表僅出現在第 2 組（該聯末頁）；末頁合計列值：案件分類 cell[0]＝「合計」、數量欄 cell[2]＝16（該聯人次加總）、金額與補付數量/單價/補付金額 cell 為空
    - Test 3（odt）: 欄位值含 `&lt;script&gt;` 與 `&` → 產出 content.xml 可被 ET 解析且該 cell 文本等於原值（自動轉義，無 XML 損毀）
    - Test 4（odt）: 模板路徑不存在 → raise FileNotFoundError（訊息含欄位名/階段，不含 PHI 全文）
  </behavior>
  <action>
    建立 odt_fill.py（RESEARCH Code Example 1/2 為基準，無既有 analog）：
    - `verify_template_hash(template_path: str, expected_sha256: str | None) -> None`：expected 非 None 時以 hashlib.sha256 比對，不符即 raise ValueError（A5 模板竄改防線；11-02 接線 print_base.sha256，本任務先提供參數）
    - `fill_template(template_odt_path: str, header_fields: dict, pages: list[list[dict]], output_odt_path: str, *, expected_sha256: str | None = None) -> str`：
      - 解析前先 regex 從 `&lt;office:document-content` 開標籤抓全部 `xmlns:*` 前綴逐一 `ET.register_namespace`（含默認命名空間，RESEARCH Pitfall 4——22 個前綴全數註冊，禁止出現 ns0/ns1）
      - `set_cell_text(cell, value)`：找 cell 內 text:p，清理既有 text:span 殘留，以 `p.text = value` 寫入（**ET 自動轉義，嚴禁字串插值**，T-11-01）
      - 頭表（每聯 table0）：依 header_fields 的鍵（代號字碼/醫療院所名稱/審查科別/原申報類別/原申報日期/年度/月份）填入對應 cell（模板 cell 順序以實測為準：cells[1]←代號字碼、cells[3]←院所名稱、cells[5]←審查科別、cells[7]←原申報類別、cells[9]←原申報日期、年度/月份/頁數在後段 cell）
      - 主表（table1）：row2~row16 依 pages 的每頁 15 行逐 cell 填 14 欄（模板實測 cell 序＝cell[0]案件分類 cell[1]流水號 cell[2]身份證字號 cell[3]姓名 cell[4]傷病名稱 cell[5]醫令序 cell[6]內容 cell[7]數量 cell[8]金額 cell[9]理由 cell[10]審核意見 cell[11]補付數量 cell[12]單價 cell[13]單價續列(無欄位，留空跳過) cell[14]補付金額）；row17 合計列僅在該聯末頁（RESEARCH Q5）——**語義明訂**：cell[0]＝「合計」、數量欄 cell[2]＝該聯資料行數加總（人次）、金額與補付數量/單價/補付金額 cell 一律留空（健保署填，系統不算補付金額、不捏造），以官方 PDF 30396_4 合計列樣式為準；合計列與說明表同頁（row17/說明 table 只出現在該聯最後一頁）
      - 分頁（D-06，RESEARCH Pattern 3）：pages 長度 &gt;1 時，每聯複製「標題＋頭表＋主表表頭(row0/row1)」產生續頁組，以 text:soft-page-break 分隔；頁數欄（頭表最後兩個 cell，格式 `x/y`）依序遞增；**合計列(row17)與說明表只出現在該聯最後一頁**
      - zip 重打包：mimetype 必須第一個寫入且 compress_type=ZIP_STORED，其餘 ZIP_DEFLATED（RESEARCH Pitfall 3）；content.xml 以 `ET.tostring(root, encoding="UTF-8", xml_declaration=True)` 寫入；重打包前先 `ET.tostring` 預檢，不可序列化時 raise 自訂 `AppealPrintFillError(ValueError)`（類比 AppealXmlEncodingError：訊息含欄位名/階段，不含值全文，T-11-03）
      - 完成後回傳 output_odt_path
    - tests/test_appeal_print.py 補上 -k odt 的四個 behavior 測試（先紅後綠）；測試讀取官方 ODT 為模板來源（tmp_path 寫入產出），不觸 soffice；Test 2 須斷言合計列值（「合計」＋人次加總＋補付欄空，見 behavior）
  </action>
  <verify>
    <automated>uv run pytest tests/test_appeal_print.py -k odt -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_appeal_print.py -k odt -x` 全綠（4 個 behavior 測試通過）
    - 產出 ODT 可被 zipfile 正常開啟且 mimetype 為首條目、compress_type=0（測試斷言）
    - -k odt 測試斷言末頁合計列值（cell[0]＝「合計」、數量欄 cell[2]＝人次加總、金額與補付數量/單價/補付金額 cell 為空）
    - grep 驗證 odt_fill.py 無 `content.replace`／`f"..." % `／`+ "&lt;"` 等字串拼接 XML 的寫法（全部經 p.text 賦值）
    - `uv run pytest tests/test_appeal_print.py -k "mapping or odt" -x` 全綠（Task 1/2 合併）
  </acceptance_criteria>
<done>uv run pytest tests/test_appeal_print.py -k odt -x 全綠；產出 ODT mimetype 首條目 ZIP_STORED；末頁合計列值已斷言（「合計」＋人次＋補付欄空）；odt_fill 欄位值一律經 p.text 賦值（grep 無字串拼接 XML）；-k "mapping or odt" 合併全綠</done>
</task>

<task type="checkpoint:decision">
  <name>Task 3: checkpoint——D-03 三聯版式差異確認＋資料來源策略確認</name>
  <files>無（決策任務；決策結果記錄於 11-01-SUMMARY.md）</files>
  <read_first>
    - .planning/phases/11-paper-appeal-print/11-CONTEXT.md（D-03 原文：「第三聯多出中央健康保險署填列核定/複核/初核/審查委員欄」）
    - .planning/phases/11-paper-appeal-print/11-RESEARCH.md（Open Q#1 與 Open Q#2 的 VERIFIED 事實）
    - .planning/phases/11-paper-appeal-print/11-VALIDATION.md（Manual-Only Verifications 第 2 項）
    - .planning/ROADMAP.md（Phase 11 Success Criteria 3 原文）
  </read_first>
  <action>
    向使用者呈現兩項實測證據並請其裁示（checkpoint:decision，autonomous: false，**不得靜默自行決定**）：
    (a) **D-03 三聯版式差異**：官方模板實測（30396_1/30396_3 ODT 與 30396_2/30396_4 PDF 交叉比對）顯示「中央健康保險署填列」的「核定|複核|初核|審查委員」空白欄在**第二聯**（健保署存查聯）說明表 row1（本 session 已 VERIFIED：聯2說明 row1 為 4 個標題 cell，第一/三聯說明表無此欄）——與 CONTEXT D-03 敘述（第三聯）相反。請使用者確認：以官方模板實體（第二聯）為準，ROADMAP Success Criteria 3 與 REQ-paper-appeal-print 驗收標準 3 的措辭由 orchestrator 更新為「第二聯」；系統不填該欄（留空供健保署複核），三聯結構差異由模板本身承載，注入邏輯不改動說明表。
    (b) **資料來源策略**：CaseStore appeal 案件 payload 的 patient_name 實測為 None（server.py `_to_appeal_case`）；render_appeal_json 不含 id_number、SubmissionCase 亦無；DeductionRecord.id_number 為遮罩後 4 碼。推薦策略：身份證字號由 submission dict 的 id_number 鍵照印遮罩值（呼叫端自 DeductionRecord.id_number 傳入，缺席即留空＋warning）、姓名/傷病名稱/審查科別/數量/金額由呼叫端另供 submission dict（同一 case_class+case_seq join，來自申報 XML 匯入）填寫、join 不到誠實留空＋警告。請使用者確認此策略或指示替代方案。
    決策確認後：記錄確認結果於 11-01-SUMMARY.md（含使用者措辭）；若使用者要求調整 field_mapping/odt_fill 行為，於同 plan 內修復並重跑對應 -k 測試。
  </action>
  <verify>
    <automated>N/A（checkpoint:decision——以使用者確認為完成條件；決策記錄於 11-01-SUMMARY.md）</automated>
  </verify>
  <acceptance_criteria>
    - 使用者對 (a) D-03 差異（以第二聯為準）與 (b) 資料來源策略（submission join 或替代）皆已明確確認或指示
    - 11-01-SUMMARY.md 記載兩項決策與使用者措辭（含「以官方模板第二聯為準」或使用者替代指示）
    - 若指示變更，field_mapping/odt_fill 已按指示修正且 `uv run pytest tests/test_appeal_print.py -k "mapping or odt" -x` 仍全綠
  </acceptance_criteria>
<done>使用者對 (a) D-03 差異（以第二聯為準）與 (b) 資料來源策略皆明確確認；11-01-SUMMARY.md 記錄裁示；若指示變更已修正且 -k "mapping or odt" 仍全綠</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 外部輸入 → field_mapping | appeal JSON／case payload／submission 資料（來自核減 CSV 衍生資料、申報 XML、CaseStore）為不可信輸入，含自由中文與 PHI |
| 模板資產 → odt_fill | 官方 ODT／基準模板為 git 版控資產（供應鏈）；竄改會影響版面與資料注入正確性 |
| 產出 ODT/PDF → 檔案系統 | 產出含 PHI（姓名/身分證字號/病歷內容），經 data/output（已 gitignore）落盤 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | odt_fill.fill_template | mitigate | 全部欄位值經 `p.text = value`（ET 文本節點，自動轉義）；嚴禁字串插值；重打包前 `ET.tostring` 預檢＋錯誤 raise（Task 2 實作） |
| T-11-02 | Information Disclosure | 產出 ODT/PDF | mitigate | 產出一律經 write_appeal_print 寫入 data/output/*（.gitignore 已含，`!data/output/.gitkeep`）；測試產出走 pytest tmp_path（Task 2 測試即用 tmp_path） |
| T-11-03 | Information Disclosure | odt_fill 錯誤處理 | mitigate | 自訂 AppealPrintFillError 訊息只記欄位名/失敗階段/碼位，不記欄位值全文（比照 AppealXmlEncodingError，ASVS V9） |
| T-11-04 | Tampering/Elevation | 檔名 | mitigate | 11-02 write_appeal_print 沿用 safe_filename 校驗後拒絕（P1-3）；本 plan 的 fill_template 不直接接受檔名 |
| T-11-05 | Integrity | 身份證字號 | mitigate | 遮罩後 4 碼照印或留空，禁止猜測補全完整字號（field_mapping Task 1 實作：來源＝submission.id_number，缺席留空＋warning；行為測試 Test 2） |
| T-11-06 | Tampering | 模板資產 | mitigate | verify_template_hash 提供 sha256 比對（A5）；11-02 以 print_base.sha256 接線；官方模板已 git-tracked |
| T-11-07 | Integrity | 輸出檔覆寫 | accept | 一案一檔名 `申復清單_{stem}.pdf`；11-02 write_appeal_print 沿用 write_appeal 的 file_stem 機制，同案多筆醫令以 `{case_seq}_{order_seq}` 避免覆寫；現階段 CLI 單案輸出，低風險 |
</threat_model>

<verification>
- Task 1: `uv run pytest tests/test_appeal_print.py -k mapping -x`
- Task 2: `uv run pytest tests/test_appeal_print.py -k odt -x`
- Wave 收束: `uv run pytest tests/test_appeal_print.py -k "mapping or odt" -x`
- 既有套件不回歸: `uv run pytest tests/ -k "not appeal_print" -q`
</verification>

<success_criteria>
- field_mapping.py 提供 build_rows/build_header/paginate 純函式，官方 14 主表資料欄（含傷病名稱欄）＋7 頭表欄逐欄對應完成，缺欄誠實降級（warnings 累積、永不捏造；身份證字號來源＝submission.id_number、傷病名稱來源＝submission.primary_diagnosis）
- odt_fill.py 提供 fill_template/verify_template_hash：ET 文本節點注入、22 命名空間註冊、mimetype 首條目 ZIP_STORED、>15 行自動分頁（D-06）、合計/說明僅末頁（合計列語義＝「合計」＋人次加總＋補付欄留空）、PHI 安全錯誤
- tests/test_appeal_print.py 的 -k mapping 與 -k odt 全綠；conftest 提供 facility_config/sample_appeal_draft；pypdf 入 dev deps
- checkpoint:decision 完成——D-03 差異（第二聯 vs 第三聯）與資料來源策略經使用者確認
</success_criteria>

<output>
After completion, create `.planning/phases/11-paper-appeal-print/11-01-SUMMARY.md`
（含 D-03 差異確認與資料來源策略的使用者裁示記錄）
</output>
