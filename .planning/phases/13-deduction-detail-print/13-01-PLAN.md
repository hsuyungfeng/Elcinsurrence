---
wave: 1
depends_on: []
files_modified:
  - tests/test_deduction_print.py
  - src/elc_audit_engine/generators/deduction_print/template.py
  - src/elc_audit_engine/generators/deduction_print/field_mapping.py
  - officialdocument/電子申復文件格式/RCPI2012R01_核減明細表_print_base.odt
  - officialdocument/電子申復文件格式/RCPI2012R01_核減明細表_print_base.sha256
autonomous: true
---

# Phase 13-01: Template & Field Mapping

## Goal

Establish the testing scaffold and implement the core mapping logic for the deduction detail print layout, along with template loading and verification.

<threat_model>
- **T-13-02**: PHI 敏感個資與身分證號遮罩 (ID Number Masking Protection) - Addressed in field mapping by hardcoding mask for `id_number`.
</threat_model>

## Tasks

<task>
  <id>13-01-01</id>
  <description>建立測試骨架與 ODT 基準模板</description>
  <read_first>
    - .planning/phases/13-deduction-detail-print/13-RESEARCH.md
    - .planning/phases/13-deduction-detail-print/13-PATTERNS.md
    - tests/test_appeal_print.py
    - officialdocument/電子申復文件格式/30396_*_print_base.sha256
  </read_first>
  <action>
    - 建立 `tests/test_deduction_print.py`，加入 Wave 0 測試骨架 (包含 mapping, odt, e2e, security marker 的空測試或跳過測試)。
    - 準備 `officialdocument/電子申復文件格式/RCPI2012R01_核減明細表_print_base.odt` 空檔案 (實際將由專人或指令替換為正確範本)，並產生對應之 `RCPI2012R01_核減明細表_print_base.sha256`。
    - 實作 `src/elc_audit_engine/generators/deduction_print/template.py`，提供 `verify_template_hash(template_path, expected_sha256)` 與 `_load_expected_sha256(template_path)`。
  </action>
  <acceptance_criteria>
    - `rtk uv run pytest tests/test_deduction_print.py` 執行可見測試存在並可 run (即使 skipped)。
    - `template.py` 提供的方法能正確驗證現有 `RCPI2012R01_核減明細表_print_base.odt` 的 sha256 雜湊。
  </acceptance_criteria>
  <requirements>REQ-deduction-print</requirements>
</task>

<task>
  <id>13-01-02</id>
  <description>實作欄位映射邏輯與降級警告</description>
  <read_first>
    - src/elc_audit_engine/generators/appeal_print/field_mapping.py
    - src/elc_audit_engine/parsers/models.py
  </read_first>
  <action>
    - 實作 `src/elc_audit_engine/generators/deduction_print/field_mapping.py`，匯出 `build_deduction_header` 與 `build_deduction_rows`。
    - 針對 `DeductionRecord` 進行對應轉換，需處理 `id_number` (強制遮罩，符合 T-13-02)、ROC 格式日期、並對缺失欄位(如 `patient_name`, `order_name`)加入 warnings 記錄而非拋錯。
    - 補齊 `tests/test_deduction_print.py` 中 `-k mapping` 的單元測試。
  </action>
  <acceptance_criteria>
    - 測試斷言 `build_deduction_header` 與 `build_deduction_rows` 能正確轉換資料。
    - 測試斷言身分證號 (T-13-02) 輸出包含 `****`，且缺失 `patient_name` 時傳回正確的警告清單。
    - `rtk uv run pytest tests/test_deduction_print.py -k mapping` 測試通過。
  </acceptance_criteria>
  <requirements>REQ-deduction-print</requirements>
</task>

## Verification

- [ ] All unit tests passing: `rtk uv run pytest tests/test_deduction_print.py -k mapping`

## must_haves

- `build_deduction_header` and `build_deduction_rows` correctly process `DeductionRecord` dictionary to string dictionary representations.
- Masking logic properly applied.
