---
wave: 3
depends_on: [13-02]
files_modified:
  - scripts/build_deduction_print.py
  - server.py
  - tests/test_deduction_print.py
autonomous: true
---

# Phase 13-03: CLI & API Servicing

## Goal

Provide user-facing entry points (CLI script and API endpoint) to trigger the generation of deduction details in the original paper format.

<threat_model>
- API implementation adheres to existing audit-log and authentication paradigms.
</threat_model>

## Tasks

<task>
  <id>13-03-01</id>
  <description>建立 CLI 工具</description>
  <read_first>
    - scripts/build_appeal_print.py
    - scripts/build_deduction_print.py
  </read_first>
  <action>
    - 建立 `scripts/build_deduction_print.py` 提供開發與批次執行介面。
    - 接受參數以輸入核減明細 (支援 CSV/JSON 檔案路徑或者 case-id 獲取)。
    - 將呼叫 `write_deduction_print` 生成之檔案路徑列印於 stdout，警告記錄列印於 stderr。
  </action>
  <acceptance_criteria>
    - 使用 `-h` 參數可查看說明文件 `rtk uv run python scripts/build_deduction_print.py -h`。
    - 實際餵入合法樣本能產出 PDF，並顯示正確之控制台輸出。
  </acceptance_criteria>
  <requirements>REQ-deduction-print</requirements>
</task>

<task>
  <id>13-03-02</id>
  <description>實作 API 端點並接入 Audit Log</description>
  <read_first>
    - server.py
  </read_first>
  <action>
    - 於 `server.py` 實作 `POST /api/deduction/print`。
    - 從 payload 或 `CaseStore` 提取核減資料，呼叫 `write_deduction_print`。
    - 確保此端點在需要時有整合進入 Audit Log 追蹤 (紀錄呼叫方存取事件，不記錄 PHI)。
    - 回傳格式須包含 `{ "pdf_url": ..., "warnings": [...] }` 等欄位。
    - 補齊 `tests/test_deduction_print.py` 針對 Flask app `POST /api/deduction/print` 端點之整合測試。
  </action>
  <acceptance_criteria>
    - 使用 `pytest` flask client 向 `/api/deduction/print` 發送合法請求應獲 200 或 201 狀態碼，回傳含有 `pdf_url`。
    - Audit log 有記錄此端點的存取事件。
  </acceptance_criteria>
  <requirements>REQ-deduction-print</requirements>
</task>

## Verification

- [ ] Full suite passes: `rtk uv run pytest`

## must_haves

- Command-line tool is operational and self-documenting.
- API is robust, logs correctly, and conforms to expected JSON response format.
