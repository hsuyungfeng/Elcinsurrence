---
phase: 02-rule-repository
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_rule_repository_models.py
  - tests/test_rule_repository_sqlite.py
  - tests/test_docx_tree_coverage.py
  - tests/test_rule_mapping_spotcheck.py
  - tests/test_rule_repository_interface.py
  - tests/fixtures/rule_mapping_20_spotcheck.json
  - src/elc_audit_engine/rule_repository/models.py
autonomous: true
requirements: [REQ-rule-repository]

must_haves:
  truths:
    - "Test suite exists and fails meaningfully (ImportError, not syntax error) before implementation begins"
    - "The 20-code acceptance list is finalized with verified-present codes (no 01015C)"
    - "RuleResult contract is defined so all later plans implement against the same shape"
  artifacts:
    - path: "src/elc_audit_engine/rule_repository/models.py"
      provides: "RuleResult dataclass — the D-07 single-query-function return type"
      contains: "class RuleResult"
    - path: "tests/fixtures/rule_mapping_20_spotcheck.json"
      provides: "20-code candidate list with source, code, name, and empty article_location/article_full_text slots for later human sign-off"
    - path: "tests/test_rule_repository_sqlite.py"
      provides: "SQLite payment_rules/drug_rules queryability tests"
    - path: "tests/test_docx_tree_coverage.py"
      provides: "Full-corpus docx tree coverage test (34 files: 23 docx + 11 doc-to-docx)"
    - path: "tests/test_rule_mapping_spotcheck.py"
      provides: "20-code spot-check regression test against the fixture"
    - path: "tests/test_rule_repository_interface.py"
      provides: "get_rule() single-entry-point contract test"
  key_links:
    - from: "tests/test_rule_repository_interface.py"
      to: "src/elc_audit_engine/rule_repository/models.py"
      via: "from elc_audit_engine.rule_repository.models import RuleResult"
      pattern: "RuleResult"
---

<objective>
Establish Wave 0 test scaffolding (Nyquist requirement — every task in this phase needs an `<automated>` verify target) and lock the `RuleResult` interface contract that Plans 02-05 implement against. Also finalize and write the 20-code human spot-check candidate list as a fixture file with empty verification slots, replacing the invalid `01015C` reference from CONTEXT.md.

Purpose: Downstream plans (02, 03, 04, 05) must not each invent their own test files or result-type shape — this plan hands them pre-written failing tests and a locked dataclass contract, preventing interface drift and rework.
Output: 1 models test file (green), 4 integration/interface test files (initially red — expected ImportError until later waves land), 1 fixture JSON (20-code candidate list, unfilled), 1 models.py with RuleResult dataclass, tests/conftest.py extended with a temp-DB fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-rule-repository/02-CONTEXT.md
@.planning/phases/02-rule-repository/02-RESEARCH.md
@.planning/phases/02-rule-repository/02-VALIDATION.md
@config/settings.py
@src/elc_audit_engine/rule_repository/__init__.py
@tests/conftest.py
</context>

<interfaces>
<!-- Existing Phase 1 conventions the executor must replicate -->

From config/settings.py (already built, Phase 1):
```python
DATA_DIR, DB_DIR, RAG_DIR, OUTPUT_DIR, RULE_SOURCE_DIR  # str paths, env-overridable
LLAMA_CPP_BASE_URL  # "http://localhost:8080"
LLAMA_CONFIG_PATH
def load_llama_config() -> dict  # raises FileNotFoundError if missing
```

From tests/conftest.py (Phase 1, to be extended not replaced):
```python
import sys, os
PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), ".."))
if PROJECT_ROOT not in sys.path:
    sys.path.insert(0, PROJECT_ROOT)
```

Verified CSV facts to encode into the fixture (from 02-RESEARCH.md, independently re-verified this session):
- Payment CSV (`診療項目代碼` PK): 2,669 rows. Header: `診療項目代碼,健保支付點數,生效起日,生效迄日,英文項目名稱,中文項目名稱,支付規定`
- Drug CSV (`藥品代號` PK): 11,273 rows. Header includes `藥品代號,藥品中文名稱,給付規定,有效起日,有效迄日` among 22 cols.
- `01015C` does NOT exist in either CSV — confirmed again this session. Must not appear in the fixture.
- Actual filenames (verify via glob, do not hardcode elsewhere): `醫療服務給付項目251027準確板_已優化填入支付規定.csv` (payment), `藥品項查詢項目檔260605 AI  摘要支付價大於0.csv` (drug — note double space before "摘要").
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define RuleResult contract + finalize 20-code fixture</name>
  <files>src/elc_audit_engine/rule_repository/models.py, tests/test_rule_repository_models.py, tests/fixtures/rule_mapping_20_spotcheck.json</files>
  <read_first>
    src/elc_audit_engine/rule_repository/__init__.py
    .planning/phases/02-rule-repository/02-CONTEXT.md
    .planning/phases/02-rule-repository/02-RESEARCH.md
  </read_first>
  <action>
    Create `src/elc_audit_engine/rule_repository/models.py` with a frozen `@dataclass` named `RuleResult` implementing D-07/D-08. Fields: `code: str`, `source: str` (one of `"payment"`, `"drug"`, `"unknown"`), `name: str` (中文名稱), `payment_text: str | None` (支付規定/給付規定 raw text from SQLite), `effective_from: str | None` (ISO `YYYY-MM-DD`), `effective_to: str | None` (ISO `YYYY-MM-DD`), `article_location: str | None` (docx tree path e.g. `"西醫基層-內科 > 壹、一般原則 > 三"`), `article_full_text: str | None`, `article_source: str | None` (one of `"docx"`, `"csv"`, `None` — per Open Question #3 in RESEARCH.md, a code's own `支付規定`/`給付規定` cell can itself serve as `article_full_text` with `article_source="csv"` when it already contains substantive review-guideline text, avoiding a forced docx-tree lookup), `found: bool`. Add a module-level factory `def not_found(code: str) -> RuleResult` returning a `RuleResult` with `source="unknown"`, `name=""`, all optional fields `None`, `found=False`.

    Finalize the 20-code human spot-check list per D-06, replacing `01015C` (verified absent from both CSVs). Use this exact final list of 20 codes, verified present in the CSVs this session with substantive non-null `支付規定`/`給付規定` text, spanning categories (檢查/處置/治療/手術/預防保健/藥品): payment codes `64140C` (手術/甲床與手指重建術), `06012C` (檢查/尿一般檢查), `06013C` (檢查/尿生化檢查), `05316C` (處置/PCA病患自控式止痛), `05401C` (治療/精神復健日間型), `05402C` (治療/精神復健住宿型全日), `05404C` (治療/居家治療醫師診治費), `05406C` (治療/居家治療其他專業人員處置費), `14033C` (檢查/B型肝炎表面抗體), `15001C` (檢查/體液細胞檢查), `16002C` (檢查/腹水分析), `19001C` (檢查/腹部超音波), `24009C` (檢查/口服葡萄糖耐量試驗), `29001C` (處置/硬腦膜下穿刺), `31` (預防保健/子宮頸抹片採樣), `32001C` (檢查/胸腔檢查), `33` (預防保健/子宮頸細胞病理檢驗); plus 3 drug codes `AC10398100`（"華琳"敏利靜糖衣錠50毫克）, `A046933100`（富必靜膜衣錠50公絲）, `AB49742100`（清樂果膜衣錠200毫克）.

    Write `tests/fixtures/rule_mapping_20_spotcheck.json` as a JSON array of exactly 20 objects, each with keys: `code`, `source` (`"payment"` or `"drug"`), `name`, `category_hint` (e.g. `"手術"`, `"檢查"`, `"藥品"` — free text aiding human review), `article_location` (set to `null` — filled by human during Plan 05's checkpoint), `article_full_text` (set to `null` — filled by human during Plan 05's checkpoint), `verified` (set to `false` for all 20 entries — flipped to `true` only after human sign-off in Plan 05). This file is the regression fixture Plan 05's spot-check test asserts against once populated; in this task it exists only as the unfilled candidate list.

    Create `tests/test_rule_repository_models.py` with the RED->GREEN tests described below (this file must pass immediately since `models.py` is created in the same task — no cross-plan dependency).
  </action>
  <behavior>
    - Test 1: constructing `RuleResult(code="64140C", source="payment", name="甲床與手指重建術", payment_text="...", effective_from=None, effective_to=None, article_location=None, article_full_text=None, article_source=None, found=True)` succeeds and `.found is True`
    - Test 2: `not_found("XXXX").found is False` and `not_found("XXXX").code == "XXXX"`
    - Test 3: `RuleResult` is a frozen dataclass — mutating an attribute after construction raises `dataclasses.FrozenInstanceError`
  </behavior>
  <acceptance_criteria>
    - `src/elc_audit_engine/rule_repository/models.py` exists and defines `class RuleResult` decorated with `@dataclass(frozen=True)`
    - `not_found` factory function exists at module level and returns `found=False`
    - `tests/fixtures/rule_mapping_20_spotcheck.json` is valid JSON (`uv run python -c "import json; d=json.load(open('tests/fixtures/rule_mapping_20_spotcheck.json')); assert len(d)==20"` exits 0)
    - `01015C` does not appear anywhere in the fixture: `grep -c '01015C' tests/fixtures/rule_mapping_20_spotcheck.json` returns 0 (grep with no match exits 1 with count 0 — acceptable, confirms absence)
    - Every code in the fixture verified present in its source CSV via: `uv run python -c "import json,csv; data=json.load(open('tests/fixtures/rule_mapping_20_spotcheck.json')); pay=set(r['診療項目代碼'] for r in csv.DictReader(open('officialdocument/審查注意事項/醫療服務給付項目251027準確板_已優化填入支付規定.csv', encoding='utf-8-sig'))); drug=set(r['藥品代號'] for r in csv.DictReader(open('officialdocument/審查注意事項/藥品項查詢項目檔260605 AI  摘要支付價大於0.csv', encoding='utf-8-sig'))); missing=[d['code'] for d in data if d['code'] not in pay and d['code'] not in drug]; assert not missing, missing; print('all 20 present')"` prints `all 20 present`
    - `uv run pytest tests/test_rule_repository_models.py -v` shows 3 passed
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_rule_repository_models.py -x</automated>
  </verify>
  <done>models.py defines RuleResult + not_found (both green in tests/test_rule_repository_models.py); fixture has exactly 20 verified-present codes, no 01015C</done>
</task>

<task type="auto">
  <name>Task 2: Write Wave 0 failing test scaffolding for SQLite, docx coverage, spot-check, and interface</name>
  <files>tests/conftest.py, tests/test_rule_repository_sqlite.py, tests/test_docx_tree_coverage.py, tests/test_rule_mapping_spotcheck.py, tests/test_rule_repository_interface.py</files>
  <read_first>
    tests/conftest.py
    .planning/phases/02-rule-repository/02-VALIDATION.md
    src/elc_audit_engine/rule_repository/models.py
  </read_first>
  <action>
    Extend `tests/conftest.py` (do not replace existing content) with a pytest fixture `tmp_rule_db_path(tmp_path)` that returns a `str` path to `tmp_path / "test_rules.sqlite3"` (does not create the file — Plan 02's `db.py` does that), so all later test files never write to `data/db/rules.sqlite3` during test runs.

    Create `tests/test_rule_repository_sqlite.py` importing `from elc_audit_engine.rule_repository import db, loaders` (not yet implemented — this file MUST fail with `ImportError`/`ModuleNotFoundError` until Plan 02 lands; this is the expected Wave 0 red state, not a bug in this task). Write 4 test functions: `test_payment_rules_table_created_with_core_columns` (asserts table `payment_rules` has columns `code, name, payment_text, effective_from, effective_to`), `test_drug_rules_table_created_with_core_columns` (same shape for `drug_rules`), `test_query_known_payment_code_64140C_returns_row`, `test_query_known_drug_code_AC10398100_returns_row`. Use the `tmp_rule_db_path` fixture and real CSV paths resolved via `config.settings.RULE_SOURCE_DIR` + glob (not hardcoded absolute paths), calling `loaders.load_payment_csv(db_path, csv_path)` and `loaders.load_drug_csv(db_path, csv_path)`.

    Create `tests/test_docx_tree_coverage.py` importing `from elc_audit_engine.rule_repository.docx_tree import tree_builder` (not yet implemented — expected red). Write 2 test functions: `test_all_source_files_converted_and_processed` asserting the count of files returned by `tree_builder.build_all_trees(source_dir, staging_dir)` equals the count of `*.doc` + `*.docx` files glob'd from `config.settings.RULE_SOURCE_DIR` (34 total: 11 `.doc` + 23 `.docx`); `test_flat_structure_doc_produces_nested_tree` asserting that running the extractor against `2-2-7第二部第二章第七節手術-113.12.01.docx` (100% `Normal` style, verified this session — zero custom heading styles) produces a tree with at least 3 distinct depth levels (not a flat 1-level tree), proving the regex hierarchy parser — not style-only detection — is active.

    Create `tests/test_rule_mapping_spotcheck.py` importing only `json` (no rule_repository import needed, so this file does NOT ImportError at Wave 0) and asserting: `test_fixture_has_20_entries` loads `tests/fixtures/rule_mapping_20_spotcheck.json` and asserts `len(data) == 20`; `test_fixture_all_entries_verified` asserts `all(entry["verified"] for entry in data)` — this assertion MUST fail at Wave 0 (all `verified` are `false` from Task 1) and MUST continue failing through Plans 02-04 until Plan 05's human checkpoint flips the fixture to all-verified.

    Create `tests/test_rule_repository_interface.py` importing `from elc_audit_engine.rule_repository import get_rule` and `from elc_audit_engine.rule_repository.models import RuleResult` (get_rule not yet implemented — expected red, lands in Plan 05). Write 3 test functions: `test_get_rule_known_code_returns_found_result` (`get_rule("64140C").found is True`), `test_get_rule_unknown_code_returns_not_found_not_exception` (`get_rule("ZZZZZZ99").found is False` — must not raise), `test_get_rule_return_type_is_ruleresult` (`isinstance(get_rule("64140C"), RuleResult)`).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_rule_repository_sqlite.py -x 2>&1 | grep -E "ModuleNotFoundError|ImportError"` matches (expected Wave 0 red, not a syntax/collection error)
    - `uv run pytest tests/test_docx_tree_coverage.py -x 2>&1 | grep -E "ModuleNotFoundError|ImportError"` matches
    - `uv run pytest tests/test_rule_mapping_spotcheck.py -x` collects and runs without ImportError, but `test_fixture_all_entries_verified` fails (exit code 1, not a collection error) — proves fixture wiring is correct while awaiting human sign-off
    - `uv run pytest tests/test_rule_repository_interface.py -x 2>&1 | grep -E "ModuleNotFoundError|ImportError"` matches
    - `grep -c "def tmp_rule_db_path" tests/conftest.py` returns 1
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_rule_repository_sqlite.py tests/test_docx_tree_coverage.py tests/test_rule_mapping_spotcheck.py tests/test_rule_repository_interface.py 2>&1 | tail -40</automated>
  </verify>
  <done>4 test files exist and collect (no syntax errors); 3 fail with ImportError (expected until Plans 02/03/05 land); spot-check test fails on the verified-flag assertion (expected until Plan 05); conftest.py has tmp_rule_db_path fixture</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test fixture data -> pytest | Fixture JSON is developer-authored, not user/network input — no external trust boundary crossed in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | tests/fixtures/rule_mapping_20_spotcheck.json | accept | Static, developer-authored, git-tracked fixture file; no runtime write path from untrusted input in this plan |
</threat_model>

<verification>
Run `uv run pytest tests/test_rule_repository_models.py -v` — expect 3 passed.
Run `uv run pytest tests/test_rule_repository_sqlite.py tests/test_docx_tree_coverage.py tests/test_rule_repository_interface.py -v 2>&1 | grep -c "ModuleNotFoundError\|ImportError"` — expect >= 3 (each file's collection error counted).
Run `uv run pytest tests/test_rule_mapping_spotcheck.py -v` — expect `test_fixture_has_20_entries` PASSED, `test_fixture_all_entries_verified` FAILED.
</verification>

<success_criteria>
- RuleResult dataclass defined and 3/3 model tests green
- 20-code fixture written with verified-present codes, 01015C excluded, all entries `verified: false`
- 4 downstream test files exist, collect without syntax errors, and fail for the *expected* reason (missing implementation, not a broken test)
- tests/conftest.py extended with tmp_rule_db_path fixture without breaking existing Phase 1 tests (`uv run pytest tests/test_config.py -v` still passes)
</success_criteria>

<output>
After completion, create `.planning/phases/02-rule-repository/02-01-SUMMARY.md`
</output>
