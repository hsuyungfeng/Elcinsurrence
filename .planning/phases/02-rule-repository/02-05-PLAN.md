---
phase: 02-rule-repository
plan: 05
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - src/elc_audit_engine/rule_repository/__init__.py
  - tests/fixtures/rule_mapping_20_spotcheck.json
autonomous: false
requirements: [REQ-rule-repository]

must_haves:
  truths:
    - "get_rule(code) is the single public entry point for downstream phases (D-07/D-08), abstracting SQLite/rule_mapping internals"
    - "Unknown codes return a RuleResult with found=False, never raise an exception"
    - "The 20-code human spot-check is completed and the fixture is flipped to verified=true for all entries, unblocking REQ-rule-repository's third acceptance criterion"
    - "get_rule() performs zero LLM calls (D-05 — query-time is cache-only)"
  artifacts:
    - path: "src/elc_audit_engine/rule_repository/__init__.py"
      provides: "Public get_rule(code) single query function per D-07/D-08"
      exports: ["get_rule"]
  key_links:
    - from: "src/elc_audit_engine/rule_repository/__init__.py"
      to: "src/elc_audit_engine/rule_repository/db.py"
      via: "query_by_code against payment_rules, drug_rules, rule_mapping"
      pattern: "query_by_code"
    - from: "src/elc_audit_engine/rule_repository/__init__.py"
      to: "src/elc_audit_engine/rule_repository/models.py"
      via: "returns RuleResult / not_found"
      pattern: "RuleResult"
</execution_context>

<objective>
Implement the D-07/D-08 single public query function `get_rule(code)` that internally combines `payment_rules`/`drug_rules` (Plan 02) and `rule_mapping` (Plan 04) lookups into one `RuleResult`, with zero LLM calls at query time. Then run the mandatory human 20-code spot-check (REQ-rule-repository's third acceptance criterion) as a checkpoint: present each of the 20 fixture codes' proposed `article_location`/`article_full_text` from the real `rule_mapping` table for user verification against source documents, and lock the fixture as a permanent regression test once confirmed.

Purpose: Completes REQ-rule-repository — this is the phase's final integration point. Downstream Phase 3-5 callers use only `get_rule(code)`; they never need to know whether a result came from CSV reuse or docx-tree LLM matching.
Output: `src/elc_audit_engine/rule_repository/__init__.py` with `get_rule(code) -> RuleResult` (replacing the current empty stub), all 4 Wave-0 test files green (`test_rule_repository_sqlite.py`, `test_docx_tree_coverage.py`, `test_rule_mapping_spotcheck.py`, `test_rule_repository_interface.py`), and `tests/fixtures/rule_mapping_20_spotcheck.json` with all 20 entries human-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-rule-repository/02-CONTEXT.md
@.planning/phases/02-rule-repository/02-RESEARCH.md
@tests/test_rule_repository_interface.py
@tests/test_rule_mapping_spotcheck.py
@tests/fixtures/rule_mapping_20_spotcheck.json
</context>

<interfaces>
<!-- Contracts from Plans 01, 02, 04 that this plan integrates -->

From Plan 01's `src/elc_audit_engine/rule_repository/models.py`:
```python
@dataclass(frozen=True)
class RuleResult:
    code: str; source: str; name: str; payment_text: str | None
    effective_from: str | None; effective_to: str | None
    article_location: str | None; article_full_text: str | None; article_source: str | None
    found: bool
def not_found(code: str) -> RuleResult
```

From Plan 02's `src/elc_audit_engine/rule_repository/db.py`:
```python
def get_connection(db_path: str) -> sqlite3.Connection
def query_by_code(conn, table: str, code: str) -> sqlite3.Row | None  # table in {"payment_rules","drug_rules","rule_mapping"}
```
(Note: Plan 04 added `rule_mapping` schema to `db.py` — `query_by_code`'s allowlist must already include it; if Plan 04 did not add it to the allowlist, this plan must add `"rule_mapping"` to the allowed table set.)

`tests/test_rule_repository_interface.py` (Plan 01, currently red) expects:
```python
from elc_audit_engine.rule_repository import get_rule
from elc_audit_engine.rule_repository.models import RuleResult
get_rule("64140C").found is True
get_rule("ZZZZZZ99").found is False  # must not raise
isinstance(get_rule("64140C"), RuleResult)
```

`tests/fixtures/rule_mapping_20_spotcheck.json` (Plan 01) currently has 20 entries with `article_location: null`, `article_full_text: null`, `verified: false` — this plan's checkpoint task fills these from the real `rule_mapping` table and flips `verified` to `true` after human confirmation.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement get_rule() single query interface</name>
  <files>src/elc_audit_engine/rule_repository/__init__.py</files>
  <read_first>
    src/elc_audit_engine/rule_repository/__init__.py
    src/elc_audit_engine/rule_repository/db.py
    src/elc_audit_engine/rule_repository/models.py
    tests/test_rule_repository_interface.py
  </read_first>
  <action>
    Replace the current empty-stub content of `src/elc_audit_engine/rule_repository/__init__.py` (currently just a docstring) with the D-07/D-08 public interface. Implement `def get_rule(code: str, db_path: str | None = None) -> RuleResult` (default `db_path` resolves to `os.path.join(config.settings.DB_DIR, "rules.sqlite3")` if not passed, so tests can inject a temp path via the `tmp_rule_db_path` fixture from Plan 01's conftest extension). Logic: (1) open connection via `db.get_connection(db_path)`; (2) try `db.query_by_code(conn, "payment_rules", code)` — if found, `source="payment"`; else try `db.query_by_code(conn, "drug_rules", code)` — if found, `source="drug"`; else return `models.not_found(code)` immediately (no rule_mapping lookup needed for a code that doesn't exist in either base table); (3) look up `db.query_by_code(conn, "rule_mapping", code)` — if present, populate `article_location`, `article_full_text`, `article_source` from that row; if absent, leave those three fields `None` (a code can exist in payment_rules/drug_rules but have no rule_mapping entry yet, e.g. if Plan 04's build hasn't covered every edge case — this must not raise); (4) construct and return `RuleResult(code=code, source=source, name=row["name"], payment_text=row["payment_text"], effective_from=row["effective_from"], effective_to=row["effective_to"], article_location=..., article_full_text=..., article_source=..., found=True)`. Wrap steps 1-4 in a `try/except sqlite3.Error` that logs via `warnings.warn` and returns `models.not_found(code)` rather than propagating a raw SQLite exception to callers (D-07's contract is "never raises" per the interface test) — but do NOT swallow `ValueError` from `db.query_by_code`'s table allowlist check, since that indicates a programming bug in this module, not a data/runtime issue.

    If Plan 04 did not already add `"rule_mapping"` to `db.py`'s `query_by_code` table allowlist, add it now (`{"payment_rules", "drug_rules", "rule_mapping"}`).
  </action>
  <behavior>
    - Test 1: `get_rule("64140C", db_path=real_db_path)` returns `found=True`, `source="payment"`, `article_source in ("csv", "docx", None)`
    - Test 2: `get_rule("ZZZZZZ99", db_path=real_db_path)` returns `found=False` without raising
    - Test 3: `isinstance(get_rule("64140C", db_path=real_db_path), RuleResult)` is `True`
    - Test 4: `get_rule` performs zero HTTP/network calls (verify by asserting no `requests` import inside `__init__.py`'s `get_rule` function — `grep -n "import requests" src/elc_audit_engine/rule_repository/__init__.py` returns no match, confirming D-05's "zero LLM at query time" by construction)
  </behavior>
  <acceptance_criteria>
    - `uv run pytest tests/test_rule_repository_interface.py -v` shows all 3 tests passed (file written in Plan 01, previously red with ImportError)
    - `get_rule` never imports or calls `requests`, `llm_client`, or any network library: `grep -n "requests\|llm_client" src/elc_audit_engine/rule_repository/__init__.py` returns no match
    - `get_rule("ZZZZZZ99")` (a code guaranteed absent from both CSVs) returns a `RuleResult` with `found=False`, confirmed by test, not by raising `KeyError`/`sqlite3.OperationalError`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_rule_repository_interface.py -v</automated>
  </verify>
  <done>get_rule() implemented as the single D-07/D-08 entry point; all 3 interface tests green; zero network/LLM imports in the query path</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 3 automated REQ-rule-repository acceptance criteria are now built and passing: SQLite payment_rules/drug_rules queryable (Plan 02), docx tree covers all 34 source files (Plan 03), and rule_mapping is precompiled for all codes with a CSV-reuse fast path plus LLM-assisted docx matching (Plan 04). This checkpoint runs the one piece that cannot be fully automated: the 20-code human spot-check (REQ-rule-repository's third acceptance criterion, D-06).

    A helper script prints, for each of the 20 codes in `tests/fixtures/rule_mapping_20_spotcheck.json`, the code's name, category hint, and the ACTUAL proposed `article_location`/`article_full_text`/`article_source` currently stored in `data/db/rules.sqlite3`'s `rule_mapping` table (queried live via `get_rule(code)`, not re-generated).
  </what-built>
  <how-to-verify>
    1. Run: `uv run python -c "
import json
from elc_audit_engine.rule_repository import get_rule
data = json.load(open('tests/fixtures/rule_mapping_20_spotcheck.json'))
for entry in data:
    r = get_rule(entry['code'])
    print(f\"--- {entry['code']} ({entry['name']}) [{entry['category_hint']}] ---\")
    print(f\"  article_source: {r.article_source}\")
    print(f\"  article_location: {r.article_location}\")
    print(f\"  article_full_text (first 300 chars): {(r.article_full_text or '')[:300]}\")
    print()
"`
    2. For each of the 20 codes, manually compare the printed `article_location`/`article_full_text` against the actual source (either the CSV's `支付規定`/`給付規定` cell for `article_source="csv"` entries, or the relevant `.docx`/`.doc`-converted file's section for `article_source="docx"` entries) to confirm the mapping is factually correct — not hallucinated, not pointing to an unrelated article.
    3. For any code where the mapping is WRONG or clearly hallucinated, note it — these are the "misses" for the hit-rate calculation. REQ-rule-repository requires the hit-rate to be acceptable (no fixed numeric threshold was specified in REQUIREMENTS.md/CONTEXT.md; use judgment — if most of the 20 are correct and misses are attributable to genuinely ambiguous/edge-case codes rather than a systemic extraction bug, the criterion is satisfiable; if the miss rate is high or reveals a systemic bug in Plan 03's tree extraction or Plan 04's matching logic, that must be fixed before sign-off, potentially requiring a gap-closure plan).
    4. Once you have reviewed all 20, tell me which entries are correct and which (if any) are wrong, and I will update `tests/fixtures/rule_mapping_20_spotcheck.json` accordingly: correct entries get their real `article_location`/`article_full_text` copied in from the live query result and `verified: true`; if some are wrong, we decide together whether to accept as a known limitation (documented) or trigger a gap-closure plan.
  </how-to-verify>
  <resume-signal>Reply with the per-code verification results (e.g., "all 20 correct" or "19 correct, 05402C is wrong because...") and I will finalize the fixture and complete the phase.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Lock verified fixture as regression test</name>
  <files>tests/fixtures/rule_mapping_20_spotcheck.json</files>
  <read_first>
    tests/fixtures/rule_mapping_20_spotcheck.json
    tests/test_rule_mapping_spotcheck.py
  </read_first>
  <action>
    Based on the human verification results from the preceding checkpoint task, update `tests/fixtures/rule_mapping_20_spotcheck.json`: for each of the 20 entries confirmed correct, set `article_location` and `article_full_text` to the actual values returned by `get_rule(code)` at verification time (copy them in as static strings — this fixture becomes a frozen regression snapshot, not a live query), and set `verified: true`. If the human identified any entries as incorrect and elected to accept them as a documented known limitation (rather than triggering a gap-closure plan), set `verified: true` anyway but add a `"note"` key to that entry explaining the accepted limitation (e.g., `"note": "docx tree matching imprecise for this code, accepted per user sign-off 2026-07-30"`) — do not silently mark a known-wrong mapping as clean without this note.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_rule_mapping_spotcheck.py -v` shows both tests passed (`test_fixture_has_20_entries` and `test_fixture_all_entries_verified` — the latter previously red since Plan 01, now green)
    - `uv run python -c "import json; d=json.load(open('tests/fixtures/rule_mapping_20_spotcheck.json')); assert all(e['verified'] for e in d); assert len(d)==20; print('20/20 verified')"` prints `20/20 verified`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/ -v</automated>
  </verify>
  <done>All 20 fixture entries verified=true with real article_location/article_full_text (or documented accepted limitations); full test suite green including tests/test_rule_mapping_spotcheck.py</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Downstream Phase 3-5 callers -> get_rule(code) | Public API boundary — this is the sole entry point per D-07/D-08; must never raise, must never call the network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Denial of Service | get_rule() exception handling | mitigate | All SQLite errors caught and converted to `not_found(code)`; callers in Phase 3-5 never need defensive try/except around this call |
| T-02-13 | Tampering | rule_mapping table allowlist in query_by_code | mitigate | Explicit allowlist check (`{"payment_rules","drug_rules","rule_mapping"}`) prevents arbitrary table-name injection even though `get_rule`'s only caller-supplied value is `code`, not `table` |
</threat_model>

<verification>
Run `uv run pytest tests/ -v` — expect the full suite green, including all 4 Wave-0 files that started red in Plan 01 (`test_rule_repository_sqlite.py`, `test_docx_tree_coverage.py`, `test_rule_mapping_spotcheck.py`, `test_rule_repository_interface.py`).
</verification>

<success_criteria>
- get_rule(code) is the single public entry point, returns RuleResult, never raises, performs zero LLM/network calls
- 20-code human spot-check completed and locked as a regression fixture (verified=true for all 20)
- Full test suite green — REQ-rule-repository's 3 acceptance criteria all automated-and-passing
</success_criteria>

<output>
After completion, create `.planning/phases/02-rule-repository/02-05-SUMMARY.md`
</output>
