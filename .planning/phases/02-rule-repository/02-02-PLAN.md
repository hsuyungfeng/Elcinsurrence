---
phase: 02-rule-repository
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - src/elc_audit_engine/rule_repository/db.py
  - src/elc_audit_engine/rule_repository/loaders/__init__.py
  - src/elc_audit_engine/rule_repository/loaders/payment_loader.py
  - src/elc_audit_engine/rule_repository/loaders/drug_loader.py
  - src/elc_audit_engine/rule_repository/loaders/dates.py
  - src/elc_audit_engine/rule_repository/scripts/build_sqlite.py
  - tests/test_rule_repository_dates.py
autonomous: true
requirements: [REQ-rule-repository]

must_haves:
  truths:
    - "payment_rules table is queryable by code and returns correct row for 64140C"
    - "drug_rules table is queryable by code and returns correct row for AC10398100"
    - "ROC and Gregorian dates are both parsed to ISO 8601 without cross-contamination between the two CSVs"
    - "All SQL queries use parameterized placeholders, never string-formatted SQL"
  artifacts:
    - path: "src/elc_audit_engine/rule_repository/db.py"
      provides: "SQLite connection management + schema creation (payment_rules, drug_rules DDL)"
      contains: "CREATE TABLE"
    - path: "src/elc_audit_engine/rule_repository/loaders/payment_loader.py"
      provides: "load_payment_csv(db_path, csv_path) -> int (row count loaded)"
      exports: ["load_payment_csv"]
    - path: "src/elc_audit_engine/rule_repository/loaders/drug_loader.py"
      provides: "load_drug_csv(db_path, csv_path) -> int (row count loaded)"
      exports: ["load_drug_csv"]
    - path: "src/elc_audit_engine/rule_repository/loaders/dates.py"
      provides: "parse_flexible_date(raw: str) -> str | None handling both ROC 7-digit and Gregorian 8-digit formats"
      exports: ["parse_flexible_date"]
  key_links:
    - from: "src/elc_audit_engine/rule_repository/loaders/payment_loader.py"
      to: "src/elc_audit_engine/rule_repository/db.py"
      via: "sqlite3.connect(db_path) + parameterized INSERT/executemany"
      pattern: "sqlite3\\.connect"
    - from: "src/elc_audit_engine/rule_repository/loaders/payment_loader.py"
      to: "src/elc_audit_engine/rule_repository/loaders/dates.py"
      via: "parse_flexible_date(row['生效起日'])"
      pattern: "parse_flexible_date"
---

<objective>
Build the SQLite structured-rule layer: `payment_rules` (from the 2,669-row payment CSV) and `drug_rules` (from the 11,273-row drug CSV), keeping only the D-01/D-02 core columns, with correct ROC-vs-Gregorian date normalization per CSV. Ship a one-shot build script that populates `data/db/rules.sqlite3` from the two source CSVs.

Purpose: This is the first of the three REQ-rule-repository acceptance criteria ("SQLite payment_rules/drug_rules 可查詢") and unblocks Plan 04's rule_mapping build (which needs SQLite codes to iterate over) and Plan 05's single query interface.
Output: `db.py` (schema + connection mgmt), `loaders/payment_loader.py` + `loaders/drug_loader.py` (CSV -> SQLite), `loaders/dates.py` (shared date-parsing helper), `scripts/build_sqlite.py` (orchestration entrypoint), and a green `tests/test_rule_repository_sqlite.py` (written in Plan 01, currently red).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-rule-repository/02-CONTEXT.md
@.planning/phases/02-rule-repository/02-RESEARCH.md
@config/settings.py
@tests/test_rule_repository_sqlite.py
@tests/conftest.py
</context>

<interfaces>
<!-- Contract from Plan 01 that this plan must satisfy -->

`tests/test_rule_repository_sqlite.py` (Plan 01, currently red) expects:
```python
from elc_audit_engine.rule_repository import db, loaders
# db must expose schema creation; loaders must expose:
loaders.load_payment_csv(db_path: str, csv_path: str) -> int
loaders.load_drug_csv(db_path: str, csv_path: str) -> int
# payment_rules table columns: code, name, payment_text, effective_from, effective_to
# drug_rules table columns: code, name, payment_text, effective_from, effective_to
```

Verified CSV header facts (re-verified this session, do not deviate):
- Payment CSV columns: `診療項目代碼, 健保支付點數, 生效起日, 生效迄日, 英文項目名稱, 中文項目名稱, 支付規定` — dates are 8-digit Gregorian (e.g. `20160401`)
- Drug CSV columns (22 total, core subset per D-01/D-02): `藥品代號, 藥品中文名稱, 給付規定, 有效起日, 有效迄日` — dates are 7-digit ROC (e.g. `1121001` = ROC 112 = 2023-10-01)
- Both CSVs are `utf-8-sig` encoded (BOM present) — must open with `encoding="utf-8-sig"`, not plain `utf-8`
- Resolve CSV filenames via `glob.glob(os.path.join(RULE_SOURCE_DIR, "醫療服務給付項目*.csv"))` and `glob.glob(os.path.join(RULE_SOURCE_DIR, "藥品項查詢項目檔*.csv"))` — do NOT hardcode the exact filename string (drug CSV has an unusual double-space before "摘要" that could change if the file is re-exported)

From config/settings.py (Phase 1, already built):
```python
RULE_SOURCE_DIR  # points to officialdocument/審查注意事項/
DB_DIR            # points to data/db/ — target for rules.sqlite3
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Date parsing helper + SQLite schema/connection management</name>
  <files>src/elc_audit_engine/rule_repository/loaders/dates.py, src/elc_audit_engine/rule_repository/db.py, tests/test_rule_repository_dates.py</files>
  <read_first>
    .planning/phases/02-rule-repository/02-RESEARCH.md
    tests/test_rule_repository_sqlite.py
  </read_first>
  <action>
    Create `src/elc_audit_engine/rule_repository/loaders/dates.py` with `def parse_flexible_date(raw: str) -> str | None`. Handle exactly two input shapes per RESEARCH.md Pitfall 3: 8-digit Gregorian `YYYYMMDD` (payment CSV, e.g. `20160401` -> `2016-04-01`) and 7-digit ROC `RRRMMDD` (drug CSV, e.g. `1121001` -> year=112+1911=2023 -> `2023-10-01`). Strip whitespace first. Treat empty string, `"null"`, `"0"`, and `"99991231"` as sentinel "no date" values returning `None`. For any other length/format, return `None` and do not raise (log via `warnings.warn` with the raw value for auditability — do not silently swallow without a trace).

    Create `src/elc_audit_engine/rule_repository/db.py` with: `SCHEMA_PAYMENT = "CREATE TABLE IF NOT EXISTS payment_rules (code TEXT PRIMARY KEY, name TEXT, payment_text TEXT, effective_from TEXT, effective_to TEXT)"`, `SCHEMA_DRUG = "CREATE TABLE IF NOT EXISTS drug_rules (code TEXT PRIMARY KEY, name TEXT, payment_text TEXT, effective_from TEXT, effective_to TEXT)"` (note: `payment_text` column name is shared/reused for both tables' 支付規定/給付規定 text — this keeps the two tables structurally identical for simpler downstream querying in Plan 05). Add `def get_connection(db_path: str) -> sqlite3.Connection` (creates parent dir via `os.makedirs(os.path.dirname(db_path), exist_ok=True)` if missing, returns a connection with `row_factory = sqlite3.Row`). Add `def init_schema(conn: sqlite3.Connection) -> None` executing both `CREATE TABLE IF NOT EXISTS` statements. Add `def query_by_code(conn: sqlite3.Connection, table: str, code: str) -> sqlite3.Row | None` using a parameterized query `f"SELECT * FROM {table} WHERE code = ?"` with `table` restricted to a hardcoded allowlist (`{"payment_rules", "drug_rules"}` — raise `ValueError` if not in allowlist) so no untrusted string ever reaches the f-string, and the actual `code` value always goes through the `?` placeholder, never string-interpolated.

    Create `tests/test_rule_repository_dates.py` with the 4 behavior tests listed below (this file is self-contained — it does not depend on Plan 01's Wave 0 scaffolding and must pass immediately once `dates.py`/`db.py` are written in this same task).
  </action>
  <behavior>
    - Test 1: `parse_flexible_date("20160401") == "2016-04-01"` (Gregorian)
    - Test 2: `parse_flexible_date("1121001") == "2023-10-01"` (ROC)
    - Test 3: `parse_flexible_date("") is None` and `parse_flexible_date("null") is None`
    - Test 4: `db.get_connection` creates parent directory and returns a connection; `db.init_schema` then `db.query_by_code` round-trips a manually inserted row
  </behavior>
  <acceptance_criteria>
    - `parse_flexible_date("20160401")` returns `"2016-04-01"` exactly
    - `parse_flexible_date("1121001")` returns `"2023-10-01"` exactly (ROC year 112 + 1911 = 2023)
    - `db.query_by_code` source contains a `?` placeholder, not an f-string-interpolated `code` value: `grep -n "WHERE code" src/elc_audit_engine/rule_repository/db.py` shows `?` in the matched line
    - `db.query_by_code` raises `ValueError` when `table` is not in `{"payment_rules", "drug_rules"}`
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_rule_repository_dates.py -v</automated>
  </verify>
  <done>dates.py correctly disambiguates ROC vs Gregorian; db.py provides schema creation + parameterized query_by_code with table-name allowlisting</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Payment + drug CSV loaders and build script</name>
  <files>src/elc_audit_engine/rule_repository/loaders/__init__.py, src/elc_audit_engine/rule_repository/loaders/payment_loader.py, src/elc_audit_engine/rule_repository/loaders/drug_loader.py, src/elc_audit_engine/rule_repository/scripts/build_sqlite.py</files>
  <read_first>
    src/elc_audit_engine/rule_repository/db.py
    src/elc_audit_engine/rule_repository/loaders/dates.py
    tests/test_rule_repository_sqlite.py
    config/settings.py
  </read_first>
  <action>
    Create `src/elc_audit_engine/rule_repository/loaders/__init__.py` re-exporting `load_payment_csv` and `load_drug_csv` from the two loader modules so `from elc_audit_engine.rule_repository import loaders; loaders.load_payment_csv(...)` and `loaders.load_drug_csv(...)` both resolve (matching the exact import pattern in `tests/test_rule_repository_sqlite.py`).

    Create `src/elc_audit_engine/rule_repository/loaders/payment_loader.py` with `def load_payment_csv(db_path: str, csv_path: str) -> int`. Read the CSV with `csv.DictReader` under `encoding="utf-8-sig"`. Map columns per D-01/D-02: `診療項目代碼 -> code`, `中文項目名稱 -> name`, `支付規定 -> payment_text`, `生效起日 -> effective_from` (via `dates.parse_flexible_date`, Gregorian 8-digit format), `生效迄日 -> effective_to` (same). Use `db.get_connection` + `db.init_schema`, then batch-insert via `conn.executemany("INSERT OR REPLACE INTO payment_rules (code, name, payment_text, effective_from, effective_to) VALUES (?, ?, ?, ?, ?)", rows)` — parameterized, never f-string SQL. Commit and return the number of rows inserted (expect 2,669).

    Create `src/elc_audit_engine/rule_repository/loaders/drug_loader.py` mirroring the payment loader: `def load_drug_csv(db_path: str, csv_path: str) -> int`. Map `藥品代號 -> code`, `藥品中文名稱 -> name`, `給付規定 -> payment_text`, `有效起日 -> effective_from` (ROC 7-digit format via same `dates.parse_flexible_date`), `有效迄日 -> effective_to`. Insert into `drug_rules` table. Return row count (expect 11,273).

    Create `src/elc_audit_engine/rule_repository/scripts/build_sqlite.py` as a standalone script (guarded by `if __name__ == "__main__":`) that: resolves the payment CSV via `glob.glob(os.path.join(config.settings.RULE_SOURCE_DIR, "醫療服務給付項目*.csv"))[0]` and drug CSV via `glob.glob(os.path.join(config.settings.RULE_SOURCE_DIR, "藥品項查詢項目檔*.csv"))[0]`, resolves `db_path = os.path.join(config.settings.DB_DIR, "rules.sqlite3")`, calls both loaders, and prints a summary (`f"payment_rules: {n_payment} rows, drug_rules: {n_drug} rows"`). Run this script once to actually populate `data/db/rules.sqlite3` for real (not just in tests) — this is the artifact Plan 04/05 will query against.
  </action>
  <behavior>
    - Test 1: `loaders.load_payment_csv(tmp_db, real_payment_csv_path)` returns `2669` and querying code `64140C` returns a row with non-null `payment_text`
    - Test 2: `loaders.load_drug_csv(tmp_db, real_drug_csv_path)` returns `11273` and querying code `AC10398100` returns a row with `effective_from == "2023-10-01"`
    - Test 3: `payment_rules`/`drug_rules` table columns match exactly `code, name, payment_text, effective_from, effective_to` via `PRAGMA table_info`
  </behavior>
  <acceptance_criteria>
    - `uv run pytest tests/test_rule_repository_sqlite.py -v` shows all 4 tests passed (the file written in Plan 01, previously red with ImportError)
    - Running `uv run python -m elc_audit_engine.rule_repository.scripts.build_sqlite` populates `data/db/rules.sqlite3` with `payment_rules` (2669 rows) and `drug_rules` (11273 rows): verify via `uv run python -c "import sqlite3; c=sqlite3.connect('data/db/rules.sqlite3'); print(c.execute('SELECT COUNT(*) FROM payment_rules').fetchone()); print(c.execute('SELECT COUNT(*) FROM drug_rules').fetchone())"` prints `(2669,)` and `(11273,)`
    - No f-string or `.format()`-constructed SQL anywhere in `loaders/`: `grep -rn "f\"SELECT\|f\"INSERT\|\.format(" src/elc_audit_engine/rule_repository/loaders/` returns no matches
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_rule_repository_sqlite.py -v</automated>
  </verify>
  <done>payment_rules (2669 rows) and drug_rules (11273 rows) load correctly from CSV into SQLite via parameterized queries; tests/test_rule_repository_sqlite.py fully green; data/db/rules.sqlite3 populated for real</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CSV file -> SQLite | Local, developer-controlled CSV files read at build time — not user/network input, but still parameterized as defense in depth per RESEARCH.md V5 |
| table name parameter -> SQL string | `query_by_code`'s `table` argument must not allow arbitrary string injection into the FROM clause |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | loaders/payment_loader.py, loaders/drug_loader.py | mitigate | All INSERT statements use `?` parameterized placeholders via `executemany`, never string-formatted SQL (RESEARCH.md ASVS V5) |
| T-02-03 | Tampering | db.py `query_by_code` | mitigate | `table` parameter restricted to a hardcoded allowlist (`{"payment_rules","drug_rules"}`) before being used in the FROM clause; raises ValueError otherwise |
| T-02-04 | Information Disclosure | data/db/rules.sqlite3 | accept | Local-only file, single-user machine per D2; no network exposure in this phase |
</threat_model>

<verification>
Run `uv run pytest tests/test_rule_repository_sqlite.py -v` — expect 4 passed (upgraded from Plan 01's red state).
Run `uv run pytest tests/ -v` — expect no regressions in Phase 1's `tests/test_config.py` or Plan 01's `tests/test_rule_repository_models.py`.
</verification>

<success_criteria>
- `payment_rules` table queryable by code, 2,669 rows loaded, dates correctly parsed as Gregorian
- `drug_rules` table queryable by code, 11,273 rows loaded, dates correctly parsed as ROC->Gregorian
- All SQL uses parameterized queries (`?` placeholders), verified via grep
- `data/db/rules.sqlite3` exists on disk with both tables populated
</success_criteria>

<output>
After completion, create `.planning/phases/02-rule-repository/02-02-SUMMARY.md`
</output>
