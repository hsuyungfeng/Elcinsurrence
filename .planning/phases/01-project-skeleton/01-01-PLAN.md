---
phase: 01-project-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - .env.example
  - .gitignore
  - config/settings.py
  - config/llama_config.json
  - src/elc_audit_engine/__init__.py
  - src/elc_audit_engine/parsers/__init__.py
  - src/elc_audit_engine/rule_repository/__init__.py
  - src/elc_audit_engine/record_aggregator/__init__.py
  - src/elc_audit_engine/comparator/__init__.py
  - src/elc_audit_engine/generators/__init__.py
  - data/.gitkeep
  - data/db/.gitkeep
  - data/rag/.gitkeep
  - data/output/.gitkeep
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
autonomous: true
requirements: [REQ-project-skeleton]

must_haves:
  truths:
    - "uv run python -c \"print('ok')\" 執行成功（uv 專案已初始化）"
    - "config.settings 模組可載入，並讀取 LLAMA_CPP_BASE_URL 等設定（含 .env override 機制）"
    - "目錄結構（pyproject.toml、config/、src/elc_audit_engine/、data/、tests/）與 DrtoolboxLocalServer 慣例一致（Python+uv、python-docx、pandas、pageindex、SQLite 佈局）"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv 專案定義，含 dependencies 清單"
      contains: "[project]"
    - path: "config/settings.py"
      provides: "設定載入機制（PROJECT_ROOT、env-var override、LLAMA_CPP_BASE_URL）"
      min_lines: 15
    - path: "config/llama_config.json"
      provides: "llama.cpp server 連線設定（D2: Ornith-1.0-9B, n_ctx 32768）"
      contains: "32768"
    - path: "src/elc_audit_engine/__init__.py"
      provides: "主套件根目錄"
    - path: "tests/test_config.py"
      provides: "驗證 config.settings 可載入且讀得到預期鍵值"
      min_lines: 10
  key_links:
    - from: "config/settings.py"
      to: ".env"
      via: "python-dotenv load_dotenv()"
      pattern: "load_dotenv"
    - from: "config/settings.py"
      to: "config/llama_config.json"
      via: "json.load 讀取 llama server 連線位址"
      pattern: "llama_config"
---

<objective>
初始化 `elc-audit-engine` 的 uv 專案骨架：`pyproject.toml`（uv 管理，Python 3.12）、`config/` 設定載入機制（沿用 DrtoolboxLocalServer 的 `settings.py` + `load_dotenv()` 模式，可讀取 llama.cpp server 位址等設定）、`src/elc_audit_engine/` 套件目錄（含 Phase 2-7 各子系統的空殼子套件，供後續 phase 填入實作）、`data/` 資料目錄（db/rag/output 三個子目錄，沿用 DrtoolboxLocalServer 的 SQLite+PageIndex 佈局）、`tests/` 測試骨架。

Purpose: 讓 Phase 2 起的所有實作（規則庫、解析器、病歷彙整器、比對器、輸出生成器）有一致的套件路徑與設定載入機制可以直接掛載，不需要每個 phase 重新决定專案佈局。
Output: 可執行的 uv 專案（`uv run` 成功）、可載入的 `config.settings` 模組、符合 DrtoolboxLocalServer 慣例的目錄結構。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/intel/decisions.md
@.planning/intel/constraints.md
@progress.md

<!-- Reference implementation being mirrored (D4 — 沿用 DrtoolboxLocalServer 佈局). Read these before creating files. -->
@/home/hsu/Desktop/DrtoolboxLocalServer/pyproject.toml
@/home/hsu/Desktop/DrtoolboxLocalServer/config/settings.py
@/home/hsu/Desktop/DrtoolboxLocalServer/config/llama_config.json
@/home/hsu/Desktop/DrtoolboxLocalServer/.env.example
@/home/hsu/Desktop/DrtoolboxLocalServer/.gitignore
</context>

<interfaces>
<!-- Concrete reference values extracted from ~/Desktop/DrtoolboxLocalServer. Use directly — no further exploration needed. -->

DrtoolboxLocalServer/pyproject.toml (`[project]` block — mirror structure, NOT the dependency list — this project's dependency list is different, see Task 1). Note: DrtoolboxLocalServer does NOT use a `src/` layout (its `src/` dir is not the installed package root, it uses flat imports), so it has no `[tool.setuptools.packages.find]` section — this project DOES use a `src/elc_audit_engine/` package layout, so Task 1 must add that section explicitly (see Task 1 action):
```toml
[project]
name = "drtoolbox-local-server"
version = "1.0.0"
description = "Local medical RAG server with dual-collection support"
dependencies = [ ... ]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
```
`.python-version` contains exactly: `3.12`

DrtoolboxLocalServer/config/settings.py (pattern to mirror — env-var overridable paths with safe fallback):
```python
import os
from dotenv import load_dotenv

load_dotenv()

PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), ".."))

MODEL_PATH = os.getenv("MODEL_PATH", os.path.join(PROJECT_ROOT, "data/models/qwen-3.6-7b-it.gguf"))
LOG_DIR = os.getenv("LOG_DIR", os.path.join(PROJECT_ROOT, "data"))
DATA_DIR = os.getenv("DATA_DIR", os.path.join(PROJECT_ROOT, "data"))
```

DrtoolboxLocalServer/config/llama_config.json (actual production values — D2 locked: Ornith-1.0-9B Q6_K_XL, n_ctx 32768, localhost:8080):
```json
{
  "model": {
    "path": "/home/hsu/llama.cpp/models/Ornith-1.0-9B-UD-Q6_K_XL.gguf",
    "name": "Ornith-1.0-9B",
    "quantization": "Q6_K_XL"
  },
  "inference": { "n_ctx": 32768, "max_tokens": 1024, "temperature": 0.7 },
  "server": { "host": "0.0.0.0", "port": 8080 }
}
```

DrtoolboxLocalServer/.env.example (pattern — path overrides + "DO NOT COMMIT REAL TOKENS/SECRETS" comment convention):
```
CLINIC_DB_PATH=data/db/clinic.db
BIND_HOST=127.0.0.1
# Third-party Integrations (DO NOT COMMIT REAL TOKENS/SECRETS)
# DEEPSEEK_API_KEY=
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: uv 專案初始化與目錄骨架</name>
  <files>pyproject.toml, .python-version, .gitignore, src/elc_audit_engine/__init__.py, src/elc_audit_engine/parsers/__init__.py, src/elc_audit_engine/rule_repository/__init__.py, src/elc_audit_engine/record_aggregator/__init__.py, src/elc_audit_engine/comparator/__init__.py, src/elc_audit_engine/generators/__init__.py, data/.gitkeep, data/db/.gitkeep, data/rag/.gitkeep, data/output/.gitkeep, tests/__init__.py</files>
  <read_first>
    /home/hsu/Desktop/DrtoolboxLocalServer/pyproject.toml
    /home/hsu/Desktop/DrtoolboxLocalServer/.gitignore
    /home/hsu/Desktop/Elcinsurrence/.gitignore
  </read_first>
  <action>
Do NOT run `uv init` (it scaffolds a template main.py/README that conflicts with existing repo files); instead hand-write `pyproject.toml` directly at repo root with: `[project]` block — `name = "elc-audit-engine"`, `version = "0.1.0"`, `description = "電子抽審自動化病歷補強與申復生成引擎"`, `requires-python = ">=3.12"`, `dependencies = ["flask>=2.0", "python-dotenv>=0.19", "pandas>=2.0", "python-docx>=1.1.0", "pageindex>=0.1.0", "chromadb>=0.4.0", "requests>=2.28"]` (Flask included per D4 stack list even though Flask wiring is Phase 9 scope — only the dependency is declared here, no Flask app code is created in this plan). Add `[build-system]` block identical in structure to DrtoolboxLocalServer's (`requires = ["setuptools"]`, `build-backend = "setuptools.build_meta"`). Because this project uses a `src/elc_audit_engine/` layout (unlike DrtoolboxLocalServer's flat layout), also add `[tool.setuptools.packages.find]` with `where = ["src"]` so `elc_audit_engine` and its five sub-packages are discovered correctly. Add `[tool.pytest.ini_options]` with `testpaths = ["tests"]`. Also add a `[dependency-groups]` block with `dev = ["pytest>=8.0"]` — `pytest` is a third-party PyPI package, NOT part of the Python standard library, and must be declared explicitly or `uv run pytest` will fail on any machine without a pre-existing global pytest install (relying on a global fallback is not environment-neutral and must not be assumed).

Create `.python-version` at repo root containing exactly `3.12` (matches DrtoolboxLocalServer).

Run `uv sync --group dev` at repo root to generate `.venv/` and `uv.lock`, install `pytest` into the project's `.venv` from the `dev` dependency group, and confirm dependency resolution succeeds.

Append to the existing `.gitignore` (do not overwrite — it currently only has OMC/embed-cache/OS-temp entries): a new `# Python / uv` section with `__pycache__/`, `*.pyc`, `.venv/`, `.env` (note: `uv.lock` must NOT be added to `.gitignore` — it must be committed), and a `# 執行期資料（可重新產生）` section with `data/db/*`, `!data/db/.gitkeep`, `data/rag/*`, `!data/rag/.gitkeep`, `logs/` (both `data/db/` and `data/rag/` use the same wildcard+negation pattern so their respective `.gitkeep` markers stay git-tracked while all other contents are ignored — a blanket `data/db/` rule would exclude `.gitkeep` too and contradict the "tracked in git" intent below).

Create package directory `src/elc_audit_engine/__init__.py` containing a single line: `"""elc-audit-engine: 電子抽審自動化病歷補強與申復生成引擎."""`. Create five empty sub-package stub dirs, each with only an `__init__.py` containing a one-line Chinese docstring naming the subsystem and its owning future phase: `src/elc_audit_engine/parsers/__init__.py` (`"""解析器：申報XML/核減清單/SOAP文字。Phase 3 實作。"""`), `src/elc_audit_engine/rule_repository/__init__.py` (`"""規則庫：SQLite payment_rules/drug_rules + PageIndex + rule_mapping。Phase 2 實作。"""`), `src/elc_audit_engine/record_aggregator/__init__.py` (`"""病歷彙整器：Provider 介面 + 本地檔案 Provider。Phase 4 實作。"""`), `src/elc_audit_engine/comparator/__init__.py` (`"""三方比對器：醫令↔規則↔病歷。Phase 5 實作。"""`), `src/elc_audit_engine/generators/__init__.py` (`"""輸出生成器：病歷補強報告 + 申復草稿。Phase 6-7 實作。"""`). These are intentionally empty placeholder packages (no logic) — do not implement subsystem logic in this plan, that is out of Phase 1 scope per REQ-project-skeleton.

Create `data/.gitkeep`, `data/db/.gitkeep`, `data/rag/.gitkeep`, `data/output/.gitkeep` (empty files) so the directory structure is tracked in git even though contents are gitignored. `data/output/` is NOT gitignored (generated reports may be inspected/committed selectively later; only `db/` and `rag/` cache dirs are ignored per this task's `.gitignore` edit above).

Create `tests/__init__.py` (empty file, makes tests/ a package for pytest discovery consistency with DrtoolboxLocalServer's `tests/` layout).
  </action>
  <verify>
    <automated>cd /home/hsu/Desktop/Elcinsurrence && uv run python -c "import elc_audit_engine; import elc_audit_engine.parsers; import elc_audit_engine.rule_repository; import elc_audit_engine.record_aggregator; import elc_audit_engine.comparator; import elc_audit_engine.generators; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "print('ok')"` prints `ok` with exit code 0
    - `pyproject.toml` exists at repo root with a `[project]` table containing `name = "elc-audit-engine"`
    - `.python-version` exists containing `3.12`
    - `uv.lock` exists at repo root (created by `uv sync --group dev`)
    - `pyproject.toml` contains a `[dependency-groups]` table with `dev = ["pytest>=8.0"]` (or equivalent pytest version pin)
    - `src/elc_audit_engine/__init__.py` and all five sub-package `__init__.py` files exist and are importable (verified by the automated check above)
    - `data/db/`, `data/rag/`, `data/output/` directories exist with `.gitkeep` present in `data/`, `data/db/`, `data/rag/`
    - `git status --short` shows `.venv/` is NOT listed as untracked-to-add (confirms `.gitignore` is effective)
    - `git add -A && git status --short` shows `data/db/.gitkeep` and `data/rag/.gitkeep` ARE staged (confirms the wildcard+negation `.gitignore` pattern tracks them, not a blanket exclusion)
  </acceptance_criteria>
  <done>uv 專案可於根目錄成功執行；src/elc_audit_engine 套件與五個子套件 stub 皆可被 import；data/ 目錄結構就緒；.gitignore 已更新排除 .venv/、__pycache__/、data/db/、data/rag/ 內容物。</done>
</task>

<task type="auto">
  <name>Task 2: Config 載入機制與 llama.cpp 連線設定</name>
  <files>config/settings.py, config/llama_config.json, .env.example</files>
  <read_first>
    /home/hsu/Desktop/DrtoolboxLocalServer/config/settings.py
    /home/hsu/Desktop/DrtoolboxLocalServer/config/llama_config.json
    /home/hsu/Desktop/DrtoolboxLocalServer/.env.example
    /home/hsu/Desktop/Elcinsurrence/progress.md
  </read_first>
  <action>
Create `config/settings.py` mirroring DrtoolboxLocalServer's `config/settings.py` pattern exactly: `import os`, `import json`, `from dotenv import load_dotenv`, `load_dotenv()` at module top, then `PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), ".."))`. Define these module-level constants, each `os.getenv("ENV_VAR_NAME", default_using_PROJECT_ROOT)`:
- `DATA_DIR = os.getenv("DATA_DIR", os.path.join(PROJECT_ROOT, "data"))`
- `DB_DIR = os.getenv("DB_DIR", os.path.join(PROJECT_ROOT, "data/db"))`
- `RAG_DIR = os.getenv("RAG_DIR", os.path.join(PROJECT_ROOT, "data/rag"))`
- `OUTPUT_DIR = os.getenv("OUTPUT_DIR", os.path.join(PROJECT_ROOT, "data/output"))`
- `RULE_SOURCE_DIR = os.getenv("RULE_SOURCE_DIR", os.path.join(PROJECT_ROOT, "officialdocument/審查注意事項"))` (D6/rule_mapping source corpus, per constraints.md — real existing directory)
- `LLAMA_CPP_BASE_URL = os.getenv("LLAMA_CPP_BASE_URL", "http://localhost:8080")` (D2 locked)
- `LLAMA_CONFIG_PATH = os.getenv("LLAMA_CONFIG_PATH", os.path.join(PROJECT_ROOT, "config/llama_config.json"))`

Add a function `load_llama_config() -> dict` that opens `LLAMA_CONFIG_PATH` with `json.load` and returns the parsed dict; if the file is missing, raise `FileNotFoundError` with a message naming the expected path (do not silently return `{}` — config must be explicit about missing setup per fail-fast principle, this is scaffolding not production hardening).

Create `config/llama_config.json` mirroring DrtoolboxLocalServer's structure but using this project's actual model path from progress.md D2 (`/home/hsu/llama.cpp/models/Ornith-1.0-9B-UD-Q6_K_XL.gguf`) and n_ctx 32768: top-level keys `model` (`path`, `name: "Ornith-1.0-9B"`, `quantization: "Q6_K_XL"`), `inference` (`n_ctx: 32768`, `max_tokens: 1024`, `temperature: 0.7`), `server` (`host: "localhost"`, `port: 8080`). Omit DrtoolboxLocalServer's GPU-batching/memory-monitoring keys (`batching`, `memory`, `gpu_monitoring`) — those are DrtoolboxLocalServer-specific ops config not needed for this project's config-loading scope; keep the JSON minimal to what Phase 1 config loading actually needs to prove out.

Create `.env.example` at repo root mirroring DrtoolboxLocalServer's `.env.example` pattern (path overrides + explicit "DO NOT COMMIT" comment for secrets, even though no real secret exists yet — per the threat model note on config-secret-handling): include commented-out `# DATA_DIR=`, `# DB_DIR=`, `# RAG_DIR=`, `# OUTPUT_DIR=`, `# RULE_SOURCE_DIR=`, `# LLAMA_CPP_BASE_URL=http://localhost:8080`, `# LLAMA_CONFIG_PATH=`, and a final section headed `# Third-party Integrations (DO NOT COMMIT REAL TOKENS/SECRETS)` with no active entries (no secrets exist in Phase 1 scope — this documents the override convention for whenever one is added in a later phase).
  </action>
  <verify>
    <automated>cd /home/hsu/Desktop/Elcinsurrence && uv run python -c "from config import settings; print(settings.LLAMA_CPP_BASE_URL); cfg = settings.load_llama_config(); assert cfg['inference']['n_ctx'] == 32768; print('config-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `config/settings.py` exists, module-level import succeeds with no exceptions
    - `settings.LLAMA_CPP_BASE_URL` equals `"http://localhost:8080"` when no env override is set
    - `settings.load_llama_config()` returns a dict where `cfg["inference"]["n_ctx"] == 32768` and `cfg["model"]["name"] == "Ornith-1.0-9B"`
    - `config/llama_config.json` is valid JSON (parses without error)
    - `.env.example` exists at repo root and contains the string `LLAMA_CPP_BASE_URL`
    - Setting env var `DATA_DIR=/tmp/custom` before import causes `settings.DATA_DIR` to resolve to `/tmp/custom` (proves env-var override works, not just default path)
  </acceptance_criteria>
  <done>config.settings 模組可載入且提供 LLAMA_CPP_BASE_URL、DATA_DIR 等路徑設定，支援環境變數覆寫；load_llama_config() 可讀取 config/llama_config.json 並回傳含 D2 鎖定值（Ornith-1.0-9B, n_ctx 32768）的 dict；.env.example 已建立供後續設定secrets時參考。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Config 載入測試骨架</name>
  <files>tests/conftest.py, tests/test_config.py</files>
  <read_first>
    /home/hsu/Desktop/DrtoolboxLocalServer/tests/conftest.py
    /home/hsu/Desktop/Elcinsurrence/config/settings.py
  </read_first>
  <behavior>
    - Test 1: `test_settings_module_importable` — `from config import settings` succeeds and `settings.PROJECT_ROOT` is an existing directory (`os.path.isdir`)
    - Test 2: `test_llama_config_defaults` — `settings.LLAMA_CPP_BASE_URL == "http://localhost:8080"` under default (no env override) conditions
    - Test 3: `test_load_llama_config_returns_locked_values` — `settings.load_llama_config()` returns dict with `["inference"]["n_ctx"] == 32768` and `["model"]["name"] == "Ornith-1.0-9B"` (asserts D2-locked values are wired, not placeholder values)
    - Test 4: `test_load_llama_config_missing_file_raises` — monkeypatch `settings.LLAMA_CONFIG_PATH` to a nonexistent path and assert `settings.load_llama_config()` raises `FileNotFoundError`, proving the fail-fast behavior from Task 2 is real (not a silently-swallowed exception)
  </behavior>
  <action>
Create `tests/conftest.py` mirroring DrtoolboxLocalServer's `tests/conftest.py` pattern: add project root to `sys.path` if needed for `config` package resolution (check if DrtoolboxLocalServer's conftest does `sys.path.insert` — mirror that exact mechanism, do not invent a different one), and define any shared pytest fixtures needed by the four tests above (a `monkeypatch`-based approach is standard pytest, no custom fixture required unless conftest reveals a project-specific pattern to follow).

Create `tests/test_config.py` implementing the four tests listed in `<behavior>` using `pytest` (declared as a `dev` dependency-group member in Task 1's `pyproject.toml` — third-party package, not stdlib) with its built-in `monkeypatch` fixture. Import `from config import settings`. For Test 4, use `monkeypatch.setattr(settings, "LLAMA_CONFIG_PATH", "/nonexistent/path/llama_config.json")` then call `settings.load_llama_config()` inside `pytest.raises(FileNotFoundError)`.
  </action>
  <verify>
    <automated>cd /home/hsu/Desktop/Elcinsurrence && uv run --group dev pytest tests/test_config.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --group dev pytest tests/test_config.py -v` exits 0 with all 4 tests passing
    - Test output shows exactly 4 collected tests, 4 passed, 0 failed, 0 errors
    - `tests/conftest.py` exists and pytest successfully discovers `config` package (no `ModuleNotFoundError` during collection)
    - `uv run which pytest` resolves to a path inside this project's `.venv/` (not a global/system pytest), confirming pytest is genuinely a declared project dependency and not an incidental global install
  </acceptance_criteria>
  <done>tests/test_config.py 的 4 個測試全數通過，證明 config 載入機制（含環境變數覆寫、llama_config.json 讀取、缺檔 fail-fast）行為符合預期，非僅「檔案存在」層級的驗證。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| local filesystem → config loader | `config/settings.py` reads `.env` and `config/llama_config.json` from local disk; no network/remote input in Phase 1 |
| env var → process | `os.getenv()` overrides accept arbitrary string values from the process environment (developer-controlled in Phase 1, not user-facing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-01 | Information Disclosure | `.env` file | mitigate | `.env` is gitignored (Task 1 `.gitignore` update); `.env.example` contains only placeholder/commented keys, explicit "DO NOT COMMIT REAL TOKENS/SECRETS" comment mirrors DrtoolboxLocalServer convention (Task 2) |
| T-01-02 | Information Disclosure | `config/llama_config.json` | accept | Contains only local model path and localhost:8080 connection info, no credentials; llama.cpp server has no auth in this deployment (D2: patient data stays local-only, no cloud exposure) — low-value target, same posture as DrtoolboxLocalServer's committed `llama_config.json` |
| T-01-03 | Tampering | `config/settings.py` path defaults | mitigate | All paths derive from `PROJECT_ROOT` (computed via `os.path.dirname(__file__)`, not user input) with env-var override only — no path is constructed from unsanitized external input in Phase 1 scope |
| T-01-04 | Denial of Service | `load_llama_config()` missing/malformed file | mitigate | Explicit `FileNotFoundError` fail-fast (Task 2) rather than silent empty-dict fallback, so downstream phases (2-7) that depend on this config fail loudly at startup instead of producing confusing runtime errors mid-pipeline |
</threat_model>

<verification>
1. `cd /home/hsu/Desktop/Elcinsurrence && uv run python -c "print('ok')"` — proves uv project executes.
2. `uv run python -c "import elc_audit_engine; from config import settings; print(settings.LLAMA_CPP_BASE_URL)"` — proves package + config both importable, prints `http://localhost:8080`.
3. `uv run --group dev pytest tests/ -v` — all tests pass (4 from Task 3).
4. `find . -maxdepth 2 -type d | grep -E "^\./(config|src|data|tests)$"` — confirms all four top-level convention directories exist matching DrtoolboxLocalServer's layout.
</verification>

<success_criteria>
- `uv run` succeeds at repo root with no errors (pyproject.toml + uv.lock present, dependencies resolved)
- `config.settings` module loads and exposes `LLAMA_CPP_BASE_URL`, `DATA_DIR`, `DB_DIR`, `RAG_DIR`, `OUTPUT_DIR`, `RULE_SOURCE_DIR` with working env-var overrides
- `config/llama_config.json` carries D2-locked values (Ornith-1.0-9B, n_ctx 32768, localhost:8080)
- Directory structure (`config/`, `src/elc_audit_engine/{parsers,rule_repository,record_aggregator,comparator,generators}/`, `data/{db,rag,output}/`, `tests/`) matches DrtoolboxLocalServer's Python+uv+SQLite+PageIndex convention
- 4/4 config tests pass via `uv run --group dev pytest tests/test_config.py -v`, with pytest resolved from the project's own `.venv` (declared as a `dev` dependency-group member in `pyproject.toml`, not an incidental global install)
- `.gitignore` correctly excludes `.venv/`, `__pycache__/`, `.env`, `data/db/*`, `data/rag/*` while keeping `.gitkeep` markers tracked (both use the wildcard+negation pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-skeleton/01-01-SUMMARY.md`
</output>
