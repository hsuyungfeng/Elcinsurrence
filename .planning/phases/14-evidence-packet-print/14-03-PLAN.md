---
wave: 3
depends_on: ["14-02"]
files_modified:
  - server.py
autonomous: true
---

# Plan 14-03: Flask API Route Wiring & Verification

<threat_model>
- **T-14-01 / T-14-02 / T-14-04**: Addressed in upstream plans.
- **T-14-03: safe_filename() 檔名路徑穿越防護 (Path Traversal Defense)**: Uses `safe_filename(case_id, "case_id")` inside `generate_evidence_packet_print` route before processing.
</threat_model>

## Tasks

<task>
<id>14-03-01</id>
<read_first>
- server.py
- .planning/phases/14-evidence-packet-print/14-PATTERNS.md
</read_first>
<action>
Add `POST /api/appeal/evidence-packet/print` route to `server.py`.
Validate JSON request. Fetch `case_id` and sanitize via `safe_filename(case_id, "case_id")`.
Retrieve case payload via `CaseStore`, attachments via `list_attachments(case_id)`.
Call `write_evidence_packet` and return JSON response `{"status": "success", "pdf_url": "/output/...", "warnings": []}`.
Ensure the route is registered in API auth exempt endpoints (`_AUTH_EXEMPT_ENDPOINTS`) if applicable according to Phase 9 rules, but audited via `record_access()`.
Add test covering the API endpoint in `tests/test_evidence_packet_pdf.py`.
</action>
<acceptance_criteria>
- Endpoint `/api/appeal/evidence-packet/print` exists in `server.py`.
- Endpoint sanitizes `case_id` using `safe_filename`.
- Endpoint calls `write_evidence_packet`.
- Integration test in `tests/test_evidence_packet_pdf.py` making a mock POST request returns a 200 JSON response containing `pdf_url` and `warnings`.
- `rtk uv run pytest tests/test_evidence_packet_pdf.py` passes.
</acceptance_criteria>
</task>

## Verification
- Flask endpoint responds correctly to a valid `case_id` request and returns expected JSON payload.

## Must Haves
- REQ-evidence-packet-print: API endpoint functional.
- Path traversal mitigation actively applied on case_id.
