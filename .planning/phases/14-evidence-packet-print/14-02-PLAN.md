---
wave: 2
depends_on: ["14-01"]
files_modified:
  - tests/test_evidence_packet_pdf.py
  - src/elc_audit_engine/generators/evidence_packet/pdf_exporter.py
  - src/elc_audit_engine/generators/evidence_packet/__init__.py
  - src/elc_audit_engine/generators/__init__.py
  - scripts/build_evidence_packet.py
autonomous: true
---

# Plan 14-02: PDF Exporter, PDF Concatenator & CLI Tool

<threat_model>
- **T-14-01 / T-14-02**: Addressed in Plan 14-01.
- **T-14-03: safe_filename() 檔名路徑穿越防護 (Path Traversal Defense)**: The CLI `build_evidence_packet.py` verifies output paths to block `os.pardir` traversal, similar to UAT-07b fixes.
- **T-14-04: 暫存檔隔離與即時清除 (Temp Directory Workspace Isolation)**: `convert_docx_and_merge_pdfs` explicitly leverages `tempfile.TemporaryDirectory()` for intermediate `.docx` files and the headless `soffice` profile directory, ensuring temporary artifacts are wiped after generation.
</threat_model>

## Tasks

<task>
<id>14-02-01</id>
<read_first>
- src/elc_audit_engine/generators/evidence_packet/pdf_exporter.py
- src/elc_audit_engine/generators/evidence_packet/__init__.py
- .planning/phases/14-evidence-packet-print/14-PATTERNS.md
</read_first>
<action>
Implement `convert_docx_and_merge_pdfs` in `src/elc_audit_engine/generators/evidence_packet/pdf_exporter.py` using `subprocess.run` (soffice) and `pypdf.PdfWriter`. Ensure all temp files use `tempfile.TemporaryDirectory()`.
Implement `render_evidence_packet` and `write_evidence_packet` in `src/elc_audit_engine/generators/evidence_packet/__init__.py`.
Export them via `src/elc_audit_engine/generators/__init__.py`.
Create E2E tests in `tests/test_evidence_packet_pdf.py` requiring `soffice`.
</action>
<acceptance_criteria>
- `convert_docx_and_merge_pdfs` executes without error if `soffice` is functional.
- `write_evidence_packet` generates a final PDF with merged attachment pages.
- `rtk uv run pytest tests/test_evidence_packet_pdf.py` passes E2E test verifying a generated PDF exists.
</acceptance_criteria>
</task>

<task>
<id>14-02-02</id>
<depends_on>14-02-01</depends_on>
<read_first>
- scripts/build_evidence_packet.py
- .planning/phases/14-evidence-packet-print/14-PATTERNS.md
</read_first>
<action>
Create CLI `scripts/build_evidence_packet.py` integrating `write_evidence_packet`.
Add path traversal checks on CLI path arguments (`os.pardir` check).
</action>
<acceptance_criteria>
- Running `rtk uv run python scripts/build_evidence_packet.py --help` shows usage.
- Providing an output path with `../` returns a non-zero exit code and prints a path traversal error to stderr.
</acceptance_criteria>
</task>

## Verification
- `rtk uv run pytest tests/test_evidence_packet_pdf.py` executes successfully.
- CLI script rejects unsafe paths.

## Must Haves
- REQ-evidence-packet-print: PDF output must be fully merged with LibreOffice and pypdf.
- Temp artifacts must be securely isolated and deleted.
