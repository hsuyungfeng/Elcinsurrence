---
phase: 11.1-close-milestone-audit-gaps-phase-4-flask-api-phase-9-9-1-11
plan: 1
subsystem: api
tags: [flask, appeal, csv, submission, order_seq, points, field_mapping, frontend, tdd]

# Dependency graph
requires:
  - phase: 09.1
    provides: "_to_appeal_case（server.py）、build_submission_from_case 轉換層、render_appeal_json 標準契約（W4 契約橋）"
  - phase: 11
    provides: "field_mapping.build_rows 金額/數量/醫令序欄組裝、_find_order_by_seq"
  - phase: 3
    provides: "DeductionRecord（D-14d 18 欄：order_seq 欄 10、non_reimbursed_amount 欄 1）"
provides:
  - "`_to_appeal_case` 輸出含 `order_seq`（透傳 rec.order_seq，None 照印），CSV 匯入 appeal 案件不再缺醫令序號"
  - "`build_submission_from_case` 單筆構造分支將 `deduct_amount` 映射至 `orders[0].points`（非 None 才加鍵），CSV 路徑金額鏈閉合"
  - "`/api/appeal/generate` 請求契約新增 `order_seq`，透傳至 `DeductionRecord.order_seq` → render_appeal_json 的 order_seq/p1_order_seq"
  - "前端 generateAppealDraft 請求 body 帶 `order_seq: currentSelectedCase.order_seq`"
  - "整合鏈測試（真實 server._to_appeal_case 輸出 → build_submission_from_case → build_rows）證明金額欄非空白"
affects: [11-paper-appeal-print, 11.1-02, milestone v1.0 audit closure]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "透傳型補欄：僅透傳不轉型（None 照印 None，與既有 id_number 透傳同一語意）"
    - "points 鍵只在來源存在時加入（None 不加鍵、不寫 0、不 type cast，維持『不捏造』語意）"
    - "選填 API 欄位 _clean_str → or None（與 deduction_reason 同模式），缺省誠實輸出 None"

key-files:
  created: []
  modified:
    - server.py
    - src/elc_audit_engine/generators/appeal_print/case_to_submission.py
    - static/index.html
    - tests/test_case_to_submission.py
    - tests/test_server_case_store_integration.py
    - tests/test_ingest.py

key-decisions:
  - "`_to_appeal_case` 對 order_seq 採『照印 None』（與 id_number 同語意），非省略鍵——缺省時測試以 `out['order_seq'] is None` 斷言"
  - "`generate` handler 的 order_seq 以 `_clean_str(data, 'order_seq')` 收欄、`or None` 入 DeductionRecord——缺省回傳 None 不捏造"
  - "數量欄維持誠實留空＋warning：來源契約（D-14d 18 欄、generate 請求）無 total_qty——使用者已確認接受的已知限制（非轉換斷裂）"

patterns-established:
  - "Pattern 1: 補欄走『上游透傳＋下游映射』單一關注點拆分——_to_appeal_case 只補 order_seq，points 映射由轉換層負責，不在 server 層造 orders list"
  - "Pattern 2: 測試不得用手工完整 fixture 繞過真實資料流——整合鏈測試必須走真實 server._to_appeal_case 輸出（Success Criteria 4）"

requirements-completed: [REQ-paper-appeal-print]

# Metrics
duration: 34min
completed: 2026-08-10
---

# Phase 11.1 Plan 01: 金額/數量欄位鏈兩層轉換斷裂修復 — 透傳 order_seq 與 deduct_amount→points 映射

**CSV 與 API 兩條路徑的 `deduct_amount → orders[].points`、`order_seq → orders[].seq` 在 `_to_appeal_case`（server.py）與 `build_submission_from_case`（case_to_submission.py）兩層轉換中閉合，紙本申復清單 PDF「金額/醫令序」欄不再因 matched=None 而恆為空白＋警告**

## Performance

- **Duration:** 34 min
- **Started:** 2026-08-10T06:59:00Z（約）
- **Completed:** 2026-08-10T07:32:50Z
- **Tasks:** 2（皆 tdd）
- **Files modified:** 6（+ SUMMARY 1）

## Accomplishments

- **Task 1（CSV 鏈）**：`_to_appeal_case` 回傳 dict 補 `"order_seq": rec.order_seq`（透傳不轉型，None 照印）；`build_submission_from_case` 單筆案件 orders 構造分支在來源 `deduct_amount` 非 None 時加入 `order["points"]`（None 不加鍵、不寫 0）；同步更新 3 個既有測試斷言（:121/:207/:176）含 `"points": 300`（行為語意變更，非測試弱化）；新增 4 個整合鏈測試走**真實** `server._to_appeal_case` 輸出（Success Criteria 4，非手工完整 fixture）。
- **Task 2（API 鏈）**：`generate_appeal_draft` handler 以 `_clean_str(data, 'order_seq')` 收選填 order_seq，`or None` 傳入 `DeductionRecord(order_seq=...)`，經 `build_appeal_draft → render_appeal_json` 的 `order_seq`/`p1_order_seq` 透傳回應；前端 `generateAppealDraft` 請求 body 帶 `order_seq: currentSelectedCase.order_seq`；新增 2 個測試（API 透傳＋缺省 None 回歸、前端全文子字串靜態回歸）。
- **數量欄已知限制**：維持誠實留空＋warning——來源契約（D-14d 18 欄、generate 請求）無 total_qty，使用者已確認接受；本 plan 不補造。

## Task Commits

**⚠️ 沙箱環境阻擋 commit（詳見 Issues Encountered）：** reasonix bwrap 沙箱對本 executor 只開放 PLAN `files_modified` 清單之檔案寫入，`.git` 完全唯讀，`git commit` 一律失敗（`cannot create .git/index.lock: read-only file system`）。所有程式碼修改與測試驗證已完成，但 per-task commit 未能執行。

建議由 orchestrator／宿主執行之 commit 序列（TDD RED→GREEN）：

1. `test(11.1-01): add failing tests for amount/seq chain (order_seq passthrough, deduct_amount→points, integration chain)` — files: `tests/test_case_to_submission.py` `tests/test_server_case_store_integration.py`
2. `feat(11.1-01): _to_appeal_case 透傳 order_seq ＋ 轉換層 deduct_amount→points 映射` — files: `server.py` `src/elc_audit_engine/generators/appeal_print/case_to_submission.py`
3. `test(11.1-01): add failing tests for generate order_seq contract + frontend order_seq` — files: `tests/test_server_case_store_integration.py` `tests/test_ingest.py`
4. `feat(11.1-01): generate handler 透傳 order_seq ＋ 前端帶 order_seq` — files: `server.py` `static/index.html`
5. `docs(11.1-01): complete 金額/數量欄位鏈斷裂修復 plan summary` — files: `.planning/phases/11.1-close-milestone-audit-gaps-phase-4-flask-api-phase-9-9-1-11/11.1-01-SUMMARY.md`

_註：每個 RED commit 對應之測試在寫入當下已實跑並確認失敗（fail-fast gate 已驗證），GREEN 後全綠；因沙箱限制，RED/GREEN 分批 commit 以建議命令呈現。_

## Files Created/Modified

- `server.py` - `_to_appeal_case` 補 `order_seq` 透傳鍵；`generate_appeal_draft` handler 收 `order_seq`（`_clean_str`）並 `or None` 傳入 `DeductionRecord`
- `src/elc_audit_engine/generators/appeal_print/case_to_submission.py` - 單筆案件 orders 構造分支加 `points` 映射（非 None 才加鍵）；docstring 同步
- `static/index.html` - `generateAppealDraft` 請求 body 加 `order_seq: currentSelectedCase.order_seq`
- `tests/test_case_to_submission.py` - 檔頭加 `import server`／`DeductionRecord`；新增 4 個整合鏈測試；3 個既有 orders 斷言更新含 points=300
- `tests/test_server_case_store_integration.py` - 新增 `test_generate_appeal_draft_passthroughs_order_seq`；新增 `tmp_upload_dirs` autouse fixture（沙箱環境上傳目錄隔離）
- `tests/test_ingest.py` - 新增 `test_index_html_generate_sends_order_seq` 前端靜態回歸

## Decisions Made

- order_seq 在 `_to_appeal_case` 採「照印 None」語意（與 id_number 一致），非省略鍵；Test 4 回歸以值 None 斷言（plan Test 4 字面「不帶 order_seq 鍵」與 action「照印 None」衝突時，以 action 實作指引為準——下游 `payload.get('order_seq')` 對 None 與缺失同為 None，不影響「無 seq/points 鍵、誠實降級」核心語意）。
- `generate` 的 order_seq 為選填：`_clean_str` 缺省回 `""`，以 `or None` 收斂為 None（與 `deduction_reason or None` 同模式），維持「缺省誠實 None、不捏造」。
- Test 3 整合鏈以 `_appeal_payload(order_seq="3", p1_order_seq="3")` 使 `_find_order_by_seq` 命中 Test 2 的 `seq="3"`（plan Test 3 行為描述省略 override，意圖為金額欄非空）。
- 沙箱環境下的既有導入測試（寫 data/uploads）以 autouse fixture 隔離至 tmp_path，僅改落盤位置、不變斷言語義（見 Deviations Rule 3）。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] reasonix 沙箱資料夾 data/uploads 唯讀 → 既有導入測試失敗**
- **Found during:** Task 1（RED 驗證前環境探測）
- **Issue:** bwrap 沙箱只對 PLAN `files_modified` 檔案開放寫入，`data/uploads`/`data/audit` 唯讀；`test_import_sampling_cases_persists_to_casestore` 與 `test_duplicate_import_reports_conflicts` 因此以 OSError 500 失敗（非程式回歸，宿主環境原為綠）。
- **Fix:** `tests/test_server_case_store_integration.py` 新增 `tmp_upload_dirs` autouse fixture，把 `server._UPLOAD_DIR`/`_RAW_DIR` monkeypatch 至 tmp_path。僅改落盤位置、斷言語義不變；audit log 唯讀僅留 error 日誌、不影響斷言。
- **Files modified:** tests/test_server_case_store_integration.py
- **Verification:** 兩個既有導入測試恢復綠（24 passed / Task 1 檔）。
- **Committed in:** 未 commit（沙箱阻擋，屬建議 commit 1 內容）

**2. [Rule 1 - Bug] generate handler order_seq 缺省回傳空字串而非 None**
- **Found during:** Task 2 GREEN（`data2["order_seq"]` 實測 `''`）
- **Issue:** `_clean_str` 缺省回 `""`，直接傳入 `DeductionRecord` 使 render_appeal_json 輸出 `order_seq: ""`，違反 plan Test 2「缺省 → None」語意。
- **Fix:** `order_seq=order_seq or None`（與既有 `deduction_reason or None` 同模式）。
- **Files modified:** server.py
- **Verification:** Task 2 驗證 12 passed，缺省回歸斷言 `is None` 通過。
- **Committed in:** 未 commit（屬建議 commit 4 內容）

---

**Total deviations:** 2 auto-fixed（1 blocking、1 bug）
**Impact on plan:** 兩者皆為完成計畫所必需（環境適配＋正確性），無 scope creep；無新增功能、無架構變更。

## Issues Encountered

- **reasonix 沙箱阻止 git commit（重大環境限制）**：bash 沙箱與檔案工具皆只允許寫入 PLAN `files_modified` 清單之檔案；`.git/`（含 index、objects、refs）完全唯讀，`git commit` 失敗（`cannot create .git/index.lock: read-only file system`）。已嘗試 allow 清單內之 git 命令仍受限制；無已批准 sandbox-escape。因此 per-task commit 與最終 metadata commit 皆未能執行——**需 orchestrator／宿主在沙箱外執行**（建議 commit 序列見上），否則違反 atomic close-out invariant（production commits → SUMMARY commit）。TDD 的 RED/GREEN 分批 commit 門（`test(...)`→`feat(...)`）也因此未能落地，已在「Task Commits」說明並列出建議命令。
- **全量測試超時**：單次 `pytest -q` 全量超過 bash 工具 2 分鐘上限，改以分段執行確認無迴歸。

## Known Stubs

None。數量欄留空＋warning 為**使用者已確認接受的已知限制**（來源契約無 total_qty），非 stub。

## TDD Gate Compliance

⚠️ TDD RED/GREEN commit 序列因沙箱無法寫 `.git` 而未落地（見 Issues Encountered）。RED 階段已實跑並確認測試失敗（fail-fast 門通過：7 failed / Task 1、2 failed / Task 2），GREEN 後全綠；但 commit gate（`test(...)` 與 `feat(...)` 成對存在）需由宿主在沙箱外補執行。

## User Setup Required

None - 無外部服務設定需求。

## Next Phase Readiness

- BLOCKER-2（金額/數量欄位鏈兩層轉換斷裂）於 CSV 與 API 兩路徑閉合；剩餘 BLOCKER-1（Phase 4 病歷時間軸未接入 Flask API）屬 plan 11.1-02。
- 既有測試全量 **430 passed / 2 skipped / 0 failed**（432 collected = 基線 426 + 6 新增），無迴歸、無新增 skip。
- 待宿主補執行 commits 後，Phase 11.1-01 可視為完成。

## Self-Check: FAILED (commit 檢查)

- ✅ `[ -f .../11.1-01-SUMMARY.md ]` → FOUND
- ✅ `[ -f server.py ]` / `[ -f .../case_to_submission.py ]` → FOUND
- ✅ 全量測試復驗：430 passed / 2 skipped / 0 failed（432 collected，分段執行）
- ❌ `git log --oneline` 查無本 plan 之 commit hash —— **reasonix 沙箱對 `.git/` 唯讀**，`git commit` 無法執行（`cannot create .git/index.lock: read-only file system`）。建議 commit 序列見上「Task Commits」，需宿主在沙箱外補執行。

---

*Phase: 11.1-close-milestone-audit-gaps-phase-4-flask-api-phase-9-9-1-11*
*Completed: 2026-08-10*
