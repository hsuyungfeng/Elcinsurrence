---
phase: 11.1-close-milestone-audit-gaps-phase-4-flask-api-phase-9-9-1-11
plan: 2
type: execute
wave: 2
depends_on: ["11.1-01"]
files_modified:
  - server.py
  - config/settings.py
  - static/index.html
  - tests/test_server_case_store_integration.py
  - tests/test_ingest.py
autonomous: true
requirements: [REQ-record-aggregator]
gap_closure: true

must_haves:
  truths:
    - "RECORDS_DIR 存在且請求帶病歷號時，/api/sampling/audit 以 LocalFileProvider 實查半年病史並把 timeline 傳入 run_presubmission_check（不再寫死 None），回應 records_degraded=false 且附病歷來源標記"
    - "/api/appeal/generate 帶 record_no 時（record_no 來自前端 appeal 面板病歷號輸入欄——plan-checker BLOCKER 2 使用者裁示，可選填），以 build_timeline 把 timeline 傳給 build_appeal_draft（②醫療必要性含半年病史），回應含 records_degraded 補充鍵"
    - "病患無病歷（PatientRecordsNotFound）→ 維持 C5 降級（timeline=None、comparator records_degraded=True）但回應標記『已查詢、病患缺席』，與『未設定病歷來源』（RECORDS_DIR 不存在）在回應與前端均可區分"
    - "前端對 records_degraded 有可見呈現（sampling 徽章區與 appeal 面板，含原因文案）——不再是『從未嘗試查詢』的無標記狀態（Success Criteria 2）"
  artifacts:
    - path: "config/settings.py"
      provides: "RECORDS_DIR（env 覆寫，預設 DATA_DIR/samples/records，比照 FACILITY_CONFIG_PATH 模式）"
    - path: "server.py"
      provides: "_records_provider()（RECORDS_DIR 存在→LocalFileProvider）＋audit/generate 兩端點 timeline 接線＋records_degraded/records_degraded_reason 回應鍵"
    - path: "static/index.html"
      provides: "records_degraded 可見呈現（sampling 徽章區＋appeal 面板）＋appeal 面板病歷號輸入欄（aRecordNoInput）＋sampling 請求帶 record_no"
  key_links:
    - from: "server.py _records_provider()"
      to: "elc_audit_engine.record_aggregator（LocalFileProvider/build_timeline）"
      via: "RECORDS_DIR 目錄存在→provider；build_timeline(provider, record_no) 於兩端點呼叫"
    - from: "/api/sampling/audit request record_no（d3 病歷號）"
      to: "run_presubmission_check(case, soap_doc, timeline)"
      via: "build_timeline(provider, record_no).timeline"
    - from: "前端 appeal 面板病歷號輸入欄（aRecordNoInput）"
      to: "build_appeal_draft(record, timeline=timeline)"
      via: "generateAppealDraft 請求 body record_no → build_timeline(provider, record_no).timeline"
    - from: "audit/generate 回應 records_degraded"
      to: "static/index.html 呈現"
      via: "if (data.records_degraded) → 顯示 records_degraded_reason 文案"
---

<objective>
修復 BLOCKER-1「病歷時間軸（RecordProvider/LocalFileProvider）從未接入 Flask API」：全庫僅 record_aggregator 自身模組與其測試具現化 Provider，`server.py:500` 與 `/api/appeal/generate` 路徑皆以 `timeline=None` 呼叫比對引擎，生產環境每次事前預審恆處於「病歷缺席降級」（records_degraded=True）、只查當次 SOAP。

Purpose: 讓 Phase 4 交付的 `LocalFileProvider`/`build_timeline` 在真實服務路徑被具現化並傳入半年病史時間軸（Success Criteria 1）；同時補前端對病歷缺席降級狀態的可見呈現（Success Criteria 2）。**generate 路徑的病歷號入口＝前端 appeal 面板新增的可選病歷號輸入欄（plan-checker BLOCKER 2 使用者裁示）**——CSV 匯入案件的 payload.record_no 恆 None（D-14d 18 欄無病歷號欄），輸入欄是該端點 timeline 查詢的唯一真實來源：有值才查，無值誠實標記 no_record_no。病歷缺席（PatientRecordsNotFound）維持既有 C5 降級語意，但回應與前端明確區分「已查詢、病患缺席」與「病歷來源未設定」——不得偽裝成「從未嘗試查詢」。infra 故障（RecordProviderError，如 JSON 損毀）依 P0-2 原則向外穿透（500），不得降級成業務結論。
Output: `settings.RECORDS_DIR`＋server 端 `_records_provider()`＋audit/generate 兩端點 timeline 接線＋records_degraded/records_degraded_reason 回應鍵＋前端呈現＋appeal 面板病歷號輸入欄＋端到端測試（前端→API 請求體真實路徑）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.0-MILESTONE-AUDIT.md（frontmatter gaps.integration BLOCKER-1 完整證據鏈）
@.planning/ROADMAP.md（Phase 11.1 Success Criteria 1/2/4）

<!-- 介面合約（已由讀碼核實，executor 不需再探索） -->
<interfaces>
src/elc_audit_engine/record_aggregator/：
- `RecordProvider` ABC（providers.py:43-68）：`name` property＋`fetch_records(patient_id) -> list[Record]`；`PatientRecordsNotFound`（:35-40，正常降級情境）與 `RecordProviderError`（:27-32，infra 故障）皆為 `RecordProviderError` 階層
- `LocalFileProvider(root_dir)`（providers.py:110-169）：讀 `<root_dir>/<patient_id>/records.json`（四類紀錄 visits/labs/exams/imaging）；病患目錄不存在 → PatientRecordsNotFound；JSON 損毀/格式錯誤 → RecordProviderError；patient_id 內部經 `safe_filename` 校驗（P1-3 防線）
- `build_timeline(provider, patient_id, *, months=6, end_date=None) -> AggregationResult`（aggregator.py:84-134）：`PatientRecordsNotFound` 已內捕並回傳 `AggregationResult(timeline=None, degraded=True, reason=...)`；`RecordProviderError` 向外拋（不降級）
- `record_aggregator/__init__.py` 對外暴露 `LocalFileProvider`/`build_timeline`/`AggregationResult`（tests/test_record_aggregator.py:14-22 既有 import 路徑）

config/settings.py：
- 既有 env 覆寫模式：`DATA_DIR = os.getenv("DATA_DIR", ...)`（:9）；`FACILITY_CONFIG_PATH = os.getenv("FACILITY_CONFIG_PATH", os.path.join(PROJECT_ROOT, "config/facility.json"))`（:21-23）——本 plan 新增 `RECORDS_DIR` 比照此模式

server.py：
- `_clean_str(data, key, *, required=False, max_len=_MAX_FIELD_CHARS)`（:230-239）
- `/api/sampling/audit`（:471-552）：request 欄位 order_code/order_name/soap_text/record_no/case_id（record_no 已讀取，:489）；`SubmissionCase(record_no=record_no, orders=(OrderRecord(code=order_code),))`（:492-495）；`result = run_presubmission_check(case, soap_doc, None)`（:500，寫死 None——本次修改點）；try 僅捕 `RuleRepositoryError`（:501-504）；回應含 `"records_degraded": result.comparison.records_degraded`（:551）
- `/api/appeal/generate`（:708-797）：欄位 case_seq/order_code/deduction_reason/case_id/is_appealing/deduct_amount/claimed_points；`DeductionRecord(...)`（:744-749）；`draft = build_appeal_draft(record, is_appealing=..., claimed_points=..., rule_text=..., rule_location=..., evidence=..., has_attachment=...)`（:769-777，未傳 timeline）；回應 `payload = json.loads(render_appeal_json(draft))` 併入 status/case_id/rule_found 三補充鍵（:791-797）
- `build_appeal_draft` 簽名（appeal.py:346-356）：`timeline: PatientTimeline | None = None` 既有參數（:351）；`build_necessity(timeline)`（:133-163）：None →「（病歷缺席，無半年病史可資佐證）」；有值 →「半年病史（...）：就診 N 次、檢驗 N 筆…」
- import 區（:15-58）目前無 `config`/`record_aggregator` import——本 plan 新增

static/index.html：
- `runSamplingAudit()`（:468-510）：請求 body（:473-479）目前無 record_no；回應處理（:481-509）只處理 support_level 四態＋undetermined，無 records_degraded 呈現
- appeal 面板 top bar（:253-263）：`aOrderName`（:256）、`aDeductionReason`（:260）——本 plan 在此右側 div（:258-261）新增病歷號輸入欄（id=`aRecordNoInput`）
- `generateAppealDraft()`（:528-571）：請求 body（:533-543）；回應處理（:545-570）消費 sections/p8/p9/rule_found
- 呈現慣例：badge/`innerText` 賦值（P1-5 防線：不得 innerHTML 拼接 API 欄位）

tests/：
- `test_server_case_store_integration.py`：`setup_tmp_casestore`（:13-19）＋`client`（:22-27，ELC_API_KEYS={"valid-key-123": "clinic_a"}）fixtures；既有 audit/generate 測試（:124-212）
- `test_ingest.py:197-213`：前端靜態回歸測試模式（`Path("static/index.html").read_text()`＋全文子字串斷言）
- `tests/fixtures/records/patient_A/records.json`：LocalFileProvider 契約 fixture（test_record_aggregator.py:25-26 既有參照）
- conftest.py 已設 `ELC_API_KEYS` 且 PROJECT_ROOT 在 sys.path（`from config import settings` 可用）
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RECORDS_DIR 設定 ＋ _records_provider() ＋ /api/sampling/audit timeline 接線 ＋ 測試</name>
  <files>config/settings.py, server.py, tests/test_server_case_store_integration.py</files>

  <read_first>
    - config/settings.py（:7-23 env 覆寫模式——DATA_DIR :9、FACILITY_CONFIG_PATH :21-23）
    - src/elc_audit_engine/record_aggregator/providers.py（:110-169 LocalFileProvider、:27-40 例外階層）
    - src/elc_audit_engine/record_aggregator/aggregator.py（:84-134 build_timeline：PatientRecordsNotFound 內捕降級、RecordProviderError 外拋）
    - server.py（:15-58 import 區、:471-552 audit_sampling_case 端點）
    - tests/test_server_case_store_integration.py（:13-27 fixtures、:124-157 audit 既有測試）
  </read_first>
  <behavior>
    - Test 1: monkeypatch `settings.RECORDS_DIR` 到含 `tmp_path/<patient_id>/records.json`（LocalFileProvider 契約，比照 tests/fixtures/records/patient_A）的目錄，audit 請求帶 `record_no=<patient_id>` → 回應 `records_degraded is False`、`records_source == "ok"`、`records_degraded_reason is None`
    - Test 2: 病患缺席（RECORDS_DIR 存在但無該病歷號目錄）→ 回應 `records_degraded is True`、`records_source == "absent"`（已查詢、C5 降級）
    - Test 3: RECORDS_DIR 不存在 → 回應 `records_degraded is True`、`records_source == "unconfigured"`（未嘗試查詢，不得偽裝成 absent）
    - Test 4（回歸）: 不帶 record_no（RECORDS_DIR 存在）→ `records_degraded is True`、`records_source == "no_record_no"`
    - Test 5: records.json 損毀（infra 故障）→ 回應 500（`status == "error"`），不得降級成業務結論（RecordProviderError 穿透）
  </behavior>
  <action>1) config/settings.py：新增 `RECORDS_DIR = os.getenv("RECORDS_DIR", os.path.join(DATA_DIR, "samples", "records"))`（比照 FACILITY_CONFIG_PATH 的 env 覆寫模式，:21-23 附近；用既有 DATA_DIR 變數，:9）。

  2) server.py import 區（:15-58）新增 `from config import settings` 與 `from elc_audit_engine.record_aggregator import LocalFileProvider, build_timeline`（record_aggregator 對外暴露路徑，見 tests/test_record_aggregator.py:14-22）。

  3) server.py 新增模組級 helper（放在 `_persist_cases`/`_to_appeal_case` 附近）：
  `def _records_provider() -> LocalFileProvider | None:`——`os.path.isdir(settings.RECORDS_DIR)` 為真 → 回傳 `LocalFileProvider(settings.RECORDS_DIR)`；否則回傳 None（代表「病歷來源未設定」，不是「病患缺席」）。

  4) `/api/sampling/audit`（:492-504）：以「Provider 具現化＋timeline 解析」取代寫死的 `run_presubmission_check(case, soap_doc, None)`。邏輯順序：
  - `provider = _records_provider()`；`timeline = None`；`records_source = "unconfigured"`
  - `provider is not None and record_no` 成立時：`agg = build_timeline(provider, record_no)`（PatientRecordsNotFound 已被 aggregator 內捕為 degraded=True，不拋）→ `timeline = agg.timeline`；`records_source = "absent" if agg.degraded else "ok"`
  - `provider is not None` 但 `record_no` 空 → `records_source = "no_record_no"`（有來源但請求未帶病歷號＝未嘗試查詢）
  - `result = run_presubmission_check(case, soap_doc, timeline)`——**不得**再以字面 None 呼叫
  - 既有 `except RuleRepositoryError`（:501-504）維持；**不得新增** `except RecordProviderError`——infra 故障依 P0-2 穿透至統一 500 脫敏（errorhandler :223-227），不吞成業務結論

  5) audit 回應（:529-552）新增兩鍵：`"records_source": records_source`；`"records_degraded_reason"`——僅 `records_degraded`（:551）為 True 時為中文原因（`unconfigured`→「病歷來源未設定（病歷目錄不存在）」；`no_record_no`→「請求未提供病歷號」；`absent`→「查無此病患病歷檔案」），否則 None。原因文案不含路徑細節（T-1112-01）。

  6) tests/test_server_case_store_integration.py 新增測試（沿用 `client`/`setup_tmp_casestore` fixtures；`monkeypatch.setattr(settings, "RECORDS_DIR", str(tmp_path / "records"))`，`from config import settings`）：Test 1-5 對應上述 behavior。records.json fixture 內容：`{"visits": [{"date": "2026-07-01", "clinic": "內科", "soap_text": "..."}]}`（最小契約即可）。audit 請求 body 沿用既有 `test_audit_sampling_case_with_optional_case_id`（:133-137）並加 `record_no`；斷言只鎖定 `status`/`records_degraded`/`records_source`/`records_degraded_reason`/HTTP code，**不得**依賴判定值（LLM 可用性無關）。Test 5 用 `with pytest.raises(...)` 或直接斷言 `resp.status_code == 500`＋`resp.get_json()["status"] == "error"`。
  </action>
  <verify>
    <automated>./.venv/bin/pytest tests/test_server_case_store_integration.py -q</automated>
  </verify>
  <done>RECORDS_DIR 存在且帶 record_no 時，audit 端點以 LocalFileProvider 實查並傳 timeline（records_degraded=false）；absent/unconfigured/no_record_no 三態在回應可區分；RecordProviderError 穿透 500；既有 audit 測試維持綠。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /api/appeal/generate timeline 接線（record_no 可選欄位）＋ 前端 appeal 面板病歷號輸入欄（plan-checker BLOCKER 2 使用者裁示） ＋ records_degraded 補充鍵 ＋ 測試</name>
  <files>server.py, static/index.html, tests/test_server_case_store_integration.py, tests/test_ingest.py</files>

  <read_first>
    - server.py（:708-749 generate_appeal_draft 開頭、:769-777 build_appeal_draft 呼叫、:791-797 回應補充鍵）
    - src/elc_audit_engine/generators/appeal.py（:346-356 build_appeal_draft 簽名含 timeline 參數、:133-163 build_necessity 降級/正常文字）
    - static/index.html（:253-263 appeal 面板 top bar、:528-571 generateAppealDraft 請求與回應）
    - tests/test_server_case_store_integration.py（:160-212 既有 generate 測試斷言模式）
    - tests/test_ingest.py（:197-213 前端靜態回歸測試模式）
  </read_first>
  <behavior>
    - Test 1（真實路徑，plan-checker BLOCKER 2 裁示）: monkeypatch `settings.RECORDS_DIR` 到含 `tmp_path/<patient_id>/records.json` 的目錄；前端 appeal 面板病歷號輸入欄（aRecordNoInput）填入 patient_id 後，generateAppealDraft 送出請求 body 含 `record_no=<patient_id>`（前端輸入欄→API 請求體的真實路徑）→ 回應 `records_degraded is False`、`records_source == "ok"`、`sections[1]["text"]` 含「半年病史」（②醫療必要性為正常摘要而非「病歷缺席」降級文字）
    - Test 2: 病歷號輸入欄留空（請求不帶 record_no，RECORDS_DIR 存在）→ 回應 `records_degraded is True`、`records_source == "no_record_no"`、`sections[1]["text"]` 含「病歷缺席」
    - Test 3: 病患缺席 → `records_degraded is True`、`records_source == "absent"`
    - Test 4（回歸）: 帶 record_no 但 RECORDS_DIR 不存在 → `records_degraded is True`、`records_source == "unconfigured"`；render_appeal_json 契約既有鍵（sections/word_stats/p1-p9）不受影響
    - Test 5（前端靜態回歸）: static/index.html 含病歷號輸入欄 `id="aRecordNoInput"` 且 generateAppealDraft 請求 body 為 `record_no: document.getElementById('aRecordNoInput').value`（全文子字串斷言）
  </behavior>
  <action>1) server.py generate handler（:717-749）：新增 `record_no = _clean_str(data, 'record_no')`（選填，病歷號＝Phase 3 d3＝`LocalFileProvider` 的 patient_id）——放在既有 `case_id`（:724）附近。

  2) `build_appeal_draft` 呼叫前（:744-777 之間）加入與 Task 1 相同的 timeline 解析（共用 `_records_provider()` helper）：
  - `provider = _records_provider()`；`timeline = None`；`records_source = "unconfigured"`
  - `provider is not None and record_no` → `agg = build_timeline(provider, record_no)`；`timeline = agg.timeline`；`records_source = "absent" if agg.degraded else "ok"`
  - `provider is not None` 但 `record_no` 空 → `records_source = "no_record_no"`
  - `draft = build_appeal_draft(record, ..., timeline=timeline)`（appeal.py:351 既有參數；其餘參數不變，含 order_seq 透傳——11.1-01 Task 2 已補）

  3) 回應段（:791-797）在 status/case_id/rule_found 三個補充鍵之後新增：`payload["records_degraded"] = timeline is None` 與 `payload["records_degraded_reason"]`（僅 degraded 時依 records_source 給中文原因，文案同 Task 1，不含路徑細節）；`payload["records_source"] = records_source`。**不得**刪除或改名 render_appeal_json 既有任何鍵。

  4) 前端 static/index.html（plan-checker BLOCKER 2 使用者裁示：appeal 面板加可選病歷號輸入欄——CSV 匯入案件的 payload.record_no 恆 None，此輸入欄是 generate 端點 timeline 查詢的唯一真實病歷號入口）：
  - appeal 面板 top bar 右側 div（:258-261，`aDeductionReason` 旁）新增 `<input type="text" id="aRecordNoInput" ...>`（前方 label「病歷號（選填，查半年病史）」；placeholder 示例如「M220518024」；樣式沿用頁面既有 input class 或 inline style，與 top bar 一致）
  - `generateAppealDraft` 請求 body（:533-543）的 record_no 改為 `record_no: document.getElementById('aRecordNoInput').value`（取代原計劃的 `currentSelectedCase.record_no`——該值恆 None）；輸入欄可選填：有值才查半年病史，空字串送出後由 server 端標記 no_record_no（誠實降級）
  - 輸入欄一律以屬性/`innerText` 呈現，不引入 innerHTML 拼接 API 欄位（P1-5 防線）

  5) tests：
  - tests/test_server_case_store_integration.py 新增 `test_generate_appeal_draft_timeline_injection`（Test 1-4）：沿用 `client`/`setup_tmp_casestore`；generate 請求 body 沿用既有（:170-175）並加 `record_no`——**請求體即前端 `generateAppealDraft` 自 `aRecordNoInput` 輸入欄送出的內容**（配合 Test 5 的靜態回歸鎖定前端取值，共同證明「前端→API 請求體」真實路徑，非另行手動構造的請求形狀）；monkeypatch `settings.RECORDS_DIR`。斷言 `sections[1]["text"]`（醫療必要性段，KEY_NECESSITY 為第二段）的內容關鍵字——正常含「半年病史」、降級含「病歷缺席」。既有 `test_generate_appeal_draft_with_optional_case_id`（:160-212）不得改動（其不帶 record_no，走 degraded 分支，既有斷言全數維持）。
  - tests/test_ingest.py 新增 `test_index_html_appeal_record_no_input`（Test 5 靜態回歸，比照 :197-213 模式）：`Path("static/index.html").read_text()` 斷言 `id="aRecordNoInput"` 與 `record_no: document.getElementById('aRecordNoInput').value` 在場。
  </action>
  <verify>
    <automated>./.venv/bin/pytest tests/test_server_case_store_integration.py tests/test_ingest.py::test_index_html_appeal_record_no_input -q</automated>
  </verify>
  <done>generate 帶 record_no（前端輸入欄送出）時 timeline 真實傳入（sections[1] 含「半年病史」）；records_degraded/records_source/records_degraded_reason 三鍵在四態（ok/absent/unconfigured/no_record_no）正確；appeal 面板有病歷號輸入欄（aRecordNoInput）且 generate 請求自輸入欄取 record_no（plan-checker BLOCKER 2 使用者裁示）；render_appeal_json 契約既有鍵不變；既有 generate 測試全綠。
  </done>
</task>

<task type="auto">
  <name>Task 3: 前端 records_degraded 可見呈現 ＋ sampling 請求帶 record_no ＋ 靜態回歸測試</name>
  <files>static/index.html, tests/test_ingest.py</files>

  <read_first>
    - static/index.html（:468-510 runSamplingAudit 請求與回應處理、:528-571 generateAppealDraft 請求與回應處理、:512-526 selectAppealCase 面板填充）
    - tests/test_ingest.py（:174-213 前端靜態回歸測試模式）
  </read_first>
  <behavior>
    - Test 1（前端靜態回歸）: static/index.html 含 `data.records_degraded`、`data.records_degraded_reason`、sampling 請求 `record_no: currentSelectedCase.record_no`（generate 請求 record_no 由 Task 2 的 aRecordNoInput 輸入欄負責，見 Task 2 Test 5）、`病歷缺席`（全文子字串斷言）
  </behavior>
  <action>1) static/index.html `runSamplingAudit()`：
  - 請求 body（:473-479）加入 `record_no: currentSelectedCase.record_no,`（d3 病歷號＝patient_id；sampling 案件 payload 自 CSV 匯入含 record_no，見 server.py:369）
  - 回應處理（:481-509）新增 records_degraded 呈現：在既有 badge 邏輯之後，`if (data.records_degraded)` → 於 `sAdviceBox` 前置一行「⚠ 病歷缺席：${data.records_degraded_reason || '未含半年病史佐證'}」（用 `innerText` 賦值，**不得** innerHTML 拼接 API 欄位——P1-5 防線）；records_degraded=false 時維持現有內容

  2) static/index.html `generateAppealDraft()` 回應處理（:545-570）新增 records_degraded 呈現：`if (data.records_degraded)` → 在 `aSec2`（醫療必要性 box）內容前加上「⚠ 病歷缺席：${data.records_degraded_reason || '本次申復未含半年病史佐證'}\n」；`data.records_degraded_reason` 為 None 時只用預設文案（防呆，不顯示 undefined）。**不在此改 generate 請求 body 的 record_no**——generate 請求的 record_no 由 Task 2 的病歷號輸入欄（aRecordNoInput）負責，本 task 不得重複加入（避免兩處寫入衝突）。

  3) tests/test_ingest.py 新增 `test_index_html_renders_records_degraded`（比照 :197-213 模式，`Path("static/index.html").read_text()`）：斷言四個子字串——`data.records_degraded`、`data.records_degraded_reason`、`record_no: currentSelectedCase.record_no`（sampling 請求送出，`.count(...) >= 1`；generate 請求的 record_no 走 Task 2 的 aRecordNoInput 輸入欄，不在本斷言範圍）、`病歷缺席`。
  </action>
  <verify>
    <automated>./.venv/bin/pytest tests/test_ingest.py::test_index_html_renders_records_degraded -q</automated>
  </verify>
  <done>前端 sampling 與 appeal 兩面板對 records_degraded 有可見呈現（含原因文案，innerText 非 innerHTML）；sampling 請求帶 record_no（generate 請求 record_no 由 Task 2 的 aRecordNoInput 輸入欄負責）；靜態回歸測試鎖定；既有 test_ingest.py 全綠。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API（/api/sampling/audit、/api/appeal/generate 的 record_no；appeal 面板病歷號輸入欄 aRecordNoInput） | 未信任輸入（病歷號）跨入；`LocalFileProvider._records_path` 以 patient_id 組檔名（P1-3 讀取型路徑穿越面） |
| API→LocalFileProvider→records.json | 本機病歷檔讀取；JSON 損毀＝infra 故障（RecordProviderError） |
| server→前端（records_degraded_reason） | 降級原因回傳；不得含內部路徑/PHI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1112-01 | Information Disclosure | `records_degraded_reason` 回傳前端 | mitigate | 文案為固定中文模板（unconfigured/no_record_no/absent 三態），不含 RECORDS_DIR 路徑、不含病歷號；`errorhandler(Exception)`（server.py:223-227）對 RecordProviderError 統一脫敏 |
| T-1112-02 | Tampering | `record_no` 路徑穿越（讀取型） | mitigate | `LocalFileProvider._records_path` 內建 `safe_filename(patient_id, "patient_id")` 校驗後拒絕（providers.py:136-139，P1-3 既有防線）；server 層以 `_clean_str` 限制長度，不額外做路徑操作 |
| T-1112-03 | Elevation of Privilege | infra 故障被吞成業務結論 | mitigate | 不新增 `except RecordProviderError`——JSON 損毀穿透至 500（P0-2 原則：系統故障不得偽裝成「查無規則/病患缺席」）；`build_timeline` 僅內捕 `PatientRecordsNotFound`（正常降級情境，aggregator.py:109-116） |
| T-1112-04 | Denial of Service | 每次 audit 讀 records.json | accept | 單檔單病患、本機讀取，半年窗過濾在記憶體完成（aggregator `_split_by_window`）；無外部佇列/遠端呼叫 |
</threat_model>

<verification>
./.venv/bin/pytest tests/test_server_case_store_integration.py tests/test_ingest.py::test_index_html_renders_records_degraded -q
（基線 426 collected / 2 skipped；本 plan 只新增/修改上述測試，不得引入 skip 或失敗；soffice/llama.cpp 依賴的既有 skip 不受影響）
</verification>

<success_criteria>
- `settings.RECORDS_DIR` 存在（env 可覆寫），server 端 `_records_provider()` 依目錄存在與否回傳 Provider 或 None
- `/api/sampling/audit` 與 `/api/appeal/generate` 均不再以字面 `timeline=None` 呼叫——帶病歷號且來源存在時傳入真實半年病史時間軸（Success Criteria 1）
- 回應含 `records_source`（ok/absent/unconfigured/no_record_no）與 `records_degraded_reason`；「已查詢、病患缺席」與「未設定病歷來源」可區分（Success Criteria 2 的誠實語意）
- `RecordProviderError` 穿透 500，不降級為業務結論
- 前端兩面板對 records_degraded 有可見呈現（含原因文案）；sampling 請求帶 record_no；appeal 面板病歷號輸入欄（aRecordNoInput）可選填，generate 請求自輸入欄取 record_no（plan-checker BLOCKER 2 使用者裁示）
- 新增/修改測試覆蓋 server 端真實 Provider 注入路徑（非手工 mock 掩蓋），全套件回歸綠燈（Success Criteria 4）
</success_criteria>

<output>
完成後建立 `.planning/phases/11.1-close-milestone-audit-gaps-phase-4-flask-api-phase-9-9-1-11/11.1-02-SUMMARY.md`
</output>
