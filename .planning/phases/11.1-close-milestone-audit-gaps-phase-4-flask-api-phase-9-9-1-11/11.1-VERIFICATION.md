---
phase: 11.1-close-milestone-audit-gaps-phase-4-flask-api-phase-9-9-1-11
verified: 2026-08-10T07:54:22Z
status: passed
score: 12/12 must-haves verified
overrides_applied: 0
---

# Phase 11.1: Close milestone audit gaps (INSERTED) Verification Report

**Phase Goal:** 修復第二輪 `/gsd-audit-milestone`（2026-08-10）發現的 2 個 critical blocker：(1) Phase 4 `RecordProvider`/`LocalFileProvider` 接入 Flask API 的事前預審與申復生成路徑（取代寫死的 `timeline=None`）；(2) 修補 `server.py` `_to_appeal_case` 與 09.1 `build_submission_from_case` 兩層轉換，讓 `deduct_amount`／`order_seq` 正確映射到 `orders[].points`/`total_qty`/`seq`，使紙本申復清單 PDF 金額/數量欄不再恆為空白。
**Verified:** 2026-08-10T07:54:22Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | **SC1**：`/api/sampling/audit` 與 `/api/appeal/generate` 不再以 `timeline=None` 呼叫引擎——實際具現化 `LocalFileProvider` 並傳入真實半年病史時間軸 | ✓ VERIFIED | server.py:562-564（audit：`_records_provider()` → `_resolve_records_source()` → `run_presubmission_check(case, soap_doc, timeline)`）；server.py:859-860 + 866（generate：同 helper → `build_appeal_draft(..., timeline=timeline)`）；server.py:435-447（`_records_provider()`：RECORDS_DIR 存在→LocalFileProvider）；server.py:450-466（`_resolve_records_source` 四態） |
| 2 | **SC2**：前端對「病歷缺席降級」有可見呈現（非「從未嘗試查詢」），三態文案可區分 | ✓ VERIFIED | index.html:527-532（sampling 面板 `if (data.records_degraded)` innerText 前置「⚠ 病歷缺席：」）；index.html:595-600（appeal 面板 aSec2 前置同文案）；server.py:428-432（三態固定中文文案 unconfigured/no_record_no/absent）；P1-5 防線 innerText 非 innerHTML |
| 3 | **SC3**：CSV 匯入與 API 生成兩路徑的紙本申復清單金額欄在來源存在時正確顯示，不再恆空白＋警告 | ✓ VERIFIED | CSV：test_case_to_submission.py:269-289（真實 `_to_appeal_case` 輸出 → `build_submission_from_case` → `build_rows`，`rows[0]["金額"]=="300"`、`"金額" not in warnings`）；API：test_server_case_store_integration.py:244-271（generate 帶 order_seq → 回應 `order_seq/p1_order_seq=="3"`，供 `_find_order_by_seq` 命中） |
| 4 | **SC4**：全套件回歸綠燈，新增/修改測試覆蓋真實資料流（非手工 fixture 掩蓋） | ✓ VERIFIED | 實跑全量：438 passed / 2 skipped / 0 failed（4 段，基線一致）；整合鏈測試走真實 `server._to_appeal_case`；Provider 注入走 tmp 真實 records.json；前端→API 請求體真實路徑（詳見 Data-Flow Trace） |
| 5 | `_to_appeal_case` 輸出含 `order_seq`（透傳 rec.order_seq，None 照印） | ✓ VERIFIED | server.py:414 `"order_seq": rec.order_seq`；test_case_to_submission.py:238-250（`out["order_seq"]=="3"`）；:299-301（缺省 None 照印） |
| 6 | `build_submission_from_case` 單筆構造分支將 deduct_amount 映射至 orders[0].points（非 None 才加鍵）；order_seq→seq | ✓ VERIFIED | case_to_submission.py:102-107（seq 與 points 皆來源存在才加鍵）；test_case_to_submission.py:253-266（seq=="3"、points==300）；:303-306（缺省無 seq/points 鍵） |
| 7 | `/api/appeal/generate` 帶 order_seq → 回應 render_appeal_json 契約的 order_seq/p1_order_seq 為該值；缺省 None 不捏造 | ✓ VERIFIED | server.py:807+834（`_clean_str` 收欄、`order_seq or None` 入 DeductionRecord）；test_server_case_store_integration.py:244-271（"3" 透傳、缺省 `is None`） |
| 8 | 前端 `generateAppealDraft` 請求 body 帶 `order_seq: currentSelectedCase.order_seq`（不再漏送） | ✓ VERIFIED | index.html:563；test_ingest.py:216-225（全文子字串靜態回歸） |
| 9 | RECORDS_DIR 存在＋請求帶病歷號 → audit 以 LocalFileProvider 實查半年病史並傳 timeline（records_degraded=false、records_source=ok） | ✓ VERIFIED | settings.py:28（RECORDS_DIR env 覆寫）；test_server_case_store_integration.py:295-314（monkeypatch RECORDS_DIR → tmp 真實 records.json → `records_degraded is False`、`records_source=="ok"`、reason None） |
| 10 | generate 帶 record_no（前端 aRecordNoInput 輸入欄送出）→ timeline 傳給 build_appeal_draft，②醫療必要性含半年病史 | ✓ VERIFIED | index.html:568（`record_no: document.getElementById('aRecordNoInput').value`）；server.py:802/859-860/866；test_server_case_store_integration.py:404-478（Test 1：`records_degraded is False`＋`"半年病史" in sections[1]["text"]`） |
| 11 | 病患無病歷（absent）維持 C5 降級但與「來源未設定」（unconfigured）在回應與前端可區分；RecordProviderError 穿透 500 | ✓ VERIFIED | server.py:450-466（absent/unconfigured/no_record_no 四態）；test_server_case_store_integration.py:317-338（absent＋reason「查無此病患病歷檔案」）；:340-358（unconfigured＋reason「病歷來源未設定（病歷目錄不存在）」）；:361-379（no_record_no）；:382-401（JSON 損毀 → 500、status=error） |
| 12 | 前端對 records_degraded 有可見呈現（sampling 徽章區＋appeal 面板，含原因文案） | ✓ VERIFIED | index.html:527-532/595-600（innerText）；test_ingest.py:248-262（四子字串靜態回歸：`data.records_degraded`、`data.records_degraded_reason`、`record_no: currentSelectedCase.record_no`、`病歷缺席`） |

**Score:** 12/12 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `server.py` | `_to_appeal_case` 輸出含 order_seq；generate handler 透傳 order_seq 至 DeductionRecord；`_records_provider()`／`_resolve_records_source()`／audit+generate timeline 接線／records_source 四態回應鍵 | ✓ VERIFIED | :388-421（order_seq 透傳）；:435-466（Provider 具現化＋四態 helper）；:562-564/615-623（audit 接線）；:802-866/895-899（generate 接線） |
| `src/elc_audit_engine/generators/appeal_print/case_to_submission.py` | 單筆案件 orders 構造分支映射 deduct_amount→points（非 None 才加鍵）＋order_seq→seq | ✓ VERIFIED | :102-107（seq/points 條件加鍵）；docstring :20-22 同步 |
| `config/settings.py` | RECORDS_DIR（env 覆寫、預設 DATA_DIR/samples/records） | ✓ VERIFIED | :28（比照 FACILITY_CONFIG_PATH 模式） |
| `static/index.html` | generateAppealDraft 帶 order_seq；aRecordNoInput 病歷號輸入欄；generate record_no 自輸入欄取值；兩面板 records_degraded 呈現（innerText）；sampling 請求帶 record_no | ✓ VERIFIED | :266-267（輸入欄）；:491（sampling record_no）；:563/568（generate order_seq＋record_no）；:527-532/595-600（呈現） |
| `tests/test_case_to_submission.py` | 整合鏈測試（真實 server._to_appeal_case 輸出）；4 個既有斷言同步 | ✓ VERIFIED | :238-310（4 個新測試）；:123-125/:180-182/:208-213（既有斷言含 points=300，行為語意變更同步，非放寬） |
| `tests/test_server_case_store_integration.py` | audit/generate timeline 四態測試（tmp 真實 records.json 注入）；generate order_seq 透傳測試；tmp_upload_dirs autouse fixture | ✓ VERIFIED | :277-478（_write_records_fixture＋6 個測試）；:244-271（order_seq）；:23（fixture） |
| `tests/test_ingest.py` | 前端靜態回歸：order_seq／aRecordNoInput／records_degraded 呈現 | ✓ VERIFIED | :216-262（3 個新測試） |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `server._to_appeal_case` 輸出 | `build_submission_from_case` | orders[0].seq=order_seq、orders[0].points=deduct_amount（非 None 才加鍵） | ✓ WIRED | server.py:414 → case_to_submission.py:102-107 |
| submission.orders | build_rows 金額欄 | `_find_order_by_seq(submission, order_seq)` 命中 → `matched['points']` | ✓ WIRED | field_mapping.py:97-106 + :164-177；整合鏈測試 :269-289 |
| generate 請求 body order_seq | render_appeal_json order_seq/p1_order_seq | `DeductionRecord(order_seq=order_seq)` | ✓ WIRED | server.py:834 → appeal.py:486/494；測試 :244-271 |
| `server._records_provider()` | `record_aggregator`（LocalFileProvider/build_timeline） | RECORDS_DIR 存在→provider；`build_timeline(provider, record_no)` | ✓ WIRED | server.py:27 import；:435-447；:461 |
| audit request record_no | `run_presubmission_check(case, soap_doc, timeline)` | `build_timeline(...).timeline` | ✓ WIRED | server.py:545/562-564 |
| 前端 aRecordNoInput | `build_appeal_draft(record, timeline=timeline)` | generateAppealDraft body record_no → `_resolve_records_source` | ✓ WIRED | index.html:568 → server.py:802/860/866；測試 :404-478＋:228-245 |
| audit/generate 回應 records_degraded | 前端呈現 | `if (data.records_degraded)` → innerText 前置 | ✓ WIRED | index.html:527/595；測試 :248-262 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| audit `records_degraded` | `result.comparison.records_degraded` | run_presubmission_check ← timeline（`build_timeline` 或 None） | 是（真實 Provider 查詢） | ✓ FLOWING |
| generate `sections[1].text`（②醫療必要性） | timeline | `build_timeline(provider, record_no).timeline` → build_appeal_draft | 是（`"半年病史" in sections[1]["text"]` 實測） | ✓ FLOWING |
| build_rows 金額欄 | `matched['points']` | submission.orders ← `_to_appeal_case` 真實 deduct_amount | 是（CSV 路徑 `"300"` 實測） | ✓ FLOWING |
| generate 請求 record_no | `document.getElementById('aRecordNoInput').value` | 前端輸入欄（使用者輸入） | 是（靜態回歸鎖定取值＋server 測試同體請求） | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| 3 個改動測試檔（含新增整合鏈/timeline/靜態回歸） | `pytest tests/test_case_to_submission.py tests/test_server_case_store_integration.py tests/test_ingest.py -q` | 56 passed | ✓ PASS |
| 全量回歸（組 2） | `pytest tests/test_appeal_print.py ... test_chroma_store.py -q` | 155 passed, 2 skipped | ✓ PASS |
| 全量回歸（組 3） | `pytest tests/test_comparator.py ... test_record_aggregator.py -q` | 105 passed | ✓ PASS |
| 全量回歸（組 4） | `pytest tests/test_reinforcement_report.py ... test_table_ocr.py -q` | 122 passed | ✓ PASS |
| 全套件合計 | — | **438 passed / 2 skipped / 0 failed**（基線一致，無迴歸、無新增 skip） | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| REQ-record-aggregator | 11.1-02（重新開放） | Provider 介面＋LocalFileProvider＋半年病史時間軸整合進生產路徑 | ✅ satisfied（重新達成） | `_records_provider()`（server.py:435-447）在 RECORDS_DIR 存在時具現化 `LocalFileProvider`，audit/generate 兩端點皆以 `build_timeline` 實查並傳入引擎——Provider 已首次在真實服務路徑被具現化（BLOCKER-1 閉合） |
| REQ-paper-appeal-print | 11.1-01 | 下游整合缺口（BLOCKER-2 金額/醫令序鏈斷裂）閉合 | ✅ satisfied（整合層修復） | CSV 路徑金額欄=points（實測 "300"）；API 路徑 order_seq/p1_order_seq 透傳可命中 `_find_order_by_seq`；數量欄留空為使用者已確認的已知限制（來源契約無 total_qty） |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| — | — | 無（未發現 stub／unwired／hollow artifact） | — | — |

### Human Verification Required

無。前端呈現為純文字 innerText 賦值（固定 id 元素 sAdviceBox/aSec2），內容由靜態回歸測試鎖定，無 CSS 佈局／動畫／交互流程變更；程式化驗證已充分覆蓋「有可見呈現」之 truth。

### Gaps Summary

無。4 條 Success Criteria 全數 VERIFIED（12/12 truths）。BLOCKER-1（Phase 4 病歷時間軸未接入）與 BLOCKER-2（金額/數量欄位鏈兩層轉換斷裂）均已閉合，有程式碼＋測試＋全量回歸三重證據。

**注意事項（非 gap，供 orchestrator 知悉）：**
- 兩個 plan 的 per-task git commit 因 reasonix 沙箱 `.git/` 唯讀未能執行——SUMMARY 已列出建議 commit 序列，需宿主在沙箱外補執行（TDD RED/GREEN 分批 commit 門尚未落地，屬流程完整性問題而非功能缺口）。
- 預設 `data/samples/records` 目錄不存在屬「病歷來源未設定」的正常 unconfigured 態，由部署方建立目錄或設 `RECORDS_DIR` 即啟用實查——誠實降級語意，非 stub。

---

_Verified: 2026-08-10T07:54:22Z_
_Verifier: Claude (gsd-verifier)_
