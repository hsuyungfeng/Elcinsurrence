---
phase: 11.1-close-milestone-audit-gaps-phase-4-flask-api-phase-9-9-1-11
plan: 2
subsystem: api
tags: [flask, record_aggregator, localfileprovider, build_timeline, records_degraded, appeal, frontend, tdd]

# Dependency graph
requires:
  - phase: 11.1-01
    provides: "generate handler order_seq 透傳（DeductionRecord.order_seq）"
  - phase: 4
    provides: "RecordProvider ABC / LocalFileProvider（records.json 契約）/ build_timeline（半年窗）/ AggregationResult（degraded 語意）"
  - phase: 5
    provides: "run_presubmission_check（timeline 參數）、comparator records_degraded = timeline is None"
  - phase: 7
    provides: "build_appeal_draft 的 timeline 參數（appeal.py:351）＋ build_necessity（病歷缺席降級文字）"
provides:
  - "`settings.RECORDS_DIR`（env 覆寫，預設 DATA_DIR/samples/records，比照 FACILITY_CONFIG_PATH 模式）"
  - "`server._records_provider()`（RECORDS_DIR 存在→LocalFileProvider；不存在→None＝來源未設定，非病患缺席）"
  - "`/api/sampling/audit` 與 `/api/appeal/generate` 不再以字面 timeline=None 呼叫——帶病歷號且來源存在時傳入真實半年病史時間軸"
  - "回應含 `records_source`（ok/absent/unconfigured/no_record_no）＋`records_degraded_reason`（固定中文模板，T-1112-01）＋`records_degraded`"
  - "`RecordProviderError` 穿透統一 500（P0-2），不降級成業務結論"
  - "前端 appeal 面板病歷號輸入欄（aRecordNoInput，plan-checker BLOCKER 2 使用者裁示）＋兩面板 records_degraded 可見呈現（innerText）"
affects: [11.1-03, milestone v1.0 audit closure, phase-10-his-integration（雲端 Provider 替換 LocalFileProvider 的切換點）]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Provider 具現化＋records_source 四態解析抽成共用 helper（_records_provider/_resolve_records_source），audit/generate 兩端點同一語意"
    - "records_degraded 以 timeline is None 計算（與 comparator.py:141 同源），原因文案集中於模組級模板映射（固定中文、不含路徑/病歷號）"
    - "病歷缺席（PatientRecordsNotFound）維持 C5 降級語意但回應與前端明確區分『已查詢、病患缺席』與『來源未設定』——不偽裝成『從未嘗試查詢』"
    - "前端呈現一律 innerText 賦值，不引入 innerHTML 拼接 API 欄位（P1-5 防線）"

key-files:
  created: []
  modified:
    - config/settings.py
    - server.py
    - static/index.html
    - tests/test_server_case_store_integration.py
    - tests/test_ingest.py

key-decisions:
  - "timeline 解析抽成 `_resolve_records_source(provider, record_no) -> (timeline, records_source)` 供 audit/generate 共用（plan 原『加入與 Task 1 相同的解析』，抽 helper 確保兩端點四態語意一致，邏輯順序與 plan action 完全一致）"
  - "generate 回應的 `records_degraded = timeline is None`（與 comparator.py:141 同源語意），非獨立狀態變數"
  - "降級原因文案集中於模組級 `_RECORDS_DEGRADED_REASONS`（unconfigured/no_record_no/absent 固定中文模板，T-1112-01），audit/generate 共用，避免文案漂移"
  - "前端 generate 請求 record_no 自 aRecordNoInput 輸入欄取值（取代 currentSelectedCase.record_no——該值恆 None）；sampling 請求 record_no 走 currentSelectedCase.record_no，兩處寫入不衝突"

patterns-established:
  - "Pattern 1: 病歷來源狀態以 records_source 四態明示（ok/absent/unconfigured/no_record_no），回應與前端皆可區分『已查詢病患缺席』與『來源未設定/未提供病歷號』"
  - "Pattern 2: infra 故障（RecordProviderError）不捕捉、穿透統一 500——系統故障不得偽裝成業務結論（P0-2/T-1112-03）"

requirements-completed: [REQ-record-aggregator]

# Metrics
duration: 8min
completed: 2026-08-10
---

# Phase 11.1 Plan 02: Phase 4 病歷時間軸接入 Flask API — RECORDS_DIR / LocalFileProvider 接線與 records_degraded 前端呈現

**BLOCKER-1 閉合：`LocalFileProvider`/`build_timeline` 首次在真實服務路徑被具現化——`/api/sampling/audit` 與 `/api/appeal/generate` 帶病歷號且來源存在時以半年病史時間軸呼叫比對/生成引擎（不再恆以 timeline=None 降級），回應含 records_source 四態＋中文原因文案，前端兩面板對病歷缺席有可見呈現且 appeal 面板新增可選病歷號輸入欄**

## Performance

- **Duration:** 8 min
- **Started:** 2026-08-10T07:38:40Z
- **Completed:** 2026-08-10T07:45:40Z
- **Tasks:** 3（Task 1/2 皆 tdd）
- **Files modified:** 5（+ SUMMARY 1）

## Accomplishments

- **Task 1（audit 端點 Provider 接線）**：`config/settings.py` 新增 `RECORDS_DIR`（env 覆寫、預設 `DATA_DIR/samples/records`，比照 FACILITY_CONFIG_PATH 模式）；`server.py` 新增模組級 `_records_provider()`（RECORDS_DIR 存在→`LocalFileProvider`，不存在→None＝來源未設定）與 `_resolve_records_source()`（timeline 解析＋records_source 四態）；`/api/sampling/audit` 以 `build_timeline(provider, record_no).timeline` 取代寫死的 `run_presubmission_check(case, soap_doc, None)`；回應加 `records_source`／`records_degraded_reason`（僅降級時給中文原因，不含路徑/病歷號）；`RecordProviderError` 不捕捉（穿透統一 500，P0-2/T-1112-03）。
- **Task 2（generate 端點＋前端病歷號輸入欄）**：generate handler 收可選 `record_no`（`_clean_str`），timeline 解析同 Task 1（共用 helper），`build_appeal_draft(..., timeline=timeline)`；回應新增 `records_degraded`（=`timeline is None`，與 comparator.py:141 同源）／`records_source`／`records_degraded_reason` 三鍵，render_appeal_json 既有鍵全部不變；前端 appeal 面板 top bar 新增病歷號輸入欄 `aRecordNoInput`（label「病歷號（選填，查半年病史）」、placeholder「M220518024」，plan-checker BLOCKER 2 使用者裁示），`generateAppealDraft` 請求 body 的 `record_no` 自輸入欄取值——無值由 server 端誠實標記 no_record_no。
- **Task 3（records_degraded 前端呈現）**：sampling 回應處理在 `sAdviceBox` 前置「⚠ 病歷缺席：${reason}」＋badge 邏輯之後（innerText 非 innerHTML）；appeal 回應處理在 `aSec2` 內容前加「⚠ 病歷缺席：${reason}\n」（reason None 時用預設文案防呆）；sampling 請求 body 帶 `record_no: currentSelectedCase.record_no`（generate 請求 record_no 由 Task 2 的 aRecordNoInput 負責，未重複加入）；新增靜態回歸測試鎖定四子字串。

## Task Commits

**⚠️ 沙箱環境阻擋 commit（詳見 Issues Encountered）：** reasonix bwrap 沙箱對 `.git/` 完全唯讀（與 11.1-01 同一環境限制），`git commit` 一律失敗（`cannot create .git/index.lock: read-only file system`）。所有程式碼修改與測試驗證已完成，但 per-task commit 未能執行。

建議由 orchestrator／宿主執行之 commit 序列（TDD RED→GREEN）：

1. `test(11.1-02): add failing tests for audit timeline injection (RECORDS_DIR/LocalFileProvider)` — files: `tests/test_server_case_store_integration.py`
2. `feat(11.1-02): RECORDS_DIR 設定 ＋ _records_provider() ＋ /api/sampling/audit timeline 接線 ＋ records_source/records_degraded_reason 回應鍵` — files: `config/settings.py` `server.py`
3. `test(11.1-02): add failing tests for appeal generate timeline + frontend record no input` — files: `tests/test_server_case_store_integration.py` `tests/test_ingest.py`
4. `feat(11.1-02): /api/appeal/generate timeline 接線 ＋ 前端 aRecordNoInput 病歷號輸入欄 ＋ records_degraded 三鍵` — files: `server.py` `static/index.html`
5. `test(11.1-02): add static regression for records_degraded rendering` — files: `tests/test_ingest.py`
6. `feat(11.1-02): 前端 records_degraded 可見呈現（兩面板）＋ sampling 請求帶 record_no` — files: `static/index.html`
7. `docs(11.1-02): complete 病歷時間軸接入 Flask API plan summary` — files: `.planning/phases/11.1-close-milestone-audit-gaps-phase-4-flask-api-phase-9-9-1-11/11.1-02-SUMMARY.md`

_註：每個 RED commit 對應之測試在寫入當下已實跑並確認失敗（Task 1: 5 failed；Task 2: 2 failed，fail-fast gate 已驗證），GREEN 後全綠；因沙箱限制，RED/GREEN 分批 commit 以建議命令呈現。_

## Files Created/Modified

- `config/settings.py` - 新增 `RECORDS_DIR = os.getenv("RECORDS_DIR", os.path.join(DATA_DIR, "samples", "records"))`（比照 FACILITY_CONFIG_PATH env 覆寫模式）
- `server.py` - import `config.settings`／`elc_audit_engine.record_aggregator`；`_records_provider()`／`_resolve_records_source()`／`_RECORDS_DEGRADED_REASONS`；audit/generate 兩端點 timeline 接線與 records_source/records_degraded/records_degraded_reason 回應鍵
- `static/index.html` - appeal 面板 `aRecordNoInput` 病歷號輸入欄；`generateAppealDraft` 請求 body `record_no` 自輸入欄取值；`runSamplingAudit` 請求 body 帶 `record_no: currentSelectedCase.record_no`＋回應 records_degraded 呈現；`generateAppealDraft` 回應在 aSec2 前加「⚠ 病歷缺席」呈現
- `tests/test_server_case_store_integration.py` - 新增 `_write_records_fixture` helper＋5 個 audit timeline 測試（ok/absent/unconfigured/no_record_no/500）＋`test_generate_appeal_draft_timeline_injection`（Test 1-4）
- `tests/test_ingest.py` - 新增 `test_index_html_appeal_record_no_input`（aRecordNoInput 靜態回歸）＋`test_index_html_renders_records_degraded`（records_degraded 呈現靜態回歸）

## Decisions Made

- 抽取 `_resolve_records_source(provider, record_no) -> (timeline, records_source)` 供 audit/generate 共用（plan Task 2 原文「加入與 Task 1 相同的 timeline 解析」——抽 helper 確保兩端點四態語意一致，邏輯順序與 plan action 完全一致）。
- generate 回應 `records_degraded = timeline is None`（與 comparator.py:141 `records_degraded=timeline is None` 同源語意），非獨立狀態變數。
- 降級原因文案集中於模組級 `_RECORDS_DEGRADED_REASONS` 映射（unconfigured「病歷來源未設定（病歷目錄不存在）」／no_record_no「請求未提供病歷號」／absent「查無此病患病歷檔案」），audit/generate 共用，避免文案漂移，且不含路徑/病歷號（T-1112-01）。
- 前端兩處 record_no 寫入點分工：generate 請求自 `aRecordNoInput` 輸入欄取值（Task 2 負責），sampling 請求走 `currentSelectedCase.record_no`（Task 3 負責）——不衝突。
- 測試病歷 fixture 日期取 `2026-07-01`，落在 `build_timeline` 預設半年窗（以執行當日 2026-08 為 end_date）內，Test 1 才能得到 timeline 非 None（ok 態）。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] reasonix 沙箱對 `.git/` 唯讀 → per-task commit 無法執行**
- **Found during:** 全 plan（環境限制，與 11.1-01 相同）
- **Issue:** bwrap 沙箱只對 PLAN `files_modified` 清單檔案開放寫入，`.git/`（index/objects/refs）完全唯讀，`git commit` 失敗（`cannot create .git/index.lock: read-only file system`）。
- **Fix:** 依交辦指示跳過實際 commit，所有程式碼修改與測試驗證照常完成；SUMMARY「Task Commits」列出建議 commit 序列（test/feat/docs 分批），由 orchestrator 在沙箱外代為執行。
- **Files modified:** 無（環境層，非程式）
- **Verification:** 全量測試 438 passed / 2 skipped（分 4 段執行）。
- **Committed in:** 未 commit（屬建議 commit 1-7 內容）

---

**Total deviations:** 1 auto-fixed（1 blocking，環境層）
**Impact on plan:** 無程式偏差。TDD RED/GREEN 測試門已實跑驗證（RED 失敗→GREEN 全綠）；僅 commit gate 需宿主補執行。無 scope creep、無架構變更。

## Issues Encountered

- **reasonix 沙箱阻止 git commit（與 11.1-01 同一環境限制，已沿用解決方式）**：見上 Deviations。SUMMARY 檔本身已照常建立（11.1-02-SUMMARY.md），內容含建議 commit 序列與完整驗證記錄。
- **bash 沙箱預綁定允許寫入檔案**：執行初期 bash 工具因預綁定尚不存在的 `11.1-02-SUMMARY.md` 而報 bwrap 錯誤；以 write_file 先建立佔位檔後恢復正常（SUMMARY 最終內容於本檔覆寫）。此為沙箱工具行為，非專案問題。
- **全量測試超時**：單次 `pytest -q` 全量接近 bash 工具 2 分鐘上限，改以 4 段執行（組 A 169+2skipped／組 B 103／組 C 68／組 D 98），合計 **438 passed / 2 skipped / 0 failed**（432 collected 基線 430+2 新增 8 個測試，無迴歸、無新增 skip）。

## Known Stubs

None。audit/generate 兩端點在「來源未設定／未提供病歷號／病患缺席」三態皆誠實降級並明示原因，無隱藏 stub；預設 `data/samples/records` 目錄不存在屬「病歷來源未設定」的正常 unconfigured 態，由部署方建立目錄或設 `RECORDS_DIR` 即啟用實查。

## User Setup Required

None - 無外部服務設定需求（`RECORDS_DIR` 為可選 env 覆寫；不設定時預審/申復誠實標記 unconfigured 降級）。

## Next Phase Readiness

- BLOCKER-1（Phase 4 病歷時間軸未接入 Flask API）閉合：audit/generate 兩端點帶病歷號且來源存在時以 LocalFileProvider 實查半年病史並傳入引擎（Success Criteria 1）；前端對 records_degraded 有可見呈現且「已查詢、病患缺席」與「來源未設定」可區分（Success Criteria 2）。
- 剩餘 Phase 11.1 工作：11.1-01（金額/數量鏈）＋本 plan 皆已執行，待宿主補執行兩 plan 之建議 commits 後 Phase 11.1 可收尾。
- 雲端 Provider（Phase 10 阻塞解除後）只需替換 `_records_provider()` 的實作（換成雲端 Provider），`_resolve_records_source` 四態語意與前端呈現不變。
- 待宿主補執行 commits 後，本 plan 可視為完成。

## Self-Check: FAILED (commit 檢查)

- ✅ `[ -f .planning/phases/11.1-close-milestone-audit-gaps-phase-4-flask-api-phase-9-9-1-11/11.1-02-SUMMARY.md ]` → FOUND
- ✅ `[ -f config/settings.py ]` / `[ -f server.py ]` / `[ -f static/index.html ]` / `[ -f tests/test_server_case_store_integration.py ]` / `[ -f tests/test_ingest.py ]` → FOUND
- ✅ 全量測試復驗：438 passed / 2 skipped / 0 failed（436 前段測試 + 分 4 段執行，含本 plan 8 個新增測試全綠）
- ✅ plan `<verification>` 命令（`pytest tests/test_server_case_store_integration.py tests/test_ingest.py::test_index_html_renders_records_degraded -q`）→ 42 passed
- ❌ `git log --oneline` 查無本 plan 之 commit hash —— **reasonix 沙箱對 `.git/` 唯讀**，`git commit` 無法執行（與 11.1-01 相同環境限制）。建議 commit 序列見上「Task Commits」，需宿主在沙箱外補執行。

---

*Phase: 11.1-close-milestone-audit-gaps-phase-4-flask-api-phase-9-9-1-11*
*Completed: 2026-08-10*
