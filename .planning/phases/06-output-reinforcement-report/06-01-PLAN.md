---
phase: 06-output-reinforcement-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/elc_audit_engine/generators/__init__.py
  - src/elc_audit_engine/generators/reinforcement_report.py
  - src/elc_audit_engine/generators/tracking.py
  - tests/test_reinforcement_report.py
autonomous: true
requirements: [REQ-output-reinforcement-report]

must_haves:
  truths:
    - "純渲染層：無 LLM、無規則庫查詢（06-CONTEXT.md domain）"
    - "輸出 Markdown checkbox 逐條審格式（D8/D-01）；候選補強敘述為 checkbox [ ] 初始未勾選（D-02）"
    - "內容含醫令支持度缺口（✅充分/⚠️薄弱/❌裸奔/❓查無規則）、半年病史摘要、候選補強敘述（逐條點選）"
    - "審核軌跡 JSON：每條狀態（採用/編輯後採用/略過/標記不符事實）、原文、編輯後文、時間（D9/D-03/D-04）"
    - "render_report/render_tracking 為純函式；檔案輸出為薄包裝（D-06/D-05）"
  artifacts:
    - path: "src/elc_audit_engine/generators/reinforcement_report.py"
      provides: "render_report(comparison) -> str：Markdown checkbox 報告"
    - path: "src/elc_audit_engine/generators/tracking.py"
      provides: "render_tracking(comparison, decisions, reviewed_at=None) -> str：審核軌跡 JSON"
    - path: "src/elc_audit_engine/generators/reinforcement_report.py"
      provides: "write_report(output_dir, case_record_no, comparison, decisions=None)：寫 .md＋.json"
    - path: "tests/test_reinforcement_report.py"
      provides: "報告結構/徽章/checkbox/警告區/病史摘要/軌跡 JSON/檔案輸出測試"
  key_links:
    - from: "src/elc_audit_engine/generators/reinforcement_report.py"
      to: "src/elc_audit_engine/comparator/models.py"
      via: "from elc_audit_engine.comparator.models import CaseComparisonResult"
      pattern: "CaseComparisonResult"
    - from: "src/elc_audit_engine/generators/tracking.py"
      to: "src/elc_audit_engine/comparator/models.py"
      via: "from elc_audit_engine.comparator.models import CaseComparisonResult"
      pattern: "CandidateNarrative"
---

<objective>
Wave 1（Phase 6 唯一一輪）：交付病歷補強報告生成器，滿足 REQ-output-reinforcement-report
三項驗收：Markdown checkbox 檢核表格式、含支持度缺口＋半年病史摘要＋候選補強
敘述（逐條點選）、醫師可勾選/編輯後存檔並留審核軌跡。

依 06-CONTEXT.md：純渲染層；報告結構（D-01）；checkbox 初始未勾選（D-02）；
審核軌跡 JSON 四狀態＋原文＋編輯後文＋時間（D-03/D-04）；檔案輸出薄包裝（D-05）；
核心純函式（D-06）。

Output: 2 個 generators 模組（reinforcement_report/tracking）＋對外 API＋
1 個測試檔，全部測試綠。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-output-reinforcement-report/06-CONTEXT.md
@.planning/intel/decisions.md（D8/D9）
@src/elc_audit_engine/comparator/models.py
@config/settings.py
</context>

<interfaces>
<!-- 既有介面，Phase 6 消費 -->

Phase 5 輸出（唯一輸入）:
```python
CaseComparisonResult(
    case_record_no, order_judgments, records_degraded,
    unknown_orders, manual_review_orders,
)
OrderJudgment(order_code, order_seq, rule_found, rule_source,
              check_item, judgment, support_level, manual_review,
              note, narratives)
Judgment(verdict, quote, reason)
CandidateNarrative(text, rule_location, prompt_only)
```

設定（Phase 1）:
```python
OUTPUT_DIR  # env-overridable，write_report 預設輸出目錄
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 報告渲染（reinforcement_report.py）</name>
  <files>src/elc_audit_engine/generators/reinforcement_report.py, tests/test_reinforcement_report.py</files>
  <read_first>
  - 06-CONTEXT.md D-01/D-02/D-06
  - src/elc_audit_engine/comparator/models.py
  </read_first>
  <automated>
    - render_report 含標題＋病歷號；含 ✅充分/⚠️薄弱/❌裸奔/❓查無規則 徽章
    - 候選補強敘述為 `- [ ] 敘述（出處）` checkbox（初始未勾選）
    - records_degraded=True → 含「⚠本報告未含病史佐證」
    - unknown_orders/manual_review_orders → 警告區列出
    - 半年病史摘要區（timeline 存在時）：四類筆數＋最近 3 筆摘要
  </automated>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 審核軌跡（tracking.py）</name>
  <files>src/elc_audit_engine/generators/tracking.py, tests/test_reinforcement_report.py</files>
  <read_first>
  - 06-CONTEXT.md D-03/D-04/D-06
  </read_first>
  <automated>
    - render_tracking 輸出 JSON：每條含 narrative text（原文）、status（採用/編輯後採用/略過/標記不符事實/未審核）、edited_text（編輯後文）、reviewed_at
    - decisions 輸入：{narrative_index: (status, edited_text)}；未審核預設 status=未審核
    - reviewed_at 可注入（固定時間）；預設 ISO UTC
    - JSON 可被 json.loads 解析
  </automated>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 檔案輸出（write_report）</name>
  <files>src/elc_audit_engine/generators/reinforcement_report.py, tests/test_reinforcement_report.py</files>
  <read_first>
  - 06-CONTEXT.md D-05
  </read_first>
  <automated>
    - write_report(output_dir, case_record_no, comparison, decisions=None) 寫出
      `病歷補強報告_{病歷號}.md` ＋ `審核軌跡_{病歷號}.json`
    - 檔案存在且內容正確（.md 含標題、.json 可解析）
  </automated>
</task>

</tasks>
