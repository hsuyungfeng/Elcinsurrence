---
context: milestone
phase: 14-evidence-packet-print
task: 3
total_tasks: 3
status: complete
last_updated: 2026-08-11T07:22:01Z
---

# ✅ Milestone v1.1 — SHIPPED 2026-08-11

No blocking constraints. All phases complete, all tests green.

<current_state>
**Milestone v1.1「紙本→數位化整合三項輸出」** 已全數交付並 git push。
- Phase 12 ✅ 影像佐證上傳與關聯驅動
- Phase 13 ✅ 核減明細原格式列印
- Phase 14 ✅ 審核軌跡＋病歷摘要＋申復理由＋影像佐證包列印

Server was running at `http://127.0.0.1:5000` with:
```
ELC_API_KEYS="clinic:dev-key-for-local-dev-001" ELC_SERVER_DEBUG=true uv run python server.py
```

Full test suite: **458 passed, 2 skipped, 0 failed** (145.99s)
</current_state>

<completed_work>

### Phase 12: 影像佐證上傳與關聯驅動
- `attachment_store.py` — Magic Bytes 驗證 + `safe_filename` + PHI-zero audit log
- `POST /api/appeal/attachments/upload` / GET / DELETE
- `generators/appeal.py` `has_attachment` / `p7` 由實體檔案存在驅動

### Phase 13: 核減明細原格式列印
- `deduction_print/` — ODT ElementTree 動態 table-row 複製 + soffice headless PDF
- `POST /api/deduction/print` + `scripts/build_deduction_print.py`
- ODT template: placeholder scaffold (見 ⚠ 下方)

### Phase 14: 審核軌跡＋佐證包列印
- `evidence_packet/` — python-docx + Pillow + pillow_heif + pypdf 合成
- `POST /api/appeal/evidence-packet/print` + `scripts/build_evidence_packet.py`
- `builder.py` facility 參數修正已 commit

### Session修復
- `RCPI2012R01_核減明細表_print_base.odt` 從 2-byte 空佔位符重建為有效 ODT (commit `46ab11a`)
- `build_evidence_packet_docx()` 加入 `facility: dict | None = None` 參數
</completed_work>

<remaining_work>
無未完成任務。

⚠ **待辦（非阻塞）**:
- `RCPI2012R01_核減明細表_print_base.odt` 目前為最小 scaffold，版面與 NHI 官方紙本不同。待取得官方 RCPI2012R01 原始 ODT 後替換並重算 SHA256。
</remaining_work>

<decisions_made>
- **Phase 13 ODT 策略**: 官方 NHI RCPI2012R01 表單未取得，以手工建立的最小 ODT scaffold 確保 API 端到端可用，待官方表單補全。
- **`build_evidence_packet_docx` 簽名**: 新增 `facility: dict | None = None` 選填參數，向下相容，已修正 `render_evidence_packet` 傳參與建構器簽名不符的問題。
- **Flask 路由格式**: `generate_deduction_print` / `generate_evidence_packet_print` 均採用 lazy `from config import settings` 模式（避免模組載入時的 circular import）。
</decisions_made>

<blockers>
無阻塞問題。
</blockers>

## Required Reading (in order)
1. `.planning/ROADMAP.md` — 確認 v1.1 已標記 shipped，Phase 15+ 尚未規劃
2. `.planning/STATE.md` — 確認目前 Phase 14 complete
3. `src/elc_audit_engine/generators/deduction_print/` — 了解 ODT 列填充機制
4. `src/elc_audit_engine/generators/evidence_packet/` — 了解 DOCX+PDF 合成管線

## Critical Anti-Patterns (do NOT repeat these)
- **`from config import settings` 位置**: 必須在函式內 lazy import，不得放在模組頂層（否則 UnboundLocalError 因 Python 作用域提升）→ 已修正 `server.py:1088`
- **ODT 模板空佔位符**: `build_print_base` 未被 Phase 13 executor 呼叫，導致 ODT 仍為 2-byte 空白。下次模板建立必須執行 `uv run python -m ...template <src.odt> <out_base.odt>` 驗證 ZIP 格式

## Infrastructure State
- **Flask server**: 已停止（session 結束）。重啟：`ELC_API_KEYS="clinic:dev-key-for-local-dev-001" ELC_SERVER_DEBUG=true uv run python server.py`
- **Git**: pushed to `origin/main` (ahead 0, as of 2026-08-11 15:22)
- **Output files**: `data/output/核減明細_92d8e611.pdf`, `data/output/申復佐證包_201.pdf`

<context>
This session: Resumed from Phase 13 planning → completed Phase 13 execution (3 plans) → planned Phase 14 → executed Phase 14 (3 plans) → activated server and live-tested all new endpoints. Milestone v1.1 完整 shipped。
</context>

<next_action>
Run `/gsd-complete-milestone` to formally archive v1.1 and initialize v1.2 planning, OR run `/gsd-verify-work 14` for interactive UAT on Phase 14 佐證包 PDF visual review.
</next_action>
