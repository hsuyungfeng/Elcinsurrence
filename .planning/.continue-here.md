---
context: default
phase: 9-doctor-toolbox-his-integration (placeholder — not started)
task: n/a
total_tasks: n/a
status: paused
last_updated: 2026-08-03T11:24:25.833Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: `pageindex` PyPI package is a cloud SaaS — never import/call it anywhere in this codebase. The tree-structured index concept is implemented locally via `src/elc_audit_engine/rule_repository/docx_tree/` (python-docx + regex + JSON). Discovered during Phase 2 (violates D2 offline-only). — Structural mitigation: treat any future `pageindex` import as a build-breaking violation; the local implementation already covers the need.

**Do not proceed until the box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Worktree provisioning can intermittently branch from stale `origin/main` | Happened during Phase 2 (Plans 02-03) — spawned worktrees landed on a pre-bootstrap commit. Root cause: GitHub `origin/main` was 20 commits stale. | advisory | Keep `origin/main` in sync with local `main` via `git push` after every merge. Check `git log --oneline -3` for recent Phase work before trusting a spawned worktree. |
| Account monthly spend limit can terminate background agents mid-task without warning | Hit repeatedly in Phase 2 (parallel executors, mid-batch LLM build). | advisory | Terminated agents leave uncommitted-but-often-correct work — before re-spawning, `cd` into the worktree, check `git status --short`, run tests, commit if valid. Check `ps -p <pid>` (not `ps aux | grep`, which proved flaky) before assuming a background job died. |
| 測試全綠不代表沒有 bug | 本次 session 確認 201 passed / 1 skipped——使用者要修的 bug 位在測試未覆蓋路徑。 | advisory | 拿到 bug 線索後**先寫能重現的測試（紅）再修（綠）**，不得盲改。 |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
No phase is in progress. GSD Phase 1–8 are all complete and merged (HEAD `5ef4902`); Phase 9 (doctor-toolbox HIS 整合) is a placeholder per ROADMAP.md, not started. This is a clean pause point.

本次 session 的任務方向已定但未開始：使用者從選單選了「修 bug / 加功能」→「修 bug（我有具體線索）」，**線索尚未提供**。測試套件已跑：201 passed / 1 skipped / 4 warnings（皆預期降級警告）。工作區未提交僅 `.planning/` 交接檔本身（STATE.md 由 resume 流程更新）與 `reasonix.toml`（環境設定，不入庫）。
</current_state>

<completed_work>

- **GSD Phase 1（專案骨架）** — complete, merged
- **GSD Phase 2（規則庫建置）** — complete, merged, all 6 plans done. 3 acceptance criteria automated-and-passing; 20-code human spot-check 20/20 correct.
- **GSD Phase 3（解析器）** — complete: 申報 XML（Big5 偵測＋致命缺漏分級）、核減明細（D-14d 18 欄）、SOAP（marker/keyword 兩層＋信度，317 關鍵詞）。真實檔回放 633 案／2,624 醫令／0 拒收。
- **GSD Phase 4（病歷彙整器）** — complete: RecordProvider ABC＋LocalFileProvider＋build_timeline。
- **GSD Phase 5（三方比對器）** — complete: 檢核項、證據組裝、LLMJudger（JSON 強制＋重試）、classify_support 三級、LLMNarrativeGenerator。
- **GSD Phase 6（病歷補強報告）** — complete: render_report＋render_tracking＋write_report。
- **GSD Phase 7（申復理由草稿）** — complete: build_appeal_draft（D10 四段）＋字數控制器（Q15：每欄 1000／合計 2000）＋P6 硬檢查＋appeal JSON（p1-p9 契約）。
- **GSD Phase 8（端到端測試）** — complete: 金標準 30 組＋端到端 3 案例＋E2E-01 修正（classify_support「部分支持」→ 薄弱）＋五層測試策略全數涵蓋。
- **本次 session**：僅執行測試套件確認狀態（201 passed / 1 skipped / 4 warnings）＋詢問任務方向，未改任何程式碼。
</completed_work>

<remaining_work>

- **立刻要做**：向使用者索取 bug 線索（錯誤訊息／traceback／重現步驟／模組／預期 vs 實際行為），拿到後先寫重現測試（紅）再修（綠）。
- **若使用者改方向**：可能加功能（parsers / generators / comparator / rule_repository / pipeline / server.py）或盤點潛在問題。
- GSD Phase 9（doctor-toolbox HIS 整合佔位）— 依 ROADMAP.md 屬未來工作，非本次暫停點。
- 既有 forward-looking notes（來自 Phase 2 外部審查，未行動）：README 依賴說明、rule_mapping 版本追蹤、46% no-match 率（ChromaDB 檢索改善）、兩筆 drug CSV 給付規定截斷、無 lint/CI、build_rule_mapping 預掃效率——均非 blocking。
</remaining_work>

<decisions_made>

- 本次 session 無技術決策——僅確認測試狀態與任務方向（修 bug，等使用者線索）。
- 既有鎖定決策（carry forward）：字數上限以官方問答集 Q15 為準（p8/p9 各 1000 中文字、合計 2000）；appeal JSON 為 Phase 2 轉 XML 契約（p3/p4/p5 待真實改支檔）；核減輸入格式只認 D-14d（`officialdocument/電子申復文件格式/` 底下都是**輸出**規格，不得建模輸入）；reader 層參數化；fixture 去識別化只洗 `d49` 姓名；`get_rule()` 會拋 `RuleRepositoryError`（Phase 5 必須處理）。
</decisions_made>

<blockers>
- 使用者 bug 線索尚未提供（human_action，blocking）：無線索無從定位；測試全綠表示 bug 在測試未覆蓋路徑。
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — 專案整體狀態、Session Continuity、carry-into-planning 重點
2. `.planning/ROADMAP.md` — Phase 1–9 目標與成功條件
3. 與 bug 相關模組的 `XX-CONTEXT.md`／`XX-01-SUMMARY.md`（視使用者線索而定）
1. `.planning/METHODOLOGY.md`（若存在）— project analytical lenses

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: 在沒有可重現測試的情況下直接改程式碼 → 先寫重現測試（紅→綠）
- [ANTI-PATTERN]: 把 `officialdocument/電子申復文件格式/` 的輸出規格當輸入格式建模（D-14 已作廢，只認 D-14d）
- [ANTI-PATTERN]: 引入雲端 SaaS 套件（`pageindex`）破壞 D2 離線保證

## Infrastructure State
- llama.cpp server（localhost:8080）：僅金標準回放（`scripts/replay_gold_standard.py`）與 rule_mapping 重建需要，一般測試零 LLM 依賴
- LibreOffice headless（soffice）：docx-tree 建置需要，測試已用功能探測（`soffice --version`）優雅跳過
- 資料庫 `data/db/rules.sqlite3`：已建置（2,669 payment + 11,273 drug + rule_mapping）
- `data/rag/` ChromaDB：已建置（165 chunks），**零查詢邏輯**（延至 Phase 5 範圍）
- 本 session 未啟動任何 server／背景程序

## Pre-Execution Critique Required
<!-- 不適用：本次暫停點不在 design 與 execution 之間 -->

<context>
使用者要修一個具體 bug（有線索但未貼）。整個 session 只跑了測試＋問方向，零程式碼變更。專案 Phase 1–8 全綠、已提交。resume 的第一步不是寫程式，而是等使用者提供線索（或再次詢問）。
</context>

<next_action>
Start with: 向使用者索取 bug 線索（錯誤訊息／重現步驟／模組／預期 vs 實際行為）；取得後先寫失敗測試再修復。
</next_action>
