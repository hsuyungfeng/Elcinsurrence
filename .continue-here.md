---
context: default
phase: 09-HIS 服務化
task: P1-4
total_tasks: 2
status: paused
last_updated: 2026-08-05T15:42:00+08:00
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: 假資料比沒接線更危險 — server 端點曾以硬編碼 if/else 回「看似可用」的判定。新增端點/欄位前檢查是否有 demo/硬編碼值會被當成真實輸出。
- [ ] CONSTRAINT: 移除依賴前必須 grep 全部 import — 任何 pyproject.toml 依賴變更前，先全量核對 import。
- [ ] CONSTRAINT: 測試可能把 bug 鎖進規格 — 修 bug 時先檢查既有測試是否在鎖定錯誤行為。
- [ ] CONSTRAINT: 識別碼一律校驗原始值後拒絕（Reject over sanitize）— 先 basename 再校驗會將攻擊悄悄清洗成合法檔名。

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| 假數據冒充真實輸出 | server 端點硬編碼判定/名稱，未接引擎 | blocking | 端點必須接真實引擎 |
| 「清洗」把攻擊變成合法值 | safe_filename 先 basename 再校驗白名單 | blocking | 一律校驗原始值後拒絕，不做清洗改寫 |
| 休眠漏洞因功能上線而活化 | P1-5 innerHTML 於 CSV 匯入上線後成爆發點 | advisory | 功能上線時重評既有安全清單 |

<current_state>
**位置**：
1. P1-4 (CSV 內容 SHA-256 hash ＋ ChromaDB source_version 綁定與自動清掃) 完成並全量測試驗證（277 passed / 2 skipped）。
2. Phase 9 (HIS 服務化) PLAN 已初步劃分（09-01-PLAN.md, 09-02-PLAN.md）。
3. 實機串接受限外部依賴，拆分為 Phase 10（不規劃）。
</current_state>

<completed_work>
- **P1-4 安全與版本管理修復**：
  - `versions.py` `extract_csv_version()` 改為從 CSV 檔名日期＋內容 SHA-256 前 12 碼組建版本，避免換版不改名跳過增量更新。
  - `chroma_store.py` `build_chroma_collection()` 新增 `source_version` 綁定至 chunk metadata，自動比對現有 collection 版本；若版本更換則自動 purge 舊版本 chunk 再重新攝取。
  - `build_chroma_index.py` 自動計算並傳送 `source_version`。
  - `test_rule_mapping_versions.py` 與 `test_chroma_store.py` 補齊測試。
  - 全套件 277 passed / 2 skipped 通過。
- **GSD 規劃**：
  - 開出 Phase 9 服務化 PLAN。
</completed_work>

<remaining_work>
- 執行 Phase 9 PLAN（認證授權、案件狀態機、任務佇列等）。
- P2 體質改善項目（參見 deepflash4improve.md §7.2）。
- 待外部真實樣本（抽樣清單與核減實體檔到手後對接 reader 參數）。
</remaining_work>

<decisions_made>
- P1-4 CSV 內容版本包含檔名日期 + SHA-256 content hash，確保內容變更必定觸發 rebuild。
- ChromaDB 在 source_version 變更時自動 purge 舊版本記錄，避免 duplicate chunk 累積。
</decisions_made>

<blockers>
- 抽樣清單官方欄位契約樣本未取得（目前參數化待對接）。
- NHI_EIIAPI.DLL 及 Windows/VPN 實機未取得（移至 Phase 10 門控）。
</blockers>

## Required Reading

1. `.planning/STATE.md` — 專案整體狀態與最新進度。
2. `deepflash4improve.md` §7 — 安全與改善清單對照表。
